# 0.2.0-r15 运行验收记录

## 交付物

- 角色卡：`../dist/checkpoint-0.2.0-r15/幻想乡物语-测试检查点-0.2.0-r15.json`
- SHA-256：`51c0f0bfa487df32c3647a8ad9c9bb4ecc7276f1ecb1c206ef4ccf22dc03dab5`
- 文件大小：`19,965,515` bytes
- 生成时间：2026-07-23
- 目标环境：`F:\agent airp\Luker`
- Luker：`2.7.0 release`
- Tavern Helper / JS-Slash-Runner：`4.8.18`

## 自动与离线门禁

- `npm run build:ui`：通过。
- `npm run check:ui`：通过。
- `npm test`：17/17 通过。
- `node --check dist/runtime/ui-mount.js`：通过。
- `npm run package:checkpoint:dry`：通过。
- `git diff --check`：通过，仅有 Git 的 LF/CRLF 提示。
- 包复读：3 个角色脚本、16 条世界书、1 条 `[initvar]`、1 条 GAL 协议；r15 UI 脚本 ID 唯一。
- 离线预览：开场、庭院、主屋目标菜单、维修确认、施工切换、GAL 分段、建议回复、手动输入、收尾与返回庭院均可到达。
- 390×844 视口无横向溢出；离线预览无控制台 error。

## Luker 导入与宿主预检

1. 通过角色管理的本地文件入口导入 r15 JSON。
2. 选择“全部导入”保留角色标签。
3. 选择“是”导入内嵌世界书 `幻想乡物语·移动庭园 0.2.0-r15`。
4. 选择“确认”启用三个内嵌角色脚本。
5. 酒馆提示 MVU 脚本加载成功、变量初始化成功。
6. r15 新聊天显示“在穿过结界之前”的常驻单壳开场；原生消息与输入区在游戏模式下未向玩家显示。
7. 开场仍保持全新状态，没有替用户提交资料，也没有触发 LLM，留给所有者从零验收。
8. r14 角色卡和世界书保留，未覆盖、未删除。

宿主日志中只观察到进入页面前已有的 Stable Diffusion WebUI 连接错误；未发现由 r15 脚本产生的新 error。

## 所有者待验收

真实 LLM、Swipe、停止/续写和 MVU 结算需要使用所有者当前 API 配置与实际模型，因此本轮没有代替所有者消耗生成或提交开场。以下项目由所有者在当前 r15 新聊天中验收：

- 确定性开场写入与复读；
- 灵梦“对话 / 摸摸头”的真实多轮楼层；
- 建议回复与手动输入；
- 结束聊天的唯一结算；
- 停止后继续、左右 Swipe、重新生成；
- 旧主屋维修的资源、时间和允许结果；
- 原生聊天恢复及返回游戏；
- 刷新、切聊天、切卡后的单例与清理。

