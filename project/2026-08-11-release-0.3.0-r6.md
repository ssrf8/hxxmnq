# 0.3.0-r6 正式候选发布记录

日期：2026-08-11

## 发布结论

- 卡类型：SillyTavern `chara_card_v2`，MVU + Tavern Helper 嵌入式脚本，远程 production UI。
- 运行 profile：`standalone-mvu`。
- UI 通道：`production`，版本 `r6`；设置中的测试控制面板在 HTML 与 CSS 两层默认关闭。
- 公共素材：继续使用 R2 live generation 8；本次没有修改或重传素材 manifest。
- 创作者注释：`creator_notes` 为空字符串，JSON 与 PNG 解码结果均确认长度为 0。
- 状态：离线发布门通过；真实 SillyTavern 导入与运行验收仍待执行，因此标记为正式候选。

## R2 UI

- URL：`https://ssrfrrt.ccwu.cc/gensokyo-moving-garden/live/ui/profiles/standalone-mvu/ui-mount-r6.js`
- 字节数：2,243,823
- SHA-256：`deb1bfc4d38fcb3f0796022649b82e89a265584c5d9ada1fe914c38a3f41eadb`
- 缓存：版本化 UI 为 `public, max-age=31536000, immutable`；profile manifest 为 `no-store`。
- 上传顺序：先创建不可变 UI 对象，再条件更新 production manifest；两项均通过公网逐字节、MIME、Cache-Control 与 SHA-256 读回。

## 正式产物

### JSON

- 路径：`dist/checkpoint-0.3.0-r6/幻想乡物语-正式版-0.3.0-r6.json`
- 字节数：329,189
- SHA-256：`b9e779bde529cee633133bb25c4255bcbbb6cc8401e73bc141d3a5daaeeed518`
- 世界书：`幻想乡物语·移动庭园 0.3.0-r6`，30 条内嵌条目。
- UI 脚本：`gensokyo-garden-ui-020-r6`，内嵌 4,881-byte production loader；不含 test manifest 引用。

### PNG

- 路径：`dist/checkpoint-0.3.0-r6/幻想乡物语-正式版-0.3.0-r6.png`
- 字节数：1,273,524
- SHA-256：`13e8c99d052926b3fdf75efb9c66e3487521c6d1e8c8d20b79ee93023ffd41fd`
- 画布：512×853，可由 `pngjs` 正常解码。
- payload：恰好一份 `chara`；base64 解码字节与正式 JSON 完全一致。

## 验证

- `npm run check:ui`：通过。
- `node --test --test-concurrency=1 tests/*.test.mjs`：754/754 通过。
- production remote UI build、publish dry-run、R2 上传与公网读回：通过。
- production package dry-run 与正式写入：通过。
- JSON parse、正式卡名/版本、世界书绑定、loader 通道与 `creator_notes`：通过。
- PNG 签名、结构解码、payload 数量和 JSON parity：通过。
- `git diff --check`：通过。

## 运行依赖与边界

- 卡内已嵌入 MVU 固定版本加载器、MVU Schema、production UI loader、初始 `stat_data` 与绑定世界书。
- 宿主仍需项目既定的 SillyTavern、Tavern Helper、MVU/MagVarUpdate 能力；Node、TypeScript、esbuild、sharp 与 pngjs 仅是开发期工具，不是玩家安装项。
- UI 和媒体依赖 HTTPS R2；loader 会校验 production manifest、同源路径、字节数与 SHA-256，失败时不降级执行未校验代码。
