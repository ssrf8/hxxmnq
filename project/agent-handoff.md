# Agent 交接文档

> 2026-08-10（一百二十三，VisitTurn 最终精简 + 额外模型世界书确认）：取消多阶段兼容与重复审计设计。`interaction.visit_memory.*.turns` 仍只由 bridge settlement 写入，额外变量模型在世界书中被明确禁止读写整棵 `interaction.visit_memory`。新 VisitTurn 固定为 `turn_id`、`character_id`、`day`、`time_period`、`summary` 五个字段；request、attempt、commit、assistant message、swipe、scene 与 period serial 只留在事务生命周期/提交回执，不再复制进长期记忆。摘要由已接受正文确定性构造，保留玩家行动、角色回应和现场经过，限制 80–100 字；短内容补“只确认已发生事实”的边界句，不虚构剧情。项目无旧存档需求，因此 schema/normalize 直接裁掉额外 VisitTurn 字段，不提供迁移器。已确认 `scripts/package-checkpoint.mjs` 会把维护源直接嵌入常驻 `[mvu_update]` 变量规则/输出格式，并单独注入 D0 完整状态；规则层与输出层均禁止变量模型写 `interaction.visit_memory`，`routing-plan.json`、正式合同、README 与额外模型运行说明已同步。门禁：聚焦 136/136、`npm run check:ui` PASS、全量 `npm test` 722/722 PASS；未重新构建、打包、上传或发布，现有旧卡不会自动获得新世界书。

> 2026-08-10（一百二十二，GAL missing-turn 两阶段最小修复 + 变量路由抓包复核）：实机确认 `createChatMessages()` 在 Promise 完成前触发 MESSAGE_RECEIVED/MVU 事件，旧路径会让 settlement 先写入正确 VisitTurn、随后 assistant 落楼再用旧 `latest.state` 覆盖同一 message-scope。第一阶段只增加 assistant 持久化期间的 settlement 屏障，两条 assistant 落楼路径共用同一状态；所有者复测确认 persisted/finalize 顺序、missing-turn、精确 request turn 与下一次发送四项全部通过。第二阶段删除同一次正常 settlement 内两处重复 `persistCommitSettled()`，保留统一收尾的一次严格 VisitTurn/lifecycle 复读，不增加宽容放行、历史 turn 过滤或第二套恢复器。门禁：聚焦 68/68、`npm run check:ui` PASS、`npm test` 722/722 PASS；未构建、打包、上传或发布。随后对真实 Chat Completion 请求复核发现：选择“随着 AI 输出”时同一剧情请求同时收到 `[mvu_plot]` 与 `[mvu_update]` 内容，但 GAL 协议仍明确禁止剧情模型输出 `<UpdateVariable>`，因此当前卡不能把同轮模式视为可靠兼容路径；推荐继续使用已验收的额外模型解析。VisitTurn 仍由 bridge settlement 独占，任何变量模型直接追加 `interaction.visit_memory...turns` 的补丁均属越权；详见 `project/extra-model-variable-analysis.md` 的 2026-08-10 抓包结论。

> 2026-08-10（一百二十一，MVU 读档 scope 错配局部修复）：实机抖动说明存档虽合并了 chat/message 两个作用域，读档却只恢复 chat-scope，导致 UI 真正读取的 assistant message-scope 留下错位状态。保持 `gensokyo-save.v1`、楼层重建与教程逻辑不变，现将非 `stat_data` 字段写回 chat-scope，把 `stat_data` 合并写入重建后最后一个 assistant 的 message-scope；正常读档与自动回滚复用同一恢复函数。门禁：聚焦 9/9、受影响集 154/154、`npm run check:ui` PASS、`npm test` 720/720 PASS。未构建卡片、未上传或发布；真实宿主的连续采样、刷新和下一回合续写仍待验收。

> 2026-08-10（一百二十，GAL v5 真实玩家楼层与双重格式约束静态实施）：新请求升为 `gal-prompt.v5`。普通互动、异变收束、决斗胜利三类入口把玩家原文、正文协议、在场快照、场景事实和道具授权作为同一完整正文写入 `createChatMessages()` 创建的 `is_hidden:false` 真实 user 楼层；Helper `generate.user_input` 逐字复用冻结正文，生成前复读不一致即失败，重生成 locator 同样拒绝楼层漂移。移除 `CHAT_COMPLETION_PROMPT_READY` 的原生 user 改写，保留只负责扫描的 `GENERATION_AFTER_COMMANDS` 路由胶囊。常驻 `[mvu_plot]` GAL 世界书恢复完整语法、输出顺序和灵梦正确示范；synthetic history、`with_depth_entries:false`、旧真实楼层隔离及 v1–v4 恢复语义不变。门禁：`npm run check:ui` PASS、`npm test` 719/719 PASS；未构建卡片、未上传 R2、未发布、未完成真实宿主楼层持久化与格式率验收。

> 2026-08-09（一百一十九，累计功能提交与交接入口收拢）：GAL 事务重构、角色记忆四批、脱敏诊断、MVU 多槽存读和请求期提示词注入已合并为源码交接提交 `767c9515a1a8923088aa5a68f4f6bc52e6e7f110`（`feat: complete GAL transaction and memory pipeline`）。提交包含 113 个源码／测试／项目文档文件，门禁为 `npm run check:ui` PASS、`npm test` 702/702 PASS、`git diff --check` PASS。后续 Agent 的固定阅读顺序为：`contract.md` → 本文顶部 → `gal-character-visit-memory-and-synthetic-history-plan.md` → `gal-character-memory-batch-4-database-coexistence-replan.md` → `gal-mvu-save-load-plan.md` → `gal-prompt-floor-injection-plan.md` → `docs-reference-next-implementation-audit.md`。当前范围已静态封账；不得再造第二套发送／监听／重生成／历史／数据库召回／存档／注入控制器。仍未发生的是新卡构建、测试通道更新、R2 上传与正式版晋升；旧 r94 只是上一发布基线，不含本提交功能。真实宿主存读与最终 prompt 顺序核对是可选验收，不得把未执行写成 runtime PASS。工作区剩余的 `reasonix.toml`、`.playwright-mcp/`、`.sandbox/`、`verify*.log`、帕秋莉／早苗／妖梦原始素材及其处理脚本不属于本提交，未经所有者确认不得顺手纳入后续提交。

> 2026-08-09（一百一十八，GAL 请求期提示词楼层注入静态封账）：新建 `gal-prompt-injection.ts`，`gal-prompt.v2` 将清理后的玩家原文只放入 `user_input`，把庭园正文协议、在场快照、场景事实、角色／道具授权合并为唯一一条 depth 1 `system/in_chat` inject；每角色 synthetic history 继续只进入 `overrides.chat_history.prompts`，固定 `with_depth_entries:false`，不发送原生旧聊天历史。请求 metadata 冻结 inject 与 hash，send/retry/regenerate 复用同一请求；旧 `gal-prompt.v1` 按无 inject 旧语义恢复。动作／结束消息不再预拼庭园协议，设施事件参与者改为结构化传入历史选择器和角色授权，避免移除包装后漏绿灯。`GensokyoScene` 不再发送给模型，旧解析仅作本地兼容。门禁：`check:ui`、全量 **702/702**、`git diff --check` 通过；未跑探针、实机 prompt 演示、打包、R2 或发布。`docs/` 差距审计已同步：脱敏诊断、MVU 多槽存读和请求期注入三项候选均静态完成；独立模型语义召回、动态临时世界书与自动存档属于可选／不适配项，不计为漏项。

> 2026-08-09（一百一十七，GAL 第六批 MVU 存档／读档静态施工完成）：主 Agent 按 `project/gal-mvu-save-load-plan.md` 完成全部代码逻辑。新增 `save-schema.ts / save-capture.ts / save-worldbook-store.ts / save-restore.ts` 与三组测试；设置页接入 8 个手动槽、标签、覆盖／读取确认、busy 和安全状态文案。存档使用当前聊天绑定世界书的 disabled meta+24KiB UTF-8 chunks，空 keys、概率 0、禁双向递归，单槽 8MiB，SHA-256/字节数/schema/messageCount/part 连续性全量校验；只保存活动页 `role/name/is_hidden/message/data` 与完整 MvuData，读取时再次白名单化楼层，不保存 swipe、extra 或请求／事务／重生成 metadata。读档先完整读取校验，再按 message_id 倒序每批 50 删除、原序每批 50 创建，楼层 `data` 原样恢复，完整 MVU 只调用 `replaceMvuData(...,{type:'chat'})`；没有最后 assistant 人工状态锚点。破坏阶段 delete/create/replace/reload 失败均用内存副本恢复旧楼层与旧 MVU；成功路径清理瞬态事务后 reload 一次。宿主壳补齐消息、世界书与 reload 能力转发。门禁：聚焦 17/17、`check:ui`、全量 **696/696**、`git diff --check` 通过；reasonix 无改动。未构建、未打包、未上传 R2、未发布、未跑探针或时机演示；真实 1.18.0 + Helper 4.8.18 最终操作验收仍待执行，静态 PASS 不得冒充实机 PASS。

> 2026-08-09（一百一十六，GAL 第六批 MVU 存档／读档规划）：新增 `project/gal-mvu-save-load-plan.md`，仅完成规划、未改业务源码。所有者否决宿主 checkpoint／branch 方案，最终裁定采用汤泉式卡内存档语义：当前聊天绑定世界书保存 8 个手动槽，每槽只含当前活动页完整聊天（role/name/is_hidden/message + 楼层原有 data）、一份真实完整 MvuData，以及最少 schema／字节数／SHA-256；世界书条目永久 disabled、空 key、禁止递归，绝不进入模型。读档先全量校验，再倒序删楼、按序重建；楼层 data 只恢复原值，不为最后 assistant 人工补最终状态；完整 MVU 必须直接 `Mvu.replaceMvuData(savedMvu,{type:'chat'})` 并复读。明确不保存 swipe、extra、request/attempt/generation/commit/regeneration metadata，不为旧楼重造 metadata，旧楼只查看不重生成，下一次发送建立全新 V2 请求。计划拆为 S00～S07，S04 破坏性读档单独验收；含内存回滚副本、8 MiB 单槽上限、世界书单次槽覆盖、静态和真实宿主矩阵。未打包、未发布、未上传 R2、未修改 reasonix。

> 2026-08-09（一百一十五，GAL 第五批脱敏诊断导出完成）：主 Agent 接管并完成 T01～T06。新增 `src/ui/diagnostic-export.ts` 与 `tests/diagnostic-export.test.mjs`，实现固定 `gensokyo-diagnostic.v1`、每次导出随机 16-byte salt + SHA-256 `d_<12hex>` 短引用、固定角色白名单、失败闭合枚举／版本／错误码、64 KiB UTF-8 硬上限和状态／记忆纯计数；无弱哈希回退，不导出玩家输入、剧情或关系摘要、数据库 rows、DOM、stack、token、Cookie、凭据或原始完整 ID。host/preview bridge 共用同一纯构造器，只读当前事务、冻结请求与现有 MVU 最新态，不读聊天历史、不召回数据库、不写状态。设置页新增用户主动触发的本地 JSON 下载，带 busy 锁、固定安全错误文案和 object URL 回收，不上传网络。攻击性 canary、递归语义键名审计、100 次稳定性与输入不变性回归已覆盖；六组聚焦测试 191/191、`check:ui`、全量 **678/678**、`git diff --check` 均通过，reasonix 零改动。实施证据见 `project/gal-diagnostic-export-batch-5-implementation-log.md`。本轮未构建、未打包、未上传 R2、未发布、未跑探针或真实 SillyTavern 点击下载；静态通过不得冒充实机通过。下一候选是宿主原生 branch/checkpoint 的 API 可行性与恢复合同规划，提示词／injects／临时世界书注入继续后置。

> 2026-08-09（一百一十四，GAL 第五批脱敏诊断导出规划）：新增 `project/gal-diagnostic-export-batch-5-runbook.md`，仅完成规划、未改业务代码。手册把施工拆为 T00～T06，逐任务强制重读 `sillytavern-embedded-ui`、`sillytavern-api-reference`、`code-quality-workflow`、`sillytavern-dev-db`、本计划与项目合同，并给出精确可改文件、禁改目录、设置页按钮 ID、`gensokyo-diagnostic.v1` schema、每次导出随机盐 SHA-256 单文件代号、64 KiB UTF-8 上限、错误码白名单、隐私 canary、聚焦／全量命令和 T01/T02 独立验收停点。本批裁定只做用户主动触发的本地 JSON 快照，不做日志环、探针、数据库读取、聊天楼层读取、自动上传、打包、R2、发布、实机或 reasonix；现有 32 位 request fingerprint 不得冒充隐私哈希或原样导出。执行 Agent 必须新建 `project/gal-diagnostic-export-batch-5-implementation-log.md`，不得自行封账、提交或推送。

> 2026-08-09（一百一十三，GAL 角色记忆第四批 R2 封账 + docs 参考审计）：第四批已按最终取舍完成。standalone-mvu 与 database-assisted 始终构造逐字节相同的卡内 MVU 48 条剧情梗概 + 12 条关系记忆 synthetic history；database-assisted 不主动归档、查表、合并或依赖数据库记忆，只保留与角色记忆无关的既有开场主角／背包同步。宿主 `generate` bridge 改为调用时动态解析当前 `TavernHelper.generate`，不会因 UI 先挂载而绕过稍后安装的数据库 wrapper；wrapper 抛错不重试、不重建 request。旧 T03～T06 主动数据库记忆模块保留为隔离研究资产，production bundle/import graph 不可达。新增双 profile 同一性、100 次稳定性、late-bound wrapper 顺序与生产 bundle 隔离测试；`check:ui`、全量 **654/654**、`git diff --check` 均通过。未做探针、真实数据库时机演示、R2、发布、checkpoint 或整卡打包，runtime 标记仍为 `DBR-C8-UNVERIFIED`，reasonix 零改动。项目入口已同步当前基线；新增 `project/docs-reference-next-implementation-audit.md` 对照 `docs/` 与 STDB，裁定下一候选优先为脱敏诊断导出，其后才是宿主原生 branch/checkpoint 存档入口；禁止直接照抄汤泉“世界书全聊天快照 → 清空并重建所有楼层”，提示词／临时世界书注入继续留待独立专项。

> 2026-08-08（一百一十二，角色“赖场”修复独立验收 + 当前项目云端同步）：复核审计、修复计划、生产代码和新增回归后，确认两轮离线修复通过验收。P1 冷却邀请在建计划前明确拒绝，协调后无计划返回 decline；P2 固定事件保存 `previousPresent`，仅为真正新增角色生成 event meta；leave 清理与 bridge restore→settle→presence→reconcile→write→projection 顺序已有合同测试。独立重跑：新增 4/4、聚焦 15/15（含 serial 0）、相关全量 150/151、项目全量 234/235、check:ui 与 diff check 通过；唯一失败仍是预存 GAL 道具选择器源码正则。旧存档已丢失 visitor_meta 不追溯恢复，真实 SillyTavern 实机验收仍待执行。本轮将 r65–r95 累计源码、R2/UI 工具链、地图 v4、赖场修复、测试、项目文档及旧文档删除同步至云端 `main`；排除 `.reasonix` 本机会话元数据、剪贴板附件、`.env` 与 `dist/`。

> 2026-08-08（一百一十一，远程 UI 交付加固 + r95 本地候选）：修复 r94 方案审计发现的发布链缺口。①`package.json` 的 remote 构建固定 `--ui-delivery=remote`，打包 dry/write 固定 `--ui-delivery=remote`；下一检查点升 r95，r94 公网对象和既有卡不覆盖。②`build-ui.mjs` 对同名不同内容的 `ui-mount-rN.js` 拒绝覆盖，宿主构建 ID 改用 rN/内容哈希而非当前时间，使相同输入可重复构建；`package-checkpoint.mjs` 强制版本化副本与当前 `ui-mount.js` 逐字节一致。③卡内 loader 强制校验 manifest schema/version/同源固定路径/bytes/64 位 sha256，按下载原字节校验，保留不可变 UI 浏览器缓存，缺少安全上下文 Web Crypto 时明确失败。④`publish-ui.mjs` 只把明确 404 视为不存在，UI 使用 `If-None-Match: *` 原子创建，manifest 使用当前 ETag + `If-Match` 乐观锁；切指针前后分别公网读回核对字节、MIME、Cache-Control 与 SHA。⑤修复远程产物测试错误解析到仓库父目录而静默跳过的问题，新增非法 manifest 在 UI 下载前被拒绝、真实打包拒绝与发布顺序回归。**本地门禁**：语法检查、`check:ui`、远程 UI 专项 4/4、连续两次 r95 remote build SHA 一致、publish dry-run、package dry-run 均通过；r95 UI 1,880,906B / SHA-256 `375f1cd8…4d061`，轻量卡 dry-run 298,937B、loader 4,231B。全测 224/225，唯一失败仍为既有 GAL 提交锁测试定位正则。**未执行**：R2 真实上传、正式 JSON/PNG 写入、真实 SillyTavern 验收。

> 2026-08-08（一百一十，项目文档更新 + 工作树 vs main 差异记录）：应所有者要求记录"当前工作树"与"git main 已提交版本"的全部差异，见 `project/workspace-vs-main-diff-2026-08-08.md`（基线：main HEAD `d6aff88`，2026-08-04，为 r64 时代快照；工作树 = r65–r94 全部累积未提交工作 + 本轮地图 v4）。**要点**：30 个跟踪文件 +808/-203、12 项未跟踪新增；功能域 A UI 远程交付（r94 loader/R2，已上 R2 并打包 r94）、B 地图 v4 拼接（本轮）、C 玩家名字投影（r93）、D 前端视觉/入口遗留（app.ts launcher/styles.css/bridge 等从未提交）、E 发布脚本（publish-cg-r2、character-arrival-departure-audit）、F 环境杂项；版本状态：package.json 与 manifest 均指 r94、release_artifacts 指 r93 正式版（r93 已打包含 superseded）、地图 v4 未打包未上 R2 未实机验收。**本轮文档动作**：`project/manifest.json` `ui_sources.garden_base` 同步 v3→v4.png（此前与 asset-manifest 不一致）；新增本差异文档；README「庭园地图分层」行更新 v4 拼接说明。**风险提示**：git 历史滞后于工作树 30 个文件，提交/回退前先读差异文档 §6 建议顺序；npm test 222/223（既有 GAL 提交锁失败，非本轮）；预览用 dist/ui/index.html 本地修补形态（备份 index.html.remote-backup，下次构建覆盖）。

> 2026-08-08（一百零九，地图拼接编辑器 + v4 底图拼接嵌入）：应所有者要求新建地图拼接编辑器 `scripts/map-stitcher/`（`index.html` 单文件 + `start.mjs` 本地服务器，默认端口 8917，`npm run preview` 之外的自立工具；`README.md` 含操作表与嵌入改动点清单），把新地图图片可视化定位拼接后导出参数 JSON 交给 Agent 嵌入。**本轮嵌入**（参数 `scripts/map-stitcher/params-2026-08-08.json`，offsetX≈0、offsetY≈781、scale 1，画布 1672×1722；新图与旧底图同为 1672×941、像素 0% 相同，为 AI 重绘同布局图）：新增确定性合成脚本 `scripts/stitch-map-layers.mjs`（同参数产出字节一致 PNG+Q70 WebP+报告），产出维护源 `src/assets/maps/garden-base-owner-v4.png`（8,891,383B、SHA-256 `41debf6e…8b40d32`）与运行版 `garden-base-owner-v4.webp`（555,344B、Q70、SHA-256 `b51c9234…e3142b95`），报告 `project/map-stitch-2026-08-08.json`。**代码改动**：①`asset-manifest.json` `maps.garden_base`（source→v4.webp、canvas→[1672,1722]、status/provenance 更新）与 `garden_no_walk_mask.canvas`→[1672,1722]；②`garden-no-walk-mask-v1.svg` 画布扩至 1722，旧形状坐标保持（下段新图区域未登记阻挡，待所有者确认）；③`garden-map.ts` `CHARACTER_FOOT_OFFSET_Y` 54/941→54/1722、蒙版画布 [1672,941]→[1672,1722]、river/bridge 采样点 720/941、826/941→/1722；④`garden-navigation.ts` 脚底半径 7/941→7/1722；⑤`garden-spatial.ts` `GARDEN_AREA_POSITIONS` 六个区域 y 按 941/1722 重算（0.43→0.235、0.58→0.317、0.27→0.148、0.36→0.197、0.70→0.383、0.69→0.377，视觉位置未变、所有锚点像素 y<781 未被新图遮挡）；⑥`tests/ui-contract.test.mjs` 同步 v4 文件名、[1672,1722]、SVG 头部、main_house 坐标断言。**门禁**：check:ui（tsc）全绿；npm test **222/223**——唯一失败为既有「GAL 回复落盘后释放本地提交锁」测试（工作树既有 app.ts 改动与测试正则不符，HEAD 基线匹配，非本轮引入，已记录不归责本轮）；build:ui:remote 通过（蒙版画布一致性校验随 manifest 自动通过）；package:checkpoint:dry 通过（UI 包 1,880,917B、26 条世界书、refuse-overwrite）。**未打包**（按纪律需所有者授权；v3 底图文件保留原位作为合成源，未归档）。**待确认**：①下段新图区域（画布 y 941-1722）是否需新增不可行走阻挡（新图底部为浅黄褐地面 163,145,82，非深水面，当前未阻挡）；②重叠带 y 781-941 保留原河道阻挡（角色不可站上该过渡带）；③所有者待会还有一张新图要拼接（届时用同一编辑器/脚本流程再合成，v4→v5）。**实机验收待执行**：新底图在离线预览与真实 SillyTavern 中的缩放/巡游/设施落位。

> 2026-08-04（一百零八，方案 A：UI 远程交付——卡内只留 loader，UI 包与 manifest 指针上 R2）：按所有者确认的三决策（manifest 指针 / 双模式 / 不可变文件名）完成 UI 外置施工。**改动**：①`scripts/build-ui.mjs` 新增 `--ui-delivery=embedded|remote`（remote 要求 `--ui-version=rN`，额外产出 `dist/runtime/ui-mount-<rN>.js` 发布副本 + `dist/runtime/ui-loader.js` 卡内 loader，占位符 `__UI_MANIFEST_URL__` 替换为 R2 manifest URL；embedded 保持现状）；②新增 `src/runtime/ui-loader.js`（2.5KB）：fetch `…/live/ui/ui-manifest.json`（no-store）→ 校验 url → fetch ui-mount 文本 → `crypto.subtle` sha256 比对 → Blob URL import → 失败在聊天区显示可见兜底提示；③新增 `scripts/publish-ui.mjs`：读 `.env`（已 gitignore，S3 凭据）→ AWS SigV4（region=auto）直传 R2 桶 `hxxwy`，ui-mount 先传（`immutable` 长缓存）manifest 最后（`no-store`），已存在拒绝覆盖，dry-run 先；④`scripts/package-checkpoint.mjs` 新增 `--ui-delivery`：remote 内嵌 loader（校验含 manifest URL + `ui-mount-<rN>.js` 产物存在），embedded 现状；⑤`tests/ui-contract.test.mjs` 新增 2 项（loader 模板结构 + 已构建产物一致性）。**门禁全绿**：check:ui、npm test 220/220、build:ui:remote（remote 产物 1.76MB + loader 2.6KB）、r94 remote dry-run **297,237 bytes（290KB，原 2.26MB 降 87%）**、embedded 对比 dry-run、publish-ui dry-run（sha256 `17774106057a953a58e63695bded939013626bc008beffc3660f1b88d5eaf7e8`）。**未执行**：R2 真实上传（publish-ui 正式模式）与 r94 正式打包（`dist/checkpoint-0.2.0-r94` 未落盘、PNG 未生成）——均在等待所有者授权；`project/manifest.json`（next_checkpoint=`0.2.0-r94-ui-remote-delivery`、planned 序列含 r94）与 `package.json` 打包脚本已升 r94。**凭据**：所有者已粘贴 R2 S3 凭据，已写入 `.env`（gitignore）；runbook 纪律要求粘贴后提醒轮换——**待所有者轮换 R2 API Token**。**文档**：`project/r2-packaging-runbook.md` 新增「UI 远程交付」章节（坐标/流程/纪律）。**下一步**：所有者授权后 → `node scripts/publish-ui.mjs --version=r94 --file=dist/runtime/ui-mount-r94.js`（真实上传）→ `npm run package:checkpoint`（--ui-delivery=remote 打 r94 remote 卡）→ 实机验收（loader 拉取、sha256 校验、旧卡刷新跟新版、断网兜底提示）。

> **r94 打包结果（同日补记，所有者已授权真实上传与打包）**：已真实上传 R2——`gensokyo-moving-garden/live/ui/ui-mount-r94.js`（1,859,290 bytes，`public, max-age=31536000, immutable`）与 `ui-manifest.json`（no-store 指针，`{"version":"r94","url":"https://ssrfrrt.ccwu.cc/gensokyo-moving-garden/live/ui/ui-mount-r94.js","sha256":"17774106057a953a58e63695bded939013626bc008beffc3660f1b88d5eaf7e8"}`），远程 GET 验证 200 + bytes 一致。已打包 r94 remote 卡：`dist/checkpoint-0.2.0-r94/幻想乡物语-测试检查点-0.2.0-r94.json`（**297,237 bytes**、SHA-256 `4d909263e6555193cde5ded2c713ed2fbdf75d98e97f68eb455c633e524dde33`、UI 脚本 `gensokyo-garden-ui-020-r94` = loader 2,613B、26 条世界书、与 dry-run 逐字节一致）与 `.png`（1,230,920 bytes、chara payload 与 JSON 逐字节一致）。`project/manifest.json` runtime_artifacts 与 `project/README.md`「当前有效轻量包」已同步 r94（release_artifacts 正式版仍指 r92）。**实机验收待所有者**：导入 r94 卡 → 首次加载经 loader 拉 R2 UI → 后续更新只改 R2 manifest 指针；断网时可见兜底提示。

> 2026-08-04（一百零七，玩家名字可见性修复——补上 stat_data → 剧情阶段的投影断链）：所有者提供分析文档《玩家名字-可见性修复..md》确认问题：开局 UI 填写的玩家姓名只写入 MVU `stat_data.player.name`（首个 assistant 楼层，bridge `applyOpeningDraft` + 复读校验），但**所有剧情阶段注入块都脱敏**——`buildPromptContext`【场景事实】无玩家姓名，gardenNarrativeContract / 世界书 / system_prompt 均无名字，模型永远读不到，填写形同虚设。根因：projection 断链/过度脱敏（完整 stat_data 按 design 只进变量阶段，但玩家姓名是玩家主动提供、UI 已确认的公开身份事实，漏了投影）。按文档推荐组合实施方案 1（数据层）+ 方案 2（规则层），方案 3（放宽 entries[5] 禁止条款）排除、方案 4（宏注入，原生聊天盲区补丁）搁置待实机验证。**改动**：①`src/ui/prompt-context.ts` 新增 `playerIdentityLine()`（name 空白回退「未命名旅人」、pronouns 有值才附「可用「…」称呼」），`buildPromptContext` 主【场景事实】与 leak guard 降级分支各加一行；②`src/ui/target-actions.ts` `gardenNarrativeContract` 新增规则「称呼玩家时使用【场景事实】提供的玩家姓名或玩家称谓；姓名与称谓只用于称呼玩家，不得据此替玩家决定人称、台词、心理、关系承诺或关键选择。」；③`src/lorebook/gal-presentation-protocol.md`（worldbook entries[7]）同步同款规则（r78 两处同步惯例）；④`tests/ui-contract.test.mjs` 新增回归（名字+称谓注入、trim、空白回退、无称谓省略括号、双处规则同步）。**门禁**：`npm test` 218/218（原 217）、`check:ui`、`build:ui:remote` 全绿；dry-run r93 通过（2,263,168 bytes、SHA-256 `0ec34701592eb01683f5f040c3876775cfe009919bfbf4c2f78288e2b1f7b904`、26 条世界书、UI 脚本 `gensokyo-garden-ui-020-r93`）。**注意**：按所有者决定**未打包**（dist/checkpoint-0.2.0-r93 未落盘、PNG 未生成）；`package.json` 三个打包脚本与 `project/manifest.json`（next_checkpoint=`0.2.0-r93-player-name-projection`、planned_checkpoint_sequence）已升到 r93 待打包，`runtime_artifacts`/`release_artifacts` 与 README「当前有效轻量包」仍指 r92；下次打包自动注入新 version（构建期）。**已知盲区（文档 2 记录，不阻塞主修复）**：`withGardenNarrativeContract` 是 UI 流程注入，原生聊天、swipe 旧消息、UI 未挂载时名字仍不可见；方案 4 宏注入需实机确认 ST 宏阶段后再定。**待实机验收**：开局填名 → 第一次真实行动正文中角色用名字/称谓称呼玩家；且模型不替玩家决定人称、台词、心理。
所有者反馈**新手教程第 9 步「进行温室研究交流」卡住，与温室交流进度不推进**。根因（用 Luker 实机聊天记录 `F:\agent airp\Luker\data\default-user\chats\幻想乡物语·移动庭园\…2026-08-04@13h04m00s533ms.jsonl` 逐楼层交叉验证）：温室交流原为两段式受控会话（`greenhouse_multiturn_conversation`，`effective_rounds` 0→1→2 完成），但会话状态依赖跨楼层的 per-message stat_data 快照传递；Luker 消息存储无独立 `message_id` 字段（jsonl 证实），运行时 `snapshot.assistantMessageId` 错位（实机 [21] 轮 `last_effective_message_id=20` 指向 user 楼层），导致 settle 写入的会话状态在下一轮 `latestPersistedState`/`persistedStateBefore` 回读时不可见——实机楼层快照显示会话被反复新建（interaction_1→2→3，每轮 `current_session` 都是 null，`effective_rounds` 永远停在 0/1），永远到不了 2，完成标记永不写入。前 8 步固定事件为单轮结算不依赖跨轮状态所以正常。**修复方案（所有者拍板）**：温室研究交流改为**单轮固定结算**，与其它固定事件同路径。改动：`event-settlement.ts`——`stageLocalSession` 改为 no-op（不再创建受控会话）、`settleConversationTurn` 单轮直接 `completeGreenhouseConversation`（幂等：已完成即 return）、`settleConversationEnd` 幂等收尾、`settlementChoices`/`GREENHOUSE_RESEARCH_MAX_EFFECTIVE_ROUNDS=1` 对齐、遗留旧会话在结算时清理（旧版卡住的玩家再点一次"温室研究交流"即完成）；`bridge.ts` `deterministicSettlementResult` research_talk/continue/end_conversation 一律返回 `conversation_settled_after_multiple_turns`；`greenhouse-vertical-slice.json` conversation 事件 `progression_session`→`progression_fixed`、`max_effective_rounds: 1`、移除 `minimum/maximum_effective_rounds`/`auto_settle_on_max_rounds`/`interaction.current_session` blocks、文案改单轮；`target-actions.ts` 两处动作文案改单轮收束；`greenhouse-rules.ts` block 文案对齐；`open-garden-rules.ts` 第 9 步 instruction 改"完成一次温室研究交流"；`app.ts` 删除 `GREENHOUSE_RESEARCH_INPUT_MAX_LENGTH` 120 字限制与提示文案改"温室研究交流已完成"；`variable-update-rules.md`/`gal-presentation-protocol.md` 世界书注入文档同步。`npm test` 217/217（含新增断言：单轮结算、重复结算幂等、**旧版残留活跃会话被新结算清理并完成**）、`check:ui`、`build:ui:remote` 全绿。**注意**：修复未打包（dist/checkpoint 与 manifest 指针未动），且需新版本号才能让实机旧实例执行新代码（version 相同会短路）；已同步源码，下次打包自动注入新 version。实机验收仍待所有者执行。

> **r92 打包结果（同日补记）**：已按 runbook 门禁链打包 `0.2.0-r92` 并发布——`npm test` 217/217 → `build:ui:remote`（remote-r2-live）→ `package:checkpoint:dry` → 正式写入 `dist/checkpoint-0.2.0-r92/`：JSON 2,261,842 bytes、SHA-256 `4303a3523c6586b77f65fcb9f6ec225278006c161f84523b55369ce10e62aeb9`；PNG 3,850,396 bytes、SHA-256 `3cd6d2ad2dbd3b51e38a40c57693d5ba75070f3b139bd69a90e010e64b87e530`（立绘 512×853 sharp 压缩，`D:/浏览器下载/MiaoMiao_Harem_AnimaBase_single_00504_.png`）。UI 脚本升级为 `gensokyo-garden-ui-020-r92`（构建期注入新 version，实机旧实例可正常替换导入），包内 UI 与 `dist/runtime/ui-mount.js` 逐字节一致、PNG chara payload 与 JSON 逐字节一致、26 条世界书、r91 旧检查点未被覆盖（refuse-overwrite）。`package.json` 三个脚本与 `project/manifest.json`（next_checkpoint/planned_checkpoint_sequence/runtime_artifacts）及 README/runbook 指针已同步到 r92。**待实机验收**：导入 r92 后重玩温室研究交流，第 9 步应一次回复即完成；旧卡住存档在更新后重点"温室研究交流"即被抢救完成。

> **正式版打包（同日补记）**：按所有者要求把 r92 以**正式发布版**形态再打一份（卡片名称与 creator_notes 改为正式版，功能内容不变）：`dist/checkpoint-0.2.0-r92/幻想乡物语-正式版-0.2.0-r92.json`（2,261,814 bytes、SHA-256 `938f5377724e4e5a7d74ca88e746c70cf7c0b42d60e3ef92a5b940dd98740929`）与 `.png`（3,850,356 bytes、SHA-256 `e565e72249c9c5474b71a8d5d6891e091a74a2829cb2944b2fc8cc77a41040ea`，立绘 512×853）。正式版元数据：`name`=`幻想乡物语·移动庭园`、`first_mes` 标签 `<移动庭园 version="0.2.0">`、`creator_notes` 首行「本文件为「幻想乡物语·移动庭园」正式发布版（0.2.0-r92）。」、`tags` 移除「测试检查点」、`character_book.description`=`幻想乡物语·移动庭园内嵌世界书`、脚本 `info` 标注正式发布版。已验证：PNG chara payload 与正式版 JSON 逐字节一致；3 个 UI 脚本 `content`、26 条世界书 entries、world 名、description/system_prompt/scenario/personality 与 r92 测试检查点逐字节一致（仅元数据字段不同）。`scripts/embed-card-png.mjs` 新增可选 `--json=`/`--output=` 参数（默认行为不变）以支持自定义命名产物。`project/manifest.json` 新增 `release_artifacts` 段指向正式版两份产物；README 增补正式版行。交付玩家用正式版 PNG，开发/验收用测试检查点。`npm test` 217/217 保持全绿。

> 2026-08-04（一百零五，card_fixed 宿主层残留清理修复——所有者侧修复，仅存在于 PNG 卡片）：所有者修复后导出的 `D:\浏览器下载\card_fixed.png`（3,857,428 bytes、SHA-256 `5288f5c4b47ee4d0f8ee60c945c5217470627dee977f0d56e4124eb23421c9e9`、mtime 2026-08-04 13:03）已解析入库。**基础**：r91 检查点（UI 脚本 id 仍为 `gensokyo-garden-ui-020-r91`、version `0.4.3-host-generate-20260803184019` 未变；26 条世界书逐条逐字节一致；`tavern_helper.scripts[0/1]`（MVU loader/schema）逐字节一致；extensions 除 scripts 外无任何差异）。**核心修复（UI 脚本 `gensokyo-garden-ui-020-r91` 76 insertions / 4 deletions，+2,905 字符）——宿主层残留清理**：tavern_helper 切换聊天/停止旧脚本时销毁脚本 iframe，原先挂在脚本 iframe `eventSource` 上的 `eventOn` 监听器随之失效，父页面上的悬浮返回按钮（`#gensokyo-game-return-frame`）、游戏界面壳、宿主样式残留无人清理。修复四处：①`registerHostChatCleanup()`——直接在父页面 `eventSource` 上注册原生 `chat_changed` 监听器（仅清理、不重建），监听器由父页面持有不随脚本 iframe 销毁而失效（参考明月秋青脚本做法）；②`startHeartbeat()`——脚本活跃期间由沙箱 iframe 内定时器每秒刷新父页面心跳标记 `__GENSOKYO_GARDEN_HEARTBEAT_024__`；③`registerHostGuard()`——父页面常驻守卫每 1.5s 检查心跳，超过 3.5s 过期（脚本被停止、iframe 被销毁）即清除返回按钮与宿主残留，作为不依赖任何事件系统能否送达的最终兜底；④`cleanupHostArtifacts()`——`pagehide`/`unload` 时移除返回按钮、游戏壳、宿主样式。另：`destroy()` 的 eventStops/observer/chat 清理三步全部加 try/catch 防护；CHAT_CHANGED 订阅改为 `source.tavern_events?.CHAT_CHANGED ?? 'chat_changed'` 事件名回退；初始化接入 `registerHostChatCleanup()`+`registerHostGuard()`，末尾 `startHeartbeat()`+注册 pagehide/unload。**卡片元数据（正式发布版命名，非功能）**：`name` 去掉「（测试检查点 0.2.0-r91）」后缀；`first_mes` 标签 `<移动庭园_测试检查点>` → `<移动庭园 version="0.2.0">`；`creator_notes` 改写为「正式发布版（0.2.0-r91）」；`tags` 移除「测试检查点」；`character_book.description` 改为「幻想乡物语·移动庭园内嵌世界书」。**注意事项**：修复已按所有者选择同步回 `src/runtime/ui-host-shell.js`（四个新函数/三个 key 常量/destroy try-catch/CHAT_CHANGED 回退/初始化接入/尾部 pagehide+unload 监听与卡片逐字一致，`node tmp/card-fixed-sandbox/verify-port.mjs` 全项对齐）；`npm test` 217/217、`npm run check:ui`（tsc --noEmit）、`npm run build:ui:remote` 全绿，`dist/runtime/ui-mount.js` 已含守卫逻辑且 `node --check` 通过。按所有者决定**未打包**检查点（`dist/checkpoint-0.2.0-rN` 与 PNG 未生成，manifest/README/runbook 指针未动）；且卡片 UI 脚本 version 与 r91 相同——若先导入 r91 再导入 `card_fixed.png`，旧实例会按 `existing.version === version` 短路走 ensureMounted 不执行新代码，须替换导入（清掉旧实例）或改 version 后重打包。源码同步后 version 仍为构建期注入，下次打包自动生成新版本号，无此问题。

> 2026-08-04（一百零四，r91 教程条点击折叠）：所有者反馈**默认教程条在庭园视图会挡住下方设施/角色抽屉**。优化：点击教程条本身（跳过按钮除外）即折叠当前步骤（会话级 `tutorialGuideCollapsedStep` 记录 step.id），不挡抽屉/地图；`renderTutorialGuide` 中 `step.id === tutorialGuideCollapsedStep` 时保持隐藏；步骤推进（`completed_key_events` 变化 → currentStep 变化）后自动重新出现，无需手动展开。实现：`src/ui/app.ts` 加 `tutorialGuideCollapsedStep`/`tutorialGuideRenderedStepId` 两个模块级变量，`renderTutorialGuide` 折叠判定 + 记录 renderedStepId，`tutorialGuide.addEventListener('click')` 折叠监听（排除 skip 按钮）。GAL 视图 r90 顶部胶囊逻辑不受影响。`npm test` 217/217、`check:ui`、`build:ui:remote`、dry-run、正式 JSON+PNG 打包全绿。检查点升级至未占用的 r91。成品 `dist/checkpoint-0.2.0-r91/幻想乡物语-测试检查点-0.2.0-r91.json`（2,263,771 bytes、SHA-256 `330e78a338a2253403861b3b325fae5f4235d37d931a9b622298c5dbddbf7e47`、26 条世界书、UI 脚本 `gensokyo-garden-ui-020-r91`、包内 UI 与 `dist/runtime/ui-mount.js` 逐字节一致、含 tutorialGuideCollapsedStep/点击监听/步骤推进重现）与 `.png` 卡片（3,852,968 bytes、SHA-256 `096061b1d6e7c06c0ffaafd80bff66fb88d98ba32e7058b0363a63c1ffb218dd`、立绘 512×853 sharp 压缩）。README/manifest/runbook/agent-handoff 已同步 r91。手机实机验收仍待所有者执行。

> 2026-08-04（一百零三，r90 手机 GAL 教程遮挡修复）：所有者实测反馈**手机端新手教程条会盖住 GAL 页面**。根因：`.gg-tutorial-guide` 是 `position: fixed; bottom: max(12px, env(safe-area-inset-bottom))` 的底部全宽条（≤700px 时 `left/right: 10px`），而 GAL 的对话盒 `.gg-dialogue-box` 与回复面板 `.gg-reply-panel` 同样位于底部——教程条直接盖住对白框与回复面板，剧情文字看不到、对白框点不到。优化方案（纯 CSS，利用 `setView()` 已维护的 `#gg-app[data-active-view="gal"]`）：GAL 视图下教程条**从底部移到顶部居中胶囊**——`top: max(8px, env(safe-area-inset-top))`、`bottom: auto`、`left: 50%` + `translateX(-50%)`、`width: max-content; max-width: calc(100vw - 16px)`、单边框圆角 `999px`、紧凑 padding/字号、跳过按钮缩为 34px 高；≤420px 保持 `minmax(0,1fr) auto` 两列布局；其他视图（庭园/设施等）教程条仍在底部，行为不变。`npm test` 217/217、`check:ui`、`build:ui:remote`、dry-run、正式 JSON+PNG 打包全绿。检查点升级至未占用的 r90（r89 发布版原样保留）。成品 `dist/checkpoint-0.2.0-r90/幻想乡物语-测试检查点-0.2.0-r90.json`（2,263,140 bytes、SHA-256 `1cdd8acbbe70ff247ac9458e6469aeeef6023ee14ae650f855ab09360032d10e`、26 条世界书、UI 脚本 `gensokyo-garden-ui-020-r90`、包内 UI 与 `dist/runtime/ui-mount.js` 逐字节一致、运行时 CSS 含 `data-active-view="gal"] .gg-tutorial-guide` 顶部胶囊规则）与 `.png` 卡片（3,852,124 bytes、SHA-256 `57fdf1a0356417eae301be906b4d7f35329f1be3559281ef16bd72ad7d4e90d3`、立绘 512×853 sharp 压缩、chara payload 与 JSON 逐字节一致）。README/manifest/runbook/agent-handoff 已同步 r90（r90 为已发布基线后的首个修复版，`released_checkpoint` 仍指 r89，r90 作为增量修复包）。手机实机验收仍待所有者执行。

> 2026-08-04（一百零二，r89 发布）：所有者确认 `0.2.0-r89` 为**正式发布版本**（首版对外发布）。文档状态同步：`project/manifest.json` 的 `status` 由 `candidate` 改为 `released`、新增 `released_checkpoint: "0.2.0-r89"`、里程碑 `m3_package_runtime_acceptance` 由 `in-progress` 改为 `released`；`project/README.md` 当前有效轻量包标注（已发布）并保留完整 SHA/bytes；`project/r2-packaging-runbook.md` 轻量卡三行修正为 r89 实际值（JSON 2,261,917 bytes / PNG 3,850,496 bytes，SHA 见 runbook）。后续新检查点从 r90 起，发布基线与本次一致。

> 2026-08-04（一百零一，r89 悬浮按钮双缺陷修复）：r88 实机发现**按钮完全消失且旧按钮按不了**。根因两处：①`ui-host-shell.js` 的 `version = '0.4.3-host-generate-r27'` 是写死常量不随构建变化——ST 页面里残留的 r87 旧实例 `existing.version === version` 相同，新脚本直接走 `existing.ensureMounted()` 短路，**r88 的 iframe 代码根本没执行**，还留下半毁坏 DOM；②返回 iframe 设了 `pointer-events: none` 全屏覆盖——iframe 元素的 pointer-events 控制整个 iframe 文档是否参与命中测试，设 none 后内部按钮永远收不到点击。修复：①`build-ui.mjs` 组装 `ui-mount.js` 时用 `.replace(/0\.4\.3-host-generate-r\d+/, 0.4.3-host-generate-<14位时间戳>)` 注入唯一 version，保证每次构建产物 version 不同、旧实例必然 mismatch 走 destroy→重建；②iframe 从全屏覆盖改为**按钮大小的悬浮层**（桌面 132×44、移动 120×42，`pointer-events` 默认 auto，其余区域自然穿透），按钮撑满 iframe，位置由宿主层恢复的 `positionReturnFrame()` 用 `visualViewport` 计算 `left/top` 强制写入（`setProperty(..., 'important')`），并恢复 `bindViewportClamping()` 监听 resize/orientationchange/visualViewport/scroll，`showNativeChat()` 时也定位一次。`npm test` 217/217、`check:ui`、`build:ui:remote`、dry-run、正式 JSON+PNG 打包全绿。检查点升级至未占用的 r89（r88 原样保留）。成品 `dist/checkpoint-0.2.0-r89/幻想乡物语-测试检查点-0.2.0-r89.json`（2,261,917 bytes、SHA-256 `6e47c5b90ce327c4737ff8fd4e819631d3454e08aabf62f2fd8a0d10776e1056`、26 条世界书、UI 脚本 `gensokyo-garden-ui-020-r89`、包内 UI 与 `dist/runtime/ui-mount.js` 逐字节一致、含注入版唯一 version/positionReturnFrame/bindViewportClamping/小尺寸 iframe，无 pointer-events:none）与 `.png` 卡片（3,850,496 bytes、SHA-256 `4070ee6849dd48e95fec26db7e77584c2753ab63226445dc4f515c7d50fec426`、立绘 512×853 sharp 压缩、chara payload 与 JSON 逐字节一致）。README/manifest/runbook/agent-handoff 已同步 r89。手机实机验收仍待所有者执行。

> 2026-08-04（一百，r88 返回按钮独立 iframe 层根治）：r87 的"JS 强制像素坐标"仍被所有者手机实测按钮跑出屏幕。根因定性：按钮直接挂在 ST 页面 `doc.body`，ST 自身 CSS（transform/contain/filter 祖先链）会破坏 `position: fixed` 的包含块语义，JS 算准了坐标也钉不住。参考所有者提供的明月秋青脚本（`sanmingyue/tavern_dist@zhino-v5.0.0/dist/明月秋青脚本/index.js`）——其 FAB 运行在 TH 独立 iframe 内（`document` 是 iframe 的，`window.parent` 是 ST 页面），`position: fixed` 不受 ST CSS 污染，坐标全部用 `window.parent.innerWidth/innerHeight` 计算。据此重构 `ui-host-shell.js`：①**删除宿主页面的 `positionReturnButton` 按钮 DOM，改为 `ensureReturnFrame()` 创建独立全屏透明 iframe**（`#gensokyo-game-return-frame`，`position:fixed; inset:0; pointer-events:none; z-index:2147483000`，挂 `doc.body`）；②iframe `srcdoc` 内自带按钮 `#gg-return`（CSS 完全独立于 ST），内嵌脚本每帧用 `window.parent.visualViewport`（回退 `innerWidth/innerHeight`）计算 `left = vw - 按钮宽 - 16px`、`top = vvTop + vh - 按钮高 - 16px` 强制写入，监听 `resize`/`orientationchange`/`scroll`/`visualViewport.resize/scroll`；③点击回调经 `window.parent[instanceKey].showGame()` 回到宿主层；④`clearHostArtifacts` 同时清理 iframe 层。按钮定位不再与 ST 布局有任何耦合。`npm test` 217/217（含新增 `ensureReturnFrame`/`returnFrameId` 断言）、`check:ui`、`build:ui:remote`、dry-run、正式 JSON+PNG 打包全绿。检查点升级至未占用的 r88（r87 原样保留）。成品 `dist/checkpoint-0.2.0-r88/幻想乡物语-测试检查点-0.2.0-r88.json`（2,260,433 bytes、SHA-256 `37e8cee90d600da5d72236e85e9ce54d4916ba317c8863a101ce2d797850a678`、26 条世界书、UI 脚本 `gensokyo-garden-ui-020-r88`、包内 UI 与 `dist/runtime/ui-mount.js` 逐字节一致、含 ensureReturnFrame/returnFrameId/srcdoc 独立层/api.showGame 桥接，旧 positionReturnButton 无残留）与 `.png` 卡片（3,848,516 bytes、SHA-256 `ad797c14d7625386a0c94ce0a51b90077c712e8fa0110df31ba9732bed6bb9b0`、立绘 512×853 sharp 压缩、chara payload 与 JSON 逐字节一致）。README/manifest/runbook/agent-handoff 已同步 r88。手机实机验收仍待所有者执行。

> 2026-08-03（九十九，r87 返回按钮强制右下角定位 v2）：r86 的"越界才 clamp"仍被所有者手机实测发现按钮跑出屏幕。按所有者明确要求"默认悬浮在屏幕右下角、可见的右下角"，重写定位逻辑：`ui-host-shell.js` 的 `clampReturnButton` 升级为 `positionReturnButton`——**每次显示/滚动/缩放/键盘弹起都主动计算像素坐标并 `setProperty(..., 'important')` 强制写入**，不再依赖 CSS `right/bottom`（会被 ST 的 transform/滚动破坏）。要点：①用 `visualViewport`（回退 `documentElement.clientWidth/Height`）计算 `left = vw - 按钮宽 - 16px`、`top = vvTop + vh - 按钮高 - 16px`，`right/bottom` 置 `auto`；②`scroll` 捕获监听（ST 聊天滚动时按钮不跟丢）；③transform 破坏兜底——实测 rect 越界时临时清掉 `#chat` 的 transform 再钉一次；④监听全部挂 `state.eventStops` 由 `destroy()` 清理。`npm test` 217/217、`check:ui`、`build:ui:remote`、dry-run、正式 JSON+PNG 打包全绿。检查点升级至未占用的 r87（r86 原样保留）。成品 `dist/checkpoint-0.2.0-r87/幻想乡物语-测试检查点-0.2.0-r87.json`（2,261,350 bytes、SHA-256 `d0316ff21bf36aada17af1ed67112d44c1fe0dbf1883dd3771b961a290c755a8`、26 条世界书、UI 脚本 `gensokyo-garden-ui-020-r87`、含 positionReturnButton/setProperty important/scroll 捕获/transform 兜底）与 `.png` 卡片（3,849,740 bytes、SHA-256 `e57436130d216453372909d4173e575ba9304f902c7776d6bcda68c843579b35`、立绘 512×853 sharp 压缩、chara payload 与 JSON 逐字节一致）。README/manifest/runbook/agent-handoff 已同步 r87。手机实机验收仍待所有者执行。

> 2026-08-03（九十八，r86 返回按钮持续视口 clamp 升级）：r85 的一次性检查修复被所有者质疑（手机键盘弹起/旋转/工具栏收缩时按钮仍可能跑出屏幕）。参考所有者提供的潮汐预设脚本（`sanmingyue/tavern_dist@cp-v2.1.0/dist/潮汐预设脚本/index.js`）悬浮窗实现：其 `.chaoxi-fab` 用 `position: fixed` + JS 每次 clamp（`window.parent.innerWidth/innerHeight`）+ `visualViewport.height` 处理移动端。据此升级 `ui-host-shell.js`：①`clampReturnButton()`——每次显示后 `requestAnimationFrame` 用 `visualViewport` 宽高 + `getBoundingClientRect` 实测，水平 right clamp 防越界、垂直按 visualViewport 高度重算（键盘弹起时底部上移）；②`bindViewportClamping()`——持续监听 `resize`/`orientationchange`/`visualViewport.resize`，实时重新 clamp（不再只显示时一次），监听清理挂 `state.eventStops` 由 `destroy()` 统一移除无泄漏；③初始化时调用 `bindViewportClamping()`。CSS 基线（safe-area fallback、窄屏/横屏媒体查询）保留。`npm test` 217/217、`check:ui`、`build:ui:remote`、dry-run、正式 JSON+PNG 打包全绿。检查点升级至未占用的 r86（r85 原样保留）。成品 `dist/checkpoint-0.2.0-r86/幻想乡物语-测试检查点-0.2.0-r86.json`（2,260,524 bytes、SHA-256 `93bb40387b357b4023bc906445901d67d87b624892e1a523b561c8ec870a78b0`、26 条世界书、UI 脚本 `gensokyo-garden-ui-020-r86`、包内 UI 与 `dist/runtime/ui-mount.js` 逐字节一致、含 bindViewportClamping/visualViewport/orientationchange/clampReturnButton）与 `.png` 卡片（3,848,636 bytes、SHA-256 `988ab1ef758a3d6ba1670da57b8952f461dd7ce07fa86cae12f5564cc2e23294`、立绘 512×853 sharp 压缩、chara payload 与 JSON 逐字节一致）。README/manifest/runbook/agent-handoff 已同步 r86。手机实机验收仍待所有者执行。

> 2026-08-03（九十七，r85 返回按钮手机适配修复）：实机反馈「返回原生聊天时，移动庭园悬浮框与手机适配差、会跑到屏幕外」。根因两层：①`env(safe-area-inset-*)` 在部分 WebView 不返回有效值且原 CSS 无 fallback → 按钮贴边溢出；②ST 聊天区 swipe/动画 transform 存在时 `position: fixed` 退化为 absolute 语义 → 按钮脱离视口定位。修复（`src/runtime/ui-host-shell.js`）：CSS 给 `env()` 全部加 fallback（`env(safe-area-inset-right, 16px)` 等）、`left:auto; top:auto`、`max-width: calc(100vw - 32px)` + `overflow:hidden` + `text-overflow:ellipsis`、`transform:none !important`；新增 `@media (max-width: 480px)`（右移 10px、字号 12px、padding 收紧）与 `@media (max-height: 480px)` 横屏（bottom 44px）；`showNativeChat()` 显示按钮后 `requestAnimationFrame` 用 `getBoundingClientRect` 检查视口，越界则强制拉回 `right:8px` 并收窄 maxWidth。纯 CSS+JS 防御，不依赖 ST 版本。`npm test` 217/217、`check:ui`、`build:ui:remote`、dry-run、正式 JSON+PNG 打包全绿。检查点升级至未占用的 r85（r84 原样保留）。成品 `dist/checkpoint-0.2.0-r85/幻想乡物语-测试检查点-0.2.0-r85.json`（2,258,895 bytes、SHA-256 `3e4e32567fe654ff5689236289d425dd9e912d6cbc7544bb068cc1f75aa54c20`、26 条世界书、UI 脚本 `gensokyo-garden-ui-020-r85`、包内 UI 与 `dist/runtime/ui-mount.js` 逐字节一致）与 `.png` 卡片（3,846,464 bytes、SHA-256 `63da6ceee095f298c66091d0dad542fac62762578939572557a12311e9b6f0fc`、立绘 512×853 sharp 压缩、chara payload 与 JSON 逐字节一致）。README/manifest/runbook/agent-handoff 已同步 r85。真实 SillyTavern 导入与手机实机验收仍待所有者执行。

> 2026-08-03（九十六，r84 PNG 卡片形态 + 修复进不去游戏）：**修复**：上一轮加「界面提示」开关时，`app.ts` 顶层 `byId('gg-map-hint')` 按 id 查找，但 `index.html:87` 该元素只有 `class="gg-map-hint"` 没有 id——`byId()` 找不到即抛错，整个 app.js 启动崩溃，预览页进不去。已给元素补 `id="gg-map-hint"`；另把 `build:ui` 默认参数改为 `--asset-mode=remote-r2-live`（此前无参默认 embedded 会生成 280MB ui-mount.js，r72 同款事故）。**PNG 卡片形态**：新增 `scripts/embed-card-png.mjs`——读取打包好的 `chara_card_v2` JSON，把 base64 作为 `chara` tEXt chunk 插入立绘 PNG（IHDR 之后，标准 PNG chunk 结构 + crc32），不重编码立绘像素；输出 `幻想乡物语-测试检查点-0.2.0-rN.png` 标准 SillyTavern 卡片。package.json 新增 `package:checkpoint:png` 脚本。**r84 打包**：`dist/checkpoint-0.2.0-r84/幻想乡物语-测试检查点-0.2.0-r84.json`（2,257,481 bytes、SHA-256 `d0510acb78f2ee258740345703375c9a990ee5363061fbc34e2303da0efc8039`、26 条世界书、UI 脚本 `gensokyo-garden-ui-020-r84`）与 `.png` 卡片（5,736,596 bytes、SHA-256 `7862cd52069407f4e495c281bd73a26b10fb0a6a05140446b11b6def7ed292d7`、立绘 1152×1920、chara payload 与 JSON 逐字节一致、全部 chunk CRC 校验通过）。立绘源 `D:\浏览器下载\MiaoMiao_Harem_AnimaBase_single_00504_.png`。`npm test` 217/217、`check:ui`、`build:ui:remote`、dry-run、正式 JSON+PNG 打包全绿。README/manifest/runbook/agent-handoff 已同步 r84。真实 SillyTavern 导入验收仍待所有者执行。

> 2026-08-03（九十五，r83 新人礼包 + 验收辅助面板隐藏）：两项改动。**①验收辅助面板隐藏**：`.gg-test-tools` 加 `display: none`，设置里的「验收辅助 → 测试控制面板」不再显示（DOM/绑定保留，测试断言只查存在性不受影响）。**②设置区新人礼包**：按钮「新人礼包」在设置页 `gg-actions` 区；点击弹出 dialog，倒计时 4 秒后「领取」按钮才可用（`secondsLeft = 4`，每秒递减）；领取调用本地 `claimStarterGift`（`shop-rules.ts` 纯函数）——校验 `interaction.starter_gift_claimed` 未领取 → 加 48 金币/4 灵感/8 物资（按 schema 上限 99999/10/20 截断）→ 置 claimed=true；bridge 正式版走 `replaceMvuData` + 复读校验，离线 preview stub 同步；已领取的聊天打开设置时按钮禁用并显示「新人礼包（已领取）」（`renderStarterGiftButton`）。**持久化跟随聊天文件**：领取标记存 `interaction.starter_gift_claimed`（boolean，随 stat_data/MVU 聊天存档持久化，非 localStorage——换浏览器/换设备仍保留），字段已登记 field-ledger，migration 兜底默认 false。新增测试（奖励值、重复领取拒绝、上限截断、迁移兜底、bridge 双端 stub、UI 断言），`npm test` 217/217、`check:ui`、`build:ui`（本地预览）、`build:ui:remote`、dry-run 与正式打包全绿。检查点升级至未占用的 r83（r82 原样保留）。成品 `dist/checkpoint-0.2.0-r83/幻想乡物语-测试检查点-0.2.0-r83.json`：2,254,681 bytes、SHA-256 `4c07cb4f09a5aeb41d1130301da874349c6c430adf4dad6aa49c7762b1d9adec`、26 条世界书、UI 脚本 ID `gensokyo-garden-ui-020-r83`，包内 UI 与 `dist/runtime/ui-mount.js` 逐字节一致，remote-r2-live。README 与 runbook 的「当前有效轻量包」指针已同步至 r83。本地预览服务器 8765 已在跑（`npm run preview`），dist 已含新人礼包可先看效果。真实 SillyTavern 导入验收仍待所有者执行。

> 2026-08-03（九十四，r82 conversation_log 写入语义修复 + 剧情连续性约束）：实机聊天（r80 检查点）发现 L9 楼层 CoT 误判——玩家在 L6-L7 用了金币·钓饵强上灵梦（NSFW），L8 玩家却"试探摸头"，模型在思维链里主动判断"这是全新分支/平行行动"，把前文 NSFW 当没发生、灵梦退回初见状态。误判根因两层：**①数据层**：协议要求模型每轮 UpdateVariable 追加 conversation_log，但写的是 `{"op":"add","path":"/interaction/conversation_log","value":"字符串"}`——按 MagVarUpdate 源码（`update_variables.ts` insert 三参数分支），`op:add` 到数组字段**不带 `-` 时整体替换字段为 value**；mvu_zod 逐命令重校验 stat_data，`z.array()` 对字符串失败、`.catch([])` 把字段重置为空数组写回——**conversation_log 每轮被清空**，L8 prompt 里【最近互动回顾】永不注入，模型只能靠原始楼层历史，无记忆锚点。**②判断层**：模型把"本轮道具授权：无"误读为"剧情分支切换/记忆重置"信号，用自身"合理性"（避免角色崩溃）压过剧情连续性。按所有者选定方案 1+2 实施：**方案 1 修写入语义**——`gardenNarrativeContract` 第 28 行与 `variable-output-format.md` 明确追加必须用 `{"op":"add","path":"/interaction/conversation_log/-","value":"..."}`（path 以 `/-` 结尾，value 为字符串），禁止不带 `/-` 的数组 path；`migrateGardenState` 兜底——若 MagVarUpdate 已把字段替换成字符串，归一化为单元素 string[]（防 `.map()` 崩溃与历史丢失）。**方案 2 加连续性约束**——`gardenNarrativeContract` 新增条目：本轮道具授权≠剧情分支/记忆重置；前文已发生事实有效；角色应带前文记忆做合理反应（困惑/质问/警惕等），不得装作什么都没发生、把玩家当陌生人；输出前核对上轮结尾与【最近互动回顾】。新增回归测试（协议 `/-` 写法、禁止不带 `/-` 的 path、连续性约束、字符串/空白兜底）。`npm test` 216/216、`check:ui`、`build:ui:remote`、dry-run 与正式打包全绿。检查点升级至未占用的 r82（r81 原样保留）。成品 `dist/checkpoint-0.2.0-r82/幻想乡物语-测试检查点-0.2.0-r82.json`：2,246,603 bytes、SHA-256 `21159ab4398af657ce2855f190cd0e3715fdd83cb1b5c83a8ad77d4014f57119`、26 条世界书、UI 脚本 ID `gensokyo-garden-ui-020-r82`，包内 UI 与 `dist/runtime/ui-mount.js` 逐字节一致，remote-r2-live。README 与 runbook 的「当前有效轻量包」指针已同步至 r82。真实 SillyTavern 导入验收仍待所有者执行。

> 2026-08-03（九十三，r81 怀表时间停止功能补全）：实机发现使用十六夜咲夜的怀表后灵梦仍正常行动/躲开——时停效果没有传达给 LLM。根因：`useSakuyaWatch`（`special-item-rules.ts:106`）只更新内部状态（`temporal_trace_active` 仅作调查事件前置条件），**从不投影给模型**；且 `bridge.useSpecialItem` 只写 `stat_data` 不写聊天楼层，怀表使用消息模型完全不可见；世界书 25 条中也没有怀表条目。按所有者选定方案实施：①数据层 `types.ts` key_item 新增 `time_stop_active` 字段；`useSakuyaWatch` 使用时置真；`advanceOneTimePeriod`（`time-rules.ts`）跨时段自动复位（时停只持续当前时段）。②投影层 `prompt-context.ts` `buildPromptContext` 在【场景事实】后注入【时间停止】段（角色定身、不能主动行动/说话/反应、玩家可自由行动、不替被定身角色编写反应、时段推进自然解除）。③世界书 `src/lorebook/items/sakuya_watch.xml` 新建怀表时停条目，打包器 `package-checkpoint.mjs` 以 const 常驻条目（entry 16，`[mvu_plot][special]`）并入，不依赖场景道具绿灯（怀表是特殊道具非场景道具，`item-routing.json` 不登记）。新增回归测试（使用时 `time_stop_active=true`、prompt 注入【时间停止】段、`advanceOneTimePeriod` 复位、未使用不注入）。`npm test` 216/216、`check:ui`、`build:ui:remote`、dry-run 与正式打包全绿。检查点升级至未占用的 r81（r80 原样保留）。成品 `dist/checkpoint-0.2.0-r81/幻想乡物语-测试检查点-0.2.0-r81.json`：2,243,847 bytes、SHA-256 `0bb5c81a1ff72c59514d918ca33acbe5b12aae3a0d0ac4c052e8adb3ddff49f7`、26 条世界书（新增怀表条目）、UI 脚本 ID `gensokyo-garden-ui-020-r81`，包内 UI 与 `dist/runtime/ui-mount.js` 逐字节一致，remote-r2-live。README 与 runbook 的「当前有效轻量包」指针已同步至 r81。真实 SillyTavern 导入验收仍待所有者执行。

> 2026-08-03（九十二，r80 跨对话记忆 conversation_log）：实机验收发现「结束对话后重新开对话，角色忘记刚才发生的事」——L5（07:15 金币钓饵→脱衣→后入）后点结束，L7 重开时灵梦像第一天第一次见面，时间退回 07:00。根因两层：①`endConversationLocal`（`activity-rules.ts:276`）结束时只清 current_session/场景道具/月见温泉/宴会，**不写任何记忆**；唯一长记忆 `memory.long_term_notes` 只在温室战斗结算写入。②新对话注入的【场景事实】强调"第 1 日清晨/时段 0"，把模型往"第一次见面"引导，且无任何上次对话回顾。按所有者选定方案 A（对话日志变量）实施：新增 `interaction.conversation_log`（string[]，每条 ≤120 字、FIFO 24 条，schema `list(text('',120),24)`，initial-state 与迁移默认 `[]`，`field-ledger.md` 已登记）；`gardenNarrativeContract` 新增每轮追加规则（UpdateVariable 追加"角色ID: 一句话摘要"，结束对话不清空）；`prompt-context.ts` 在【场景事实】后注入【最近互动回顾：结束对话不会抹去这些记忆】段（取尾 6 条，按在场角色 ID 过滤，不在场不注入）。新增全链路测试（迁移默认/保留、endConversationLocal 不清空、prompt 注入过滤）。`npm test` 216/216、`check:ui`、`build:ui:remote`、dry-run 与正式打包全绿。检查点升级至未占用的 r80（r79 原样保留）。成品 `dist/checkpoint-0.2.0-r80/幻想乡物语-测试检查点-0.2.0-r80.json`：2,239,454 bytes、SHA-256 `227cac31874bede78fe7a25fa1f6a6f36d938777486c148a8ec9023dcd12f6f4`、25 条世界书、UI 脚本 ID `gensokyo-garden-ui-020-r80`，包内 UI 与 `dist/runtime/ui-mount.js` 逐字节一致，remote-r2-live；打包产物为双重转义（`\\uXXXX` 大写 hex），人工校验注意大小写。README 与 runbook 的「当前有效轻量包」指针已同步至 r80。真实 SillyTavern 导入验收仍待所有者执行。

> 2026-08-03（九十一，r79 sexual 立绘 URL 重复前缀 404 修复）：实机测试发现 GAL 页面 sexual 立绘 `<img id="gg-portrait">` 的 src 为 `…/live/gensokyo-moving-garden/live/characters/…`（重复路径段）导致 404。根因：`build-ui.mjs:368` remote-r2-live 构建时 `previewAssetBase = baseUrl + "/gensokyo-moving-garden/live"` 注入 `data-asset-base`；`app.ts:188` 读为 `assetBase`；`app.ts:646` 调用 `mergeRemoteSexualPortraitSources(..., assetBase)`；而 `gal-portrait-registry.ts:206` 用 `base + '/' + file.key` 拼接且 `file.key` 本身已含 `gensokyo-moving-garden/live/` 前缀——两次拼接重复，仅 sexual 池受影响（nude/normal 走打包期单次拼接正确）。修复两处：①`app.ts:646` 改用 `resolved.manifest.asset_base_url`（纯 origin，resolver 已校验）；②`gal-portrait-registry.ts` `mergeRemoteSexualPortraitSources` 新增防御——base 的 pathname 必须为 `/`，否则跳过该文件，杜绝带前缀 base 再次造成重复路径。新增回归断言（base 带前缀时 URL 不得出现重复 `/live/` 段）。`npm test` 215/215、`check:ui`、`build:ui:remote`、dry-run 与正式打包全绿。检查点升级至未占用的 r79（r78 原样保留）。成品 `dist/checkpoint-0.2.0-r79/幻想乡物语-测试检查点-0.2.0-r79.json`：2,236,768 bytes、SHA-256 `61d05dc4b8a6dbfadb8d5d6cf9fb586f2ebddc43220144985df8213cefe4e645`、25 条世界书、UI 脚本 ID `gensokyo-garden-ui-020-r79`，包内 UI 与 `dist/runtime/ui-mount.js` 逐字节一致，remote-r2-live。README 与 runbook 的「当前有效轻量包」指针已同步至 r79。真实 SillyTavern 导入验收仍待所有者执行。

> 2026-08-03（九十，r78 实机验收：sexual 触发生效）：所有者在 r78 导入后继续测试，检查 Luker 新聊天 `幻想乡物语·移动庭园（测试检查点 0.2.0-r78）`（21:14 起）。结果：**r78 的 sexual 触发规则生效**——L5 脱衣轮正确 `normal→nude`，L9 后入轮正确输出 `visual_mode="sexual"` + `pose_id="rear"` + `act_id="vaginal"`（命中 R2 `characters/reimu/gal/sexual/rear/vaginal/01.png`），前戏 beat 保持 `nude` 不提前升级，三段式过渡完全符合协议；无 CoT 泄漏（r77 兜底生效）。另发现模型在所有轮次把 `suggested_replies` 的字段名写成 `intention` 而协议要求 `intent`：`normalizeReply`（`gal-scene.ts:80-85`）不认 `intention`，回退用 `label` 作 intent——按钮仍正常显示、点击以 label 发送，仅隐藏意图丢失；协议从未明确该 key 名是根因。**所有者决定暂不处理**（不影响功能），后续如需修：解析器加 `intention` 别名 + 协议明确 key 名。r78 保持当前有效轻量包，无重新打包。

> 2026-08-03（八十九，r78 sexual 触发规则注入每轮正文协议）：实机验收发现模型在插入性行为正文（后入、抽送）中仍输出 `visual_mode="nude"` 而非 `sexual`。归因是**双重协议缺陷**：每轮注入用户消息的【庭园正文协议】段（`gardenNarrativeContract`）的 dialogue 模板只有 `char/reaction/pose`，**缺 `visual_mode` 与 `act`**，且完全没有 sexual 触发规则；sexual 定义只埋在世界书 entry#7 长文里，模型在最新指令位置看不到，于是"脱衣标 nude"记住了、"插入应标 sexual"没学会。修复：`src/ui/target-actions.ts` 的 `gardenNarrativeContract` 把 dialogue 模板补为 `visual_mode="normal|nude|sexual"` + `act="vaginal|anal|none"`，并新增明确规则「正文已进入明确亲密行为（插入、口交等）时 dialogue 与 GensokyoScene 的 visual_mode 必须为 sexual，给出已登记 pose_id 与 act_id（如 rear/vaginal），不得停留在 nude」；`gal-presentation-protocol.md` 格式与 sexual 定义已与之一致无需改动。新增 5 条回归断言。`npm test` 215/215、`check:ui`、`build:ui:remote`、dry-run 与正式打包全绿。检查点升级至未占用的 r78（r77 原样保留）。成品 `dist/checkpoint-0.2.0-r78/幻想乡物语-测试检查点-0.2.0-r78.json`：2,236,597 bytes、SHA-256 `fde445914f3fa37d8b83cffff26fda373a7483d571ee1dd170c8f3d601a38b67`、25 条世界书、UI 脚本 ID `gensokyo-garden-ui-020-r78`，包内 UI 与 `dist/runtime/ui-mount.js` 逐字节一致，remote-r2-live。README 与 runbook 的「当前有效轻量包」指针已同步至 r78。真实 SillyTavern 导入验收仍待所有者执行。

> 2026-08-03（八十八，r77 CoT 泄漏协议加固与代码兜底）：实机验收发现第 5 楼 assistant 输出在【庭园正文开始/结束】之间混入自指纠错 CoT「（注意：…）修正：」——模型误把玩家台词标成 `<dialogue char="reimu">` 后当场纠错并把思考过程写进正文。归因：协议只禁止「正文开始前输出思维链」，未禁止正文内自指纠错痕迹；模型遵守了「玩家台词放 narration」却把纠错过程本身输出了。按所有者决定：**协议侧**在 `gardenNarrativeContract`（`src/ui/target-actions.ts`）与 `gal-presentation-protocol.md` 庭园正文协议节各加一条「正文内严禁自指纠错文本、格式错误必须静默重写」；**代码侧**在 `src/ui/gal-scene.ts` 新增导出 `stripCotLeakage`（正则要求括号内含「应该/这句话/标签/规则/协议」等自指信号词防误伤），接入 `gardenBodySection` 与 `stripNarrativeNoise` 两条路径；误标 dialogue 的玩家台词按确认只靠协议约束、不做启发式改写。新增回归测试（真实泄漏剥离、正文台词「（注意：…）」陈述保留、cleanNarrativeText 路径剥离）。`npm test` 215/215、`check:ui`、`build:ui:remote`、dry-run 与正式打包全绿。检查点升级至未占用的 r77（package.json 与 manifest.json 指针同步；r76 原样保留）。成品 `dist/checkpoint-0.2.0-r77/幻想乡物语-测试检查点-0.2.0-r77.json`：2,235,361 bytes、SHA-256 `b76ce1a76233c21226112972924ddedb739bb57c263379fd03cd9d63103fcb4f`、25 条世界书、UI 脚本 ID `gensokyo-garden-ui-020-r77`，包内 UI 与 `dist/runtime/ui-mount.js` 逐字节一致，remote-r2-live。README 与 runbook 的「当前有效轻量包」指针已同步至 r77。真实 SillyTavern 导入验收仍待所有者执行。

> 2026-08-03（八十七，r76 怀表 GAL 守卫修复与打包）：所有者在 r75 实机测试发现 GAL 页面选中怀表被拦「战斗、固定剧情或受控会话进行中，不能启动怀表」。根因：`useSakuyaWatch`（`src/ui/special-item-rules.ts`）守卫同时检查 `battle.current || events.active_event || interaction.current_session`，而 GAL 即时使用入口恰恰只在对话/受控会话中可用——设计意图（R82 怀表作为 GAL 即时使用选项）与守卫自相矛盾，导致怀表在 GAL 中永远无法使用。按所有者决定放宽守卫：仅 `battle.current` 时禁止（错误文案改为「战斗进行中，不能启动怀表」），对话/受控会话/固定剧情事件中允许即时使用；异变卡守卫（`anomaly-rules.ts`）语义不同保持不变。新增回归断言：受控会话中可用、active_event 中可用、战斗中抛错。`npm test` 214/214、`check:ui`、`build:ui:remote`、dry-run 与正式打包全绿。检查点升级至未占用的 r76（package.json 与 manifest.json 指针同步；r75 原样保留）。成品 `dist/checkpoint-0.2.0-r76/幻想乡物语-测试检查点-0.2.0-r76.json`：2,233,544 bytes、SHA-256 `ebe00ffa21af045b109ee3eefa6534edd840b63bfa481d441005295fa6441adc`、25 条世界书、UI 脚本 ID `gensokyo-garden-ui-020-r76`，包内 UI 与 `dist/runtime/ui-mount.js` 逐字节一致，remote-r2-live。README 与 runbook 的「当前有效轻量包」指针已同步至 r76。真实 SillyTavern 导入验收仍待所有者执行。

> 2026-08-03（八十六，r75 轻量测试检查点打包交付）：所有者要求打包一份测试 JSON。按 runbook 固定顺序执行：`check:ui`（tsc 通过）、`npm test`（214/214 全绿）、`npm run build:ui:remote`（remote-r2-live，origin `https://ssrfrrt.ccwu.cc`）、`package:checkpoint:dry`、`package:checkpoint` 全绿。因 `dist/checkpoint-0.2.0-r74/` 已存在 12:27 旧构建（UI 不含 13:04–20:11 的商店/背包/GAL 道具选择器与道具绿灯等 14 处源改动），经所有者选择升级至未占用的 r75：`package.json` 两个打包脚本、`manifest.json` 的 `next_checkpoint`/`planned_checkpoint_sequence`/`runtime_artifacts` 三处指针同步升至 `0.2.0-r75`，r74 旧产物原样保留。成品 `dist/checkpoint-0.2.0-r75/幻想乡物语-测试检查点-0.2.0-r75.json`：2,233,704 bytes、SHA-256 `926cff8ab75c552eb108995e39a5385a2fa586afe40de30d28c44c1e31f23a0b`、25 条世界书、UI 脚本 ID `gensokyo-garden-ui-020-r75`，包内 UI 与 `dist/runtime/ui-mount.js` 逐字节一致，remote-r2-live 模式且固定生产 origin；dry-run 与正式写入字节/哈希完全一致，collision_policy `refuse-overwrite`。README 与 runbook 的"当前有效轻量包"指针已同步至 r75。真实 SillyTavern 导入验收仍待所有者执行。

> 2026-08-03（八十五， CG Agent 详细执行手册）：新增 `project/nsfw-cg-agent-execution-runbook.md`，面向不了解项目历史的主 Agent／子 Agent，逐步固化不看图审计、跨角色哈希查重、冻结语义映射、候选编号、台账状态机、首包文件级施工范围、最低测试矩阵、远端 manifest 只读预检、generation 并发基线、dry-run／正式 staging、原字节四方哈希、上传计划、逐 key 碰撞决策、真实上传器缺口、manifest-last、失败回滚、单协调者与子 Agent 任务模板、停止条件及最终报告格式。`CG/README.md`、命名合同、灵梦 R2 计划和项目总览均已链接该手册。手册明确当前上传器仅支持 dry-run；没有另行实现并获生产写入授权前，任何 Agent 的终点只能是 staging／碰撞审计，不得声称已发布。本轮仍未查看图片、未复制图片、未生成 staging、未写 R2。

> 2026-08-03（八十四，灵梦 sexual CG 文件名审计、逐角色台账与 R2 更新计划）：严格未查看或渲染图片，只读取文件名、大小、SHA-256 与 PNG 8 字节签名。`CG/灵梦/` 共 11 张、27,877,219 bytes，11/11 PNG 签名正确、重复哈希组 0；按所有者中文名映射为 9 个逻辑池，其中 `cowgirl.vaginal` 与 `missionary.anal` 各有 `01/02` 双候选。新增 `CG/README.md` 和八名固定角色各自的 `上传台账.md`；灵梦台账记录精确 source、字节与哈希，其他角色为空表。生产只读探测确认 `live/manifest.json` 为 `gensokyo-r2-live.v1` generation 1、184 files／208,627,661 bytes、sexual entries 0，故灵梦 11 项统一标记 `待上传`。新增 `project/nsfw-cg-r2-live-update-plan.md`：先实现 sexual resolver、v1 可选卡池字段、原字节维护源与测试，再从执行时最新 manifest 生成 N+1 staging，逐 key 碰撞审计，媒体先上传并 GET 校验，manifest 最后更新，最后回写台账。本轮未复制图片、未生成 staging、未调用上传器、未写 R2。

> 2026-08-03（八十三，体位图片固定命名与 R2-only 更新规划）：新增 `project/nsfw-pose-live-asset-naming-plan.md`，冻结 `character_id × 17 pose_id × act_id × 01–99` 命名合同；每角色预留 28 个逻辑池，灵梦可在图片不全时只发布已人工确认候选，缺池按 `nude`／`normal` 回退。运行 key 统一为 `live/characters/<id>/gal/sexual/<pose>/<act>/<nn>.png`；所有 sexual CG 直接发布所有者原始 PNG 字节，不压缩、不转 WebP、不缩放、不改 Alpha、不量化、不重编码，维护源与 R2 对象 SHA-256 必须一致。后续补图先上传并校验媒体、最后更新单调 generation 的 `live/manifest.json`。同时废止 GAL 计划中残留的 `gal-pools/`／`channels/gal-stable.json` 双轨表述。必须注意：当前 `gal-portrait-registry.ts` 的 sexual 列表仍为空、resolver 仍只实际读取 normal/nude；首个公开包必须先实现 `act_id` sexual resolver、动态候选发现、路径校验和回归测试，完成后才能承诺后续只更新 R2。规划过程未查看任何图片，也未对 `CG/灵梦/` 原始流水号文件猜测体位。

> 2026-08-03（八十二，商店／背包／GAL 道具选择器 UI 优化与道具绿灯世界书）：本轮两类改动。**UI 优化**：灵梦小店新增商品分页（10/页，单页隐藏翻页控件）、每件商品 `blurb` 精炼介绍（商店目录与 items 目录共用短文案，详情框默认显示、3 行截断，点击弹出完整介绍）、购买成功/失败改为左下角详情框上方的气泡提示（success 绿／error 红，4.2s 自动消失，原底部状态条仍同步）；移除商店内 `gg-shop-use` 使用按钮（怀表／异变卡改由背包与 GAL 选择器入口）；详情区布局修复（标题固定，描述不再顶动标题）。背包改为每页 4 件并压缩卡片/间距降低整页高度。GAL 场景道具选择器新增分页（6/页）、blurb 短描述（2 行截断）、长按 500ms 弹完整介绍、「不使用道具」固定跨行置顶，并把怀表作为即时使用选项加入（金色「刻」标记、当日冷却禁用，点击走 `useShopItem`）。符卡副本选关 UI 改为单卡居中横版关卡公告卡（主题色辉光条、Boss 名放大、按钮横排主次）。**道具绿灯世界书**：新增 `src/lorebook/item-routing.json`（`item-greenlight.v1`）与 `src/lorebook/items/*.xml` 共 8 条道具世界书（振动棒/跳蛋无限定，金币钓饵限灵梦，青蛙诱饵/冰之玩具限琪露诺，梦境菇/服从之页限魔理沙，人偶化限爱丽丝）；`src/ui/item-greenlights.ts` 实现 `itemGreenlightContext`/`stripItemGreenlights`，触发源为 `scene_item_context.entries`，在 `withGardenNarrativeContract` 角色绿灯段后注入【道具档案绿灯】；打包脚本校验 `GSK_ITEM_*_ACTIVE` 格式并以条目 id 18+ 注册、防递归诱发；`lorebook_entries` 现为 25。门禁：`check:ui`、`npm test` 214/214、`build:ui`、`package:checkpoint:dry`（remote-r2 构建后）全绿。**遗留**：道具生效限定目前只在世界书条目层约束模型，代码级强制未做；商店/背包/GAL 选择器改动与绿灯加载均未在真实 SillyTavern 导入验收。

> 2026-08-02（八十一，r69 GAL 道具选择器收尾解锁）：真实 SillyTavern 外层控制台诊断确认，回复稳定后 `transactionBusy=false`、GAL 输入仍可用、结尾显示“结束聊天”与 `10/10`，但 `#gg-scene-item` / `#gg-scene-item-trigger` 仍为 disabled。根因是 `submitGalMessage()` 在 `submissionInFlight=true` 时先 `refresh()`，选择器据此绘制锁定；`finally` 才释放本地锁，却没有重绘选择器。现已在发送收尾释放锁后、且仍停留 GAL 时立即 `renderSceneItemPicker()`；本地结束失败而继续停留 GAL 的收尾也同样重绘。新增回归断言，防止后续把“当前剧情阶段不可追加道具”残留在已经完成的正常对话里。`check:ui`、`npm test` 205/205 与构建已通过；r69 轻量包将固定读取 live 素材接口，旧检查点不覆盖。真实验收需导入 r69，以正常角色对话发送一次、等回复完整落盘后确认道具按钮自动恢复可点。

> 2026-08-02（八十，r63 新 R2 release 与轻量打包）：琪露诺静态 turnaround 顶部残留已清理，维护源 PNG 与运行 WebP 同步；GAL 立绘缩放、琪露诺待机／移动方向变换与亮度、开场素材 8 次尝试、GAL 首次触发 LLM 竞态修复均已进入当前维护源。为避免 r62 默认 embedded 构建产生 75,429,207 bytes 肥包，从隔离干净素材提交 `0e5ecacdee9f` 生成并发布不可变 R2 release `0.2.0-r62-0e5ecacdee9f`：114 个素材共 54,971,703 bytes，manifest 声明哈希 `0f068864b044613d4d5110ad6f7a850f7aecec1609821a90d0a5ed16cd5a8965`；素材先上传并逐项校验，SVG 以 GET+SHA-256 兜底，manifest 最后上传且公网文件与本地逐字节一致。随后使用显式 `remote-r2` 构建生成 r63，成品 `dist/checkpoint-0.2.0-r63/幻想乡物语-测试检查点-0.2.0-r63.json` 为 2,145,471 bytes，SHA-256 `0923e1c5517beff64a7fbedcf7c02d52636c79ddc073ae37ea9a25b3f0183f06`；`check:ui`、`npm test` 200/200、R2 专项 16/16、包内 UI parity 和秘密扫描通过。完整、可复制的 staging／上传／远程构建／打包顺序见 `project/r2-packaging-runbook.md`。旧 release 不可自动删除：r56–r61 仍固定引用 `0.2.0-r55-1ef0d7d6cbab`；删除必须另获精确前缀授权并先完成消费者清点。真实 SillyTavern 导入 r63 仍待所有者验收。

> 2026-08-01（七十九，r58 地图／快进／R2 设施修复候选）：本轮处理四项实测反馈。设施操作菜单原本虽然收到 GardenMap 的逐帧锚点回调，却在桌面端被 `245px` 固定安全区夹住，现改为直接使用有限数值锚点，拖图冒烟中魔法温室菜单从 `243.2/135.3` 移到 `245.2/190.3`，相机 Y 同步移动 `55px`。教程“跳过指引”现改为零模型调用 `m2_open_garden`，成功后才隐藏指引，正式进度为 13/13 完成。读取 `F:\agent airp\Luker\data\default-user\chats\幻想乡物语·移动庭园（测试检查点 0.2.0-r57）` 最新聊天后确认：最后 assistant 已完成生成，最终事件 settlement 已进入 `stat_data.events.settled_ids`，误拦截来自 iframe 重挂载后遗留的协调器 `settling`；`applyTestJump` 现先拒绝真实生成态，再对 `settling` 执行持久层恢复并复读。四设施素材和生产 R2 URL 均有效，消失根因是预加载先使用非 CORS 缓存、Canvas 后使用 anonymous CORS；地图、人物、设施、战斗等 Canvas 首次预加载现统一 anonymous CORS。`check:ui`、embedded 构建、`npm test` 196/196、固定生产 manifest 的 remote-R2 构建、R2 专项 13/13、embedded／remote-R2 内置浏览器冒烟、dry-run 与正式打包全绿。成品为 `dist/checkpoint-0.2.0-r58/幻想乡物语-测试检查点-0.2.0-r58.json`，2,138,350 bytes，SHA-256 `a7edffabd04785e309ddb7bea71bb94c800967eb75a454faaf6ade18d67d9229`，UI 脚本 ID `gensokyo-garden-ui-020-r58`，16 条世界书；r57 未覆盖。浏览器验证仍是本地宿主模拟，真实 SillyTavern 中的旧聊天快进恢复需导入 r58 后最终复验。

> 2026-08-01（七十八，r57 确定性开场修复候选）：所有者在真实 SillyTavern 导入 r56 后发现开场停在“祖父留下的故事仍在展开”，并确认默认开局不应调用 LLM。根因是 `OpeningController` 回归到 `commitOpening → /trigger → 等待 assistant → enterGarden` 的生成式两阶段流程，同时契约与打包提示也被旧流程覆盖。现已把默认入口恢复为 `initializeOpening(draft, frozenChatId)`：素材门通过后直接在首个 assistant 楼层幂等合并初始状态、调用 `Mvu.replaceMvuData` 并复读，不创建 user/assistant 楼层、不触发 `/trigger`、不调用 LLM；生成式序章／修复面板从默认 DOM 和控制器移除，桥接层只保留历史聊天兼容解析。开场契约、世界书首次行动引导、首条消息、打包说明与回归测试已同步。`check:ui`、embedded 构建、`npm test` 194/194、固定生产 manifest 的 `remote-r2` 构建、R2 专项 13/13、dry-run 与正式打包全绿。已生成 `dist/checkpoint-0.2.0-r57/幻想乡物语-测试检查点-0.2.0-r57.json`，2,137,778 bytes，SHA-256 `1ed2a5bcb57ce15f1f4356adac1cf93421bf931a5be3e524bfa31b7ac7b2df31`，UI 脚本 ID `gensokyo-garden-ui-020-r57`，16 条世界书；固定生产 R2 release `0.2.0-r55-1ef0d7d6cbab`。r56 未覆盖但已被替代，不应继续测试。真实门禁仍需新聊天导入 r57：点击入场前后楼层数不变、无生成请求、直接进入庭园，第一次真实行动才首次调用 LLM。

> 2026-08-01（七十七，r56 全员摸摸头／净化历史测试候选）：审查确认 `pat_head` 已从灵梦专属分支移到统一角色分支，八名登记角色各得到且只得到一次普通 GAL 互动；提示明确“只是尝试，不预设接受”，不绑定事件、不触发本地结算。新增八角色回归测试。同期 r56 收入 GAL 顶栏小型历史入口与玩家消息隐私元数据：手动输入只显示原文，建议回应只显示可见标签，程序自动提交明确隐藏；旧消息仅走兼容净化。五件旧场景道具退役、八件新道具、战斗预加载 anonymous CORS 修复均随当前维护源进入候选。`check:ui`、embedded 构建、`npm test` 194/194、固定生产 manifest 的 `remote-r2` 构建、R2 专项 8/8 与 dry-run 全绿。已生成 `dist/checkpoint-0.2.0-r56/幻想乡物语-测试检查点-0.2.0-r56.json`，2,145,682 bytes，SHA-256 `b4bb4e61a2b772d5068b999853fe73e9aa67e802c7ec47ce14030a05ea111784`，UI 脚本 ID `gensokyo-garden-ui-020-r56`，16 条世界书；产物固定生产 R2 release `0.2.0-r55-1ef0d7d6cbab`，r54 及更早检查点未覆盖。当前状态仍是离线 candidate；需在真实 SillyTavern 新聊天验收八人摸头、历史净化、R2 加载与既有玩法入口后才能接受。

> 2026-08-01（七十六，生产 R2 战斗图集回退诊断）：生产 release 的普通预加载计数虽为入口 16/16、总调度 114/114、失败 0，但符卡副本实测仍显示几何回退。代表性战斗图集 URL 均正确指向 `0.2.0-r55-1ef0d7d6cbab`，R2 公开响应也带 `Access-Control-Allow-Origin: *`；独立浏览器探针进一步确认同一批 WebP 在普通 `Image` 模式全部可解码，而在 `crossOrigin="anonymous"` 模式下玩家、Boss、通用特效与本地弹幕图集失败，仅妖精图集在该次竞争中成功。根因位于浏览器缓存请求模式冲突：`AssetPreloader.loadAsset` 先以非 CORS 图片请求缓存战斗 URL，`loadBattleAtlas` 随后以 anonymous CORS 重载相同 URL，浏览器可能复用前一种无 CORS 资格的缓存响应并触发 `onerror`，导致 renderer 按设计退回几何图形。当前测试只检查 Canvas loader 设置 CORS，未覆盖“非 CORS 预加载后再 CORS 加载同 URL”的真实顺序。修复应优先让战斗类预加载从第一次请求起就使用 anonymous CORS，或让预加载器与 atlas 共享同一个已验证的图片实例；不能简单移除 atlas CORS。修复后必须重新构建新 release（不可覆盖当前前缀），并在真实战斗中验证玩家、Boss、敌弹、特效及妖精均实际调用贴图绘制。

> 2026-08-01（七十五，R2 生产 release 发布）：从干净提交 `1ef0d7d6cbab43e02b455b5e7c04e880cac57ef2` 的隔离 worktree 生成不可覆盖 release `0.2.0-r55-1ef0d7d6cbab`，固定 origin 为 `https://ssrfrrt.ccwu.cc`。114 个素材共 `54,968,864` bytes，manifest 声明 SHA-256 为 `c44ff80e99c51f63fb61092bfeff9e0fd0e2ee467ae614437a66615bc45c2b29`；上传前确认前缀为空，素材先上传，manifest 最后上传，远端共 115 对象。标准构建与 `npm test` 191/191、`remote-r2` 构建、`check:ui` 和 R2 专项 8/8 均通过；114 个远端素材逐项核对长度、MIME 与 Cache-Control，唯一未在 S3 HEAD 暴露长度的 SVG 改以公开下载字节数和 SHA-256 复核通过。正式域名 manifest 与本地发布文件整文件哈希一致，代表性 WAV 匿名 Range 返回 206，CORS、MIME、长度和 immutable 缓存头通过。生产浏览器随后确认构建坐标为新域名／新 release／新哈希，入口 16/16、总调度 114/114、失败 0，控制台无 warning/error。旧 release `0.2.0-r55-bbc0e074f993`、`r2.dev` 与 r54 均未覆盖；下一步是真实 SillyTavern 候选验收。所有秘密未写入仓库，聊天中暴露过的 Token 与 S3 密钥仍须轮换。

> 2026-08-01（七十四，R2 生产域名与 CORS 激活）：所有者明确授权后，已用项目唯一桶 `hxxwy` 的 S3 兼容凭据复核并按仓库模板应用匿名公开读取 CORS：允许 GET/HEAD 与 `Range`，暴露 `Content-Length`／`Content-Range`／`ETag`，预检缓存 7200 秒。账户级 Cloudflare Token 经正确的 `/accounts/{account_id}/tokens/verify` 端点确认为 active；使用所有者提供且由公开 DNS 委派关系佐证的 Zone ID，将 `ssrfrrt.ccwu.cc` 绑定到 `hxxwy`，启用公开访问并把最低 TLS 固定为 1.2。Cloudflare 回读 ownership/SSL 均为 active；自定义域名下固定 release manifest 可读，代表性 WAV 匿名 Range 返回 206，MIME、长度、CORS、ETag 与 immutable 缓存头通过，原 `r2.dev` 仍保持启用。旧 release `0.2.0-r55-bbc0e074f993` 未覆盖，其 manifest 仍固定声明旧 `r2.dev` origin；下一步必须从干净提交为 `https://ssrfrrt.ccwu.cc` 生成新的不可覆盖 release，再构建并复验 `remote-r2`，不能直接篡改旧 manifest。凭据曾由所有者发入聊天，完成迁移后必须轮换；本轮未把任何凭据写入仓库或临时文件。

> 2026-08-01（七十三，R2 首次预发布与 main 整理）：所有者授权后，已从干净提交 `bbc0e074f993` 生成不可覆盖 release `0.2.0-r55-bbc0e074f993`，发布到项目唯一 R2 桶 `hxxwy`。114 个素材对象先上传，manifest 最后上传；素材共 `54,968,893` bytes，远端加 manifest 共 115 对象、`55,050,924` bytes，manifest SHA-256 为 `75c797954353be3d5272a7649c9e6491b151aae43743ebd8406165724a83c08e`。桶已启用短期 `r2.dev` 公开读取并应用 GET/HEAD、Range、暴露长度／范围／ETag 的 CORS；公开 manifest 与代表性地图、UI、SVG、角色和音频请求均通过。`remote-r2` 预览随后补齐全部 dataset，GAL 注册表只接受当前可信 asset base；预加载器移除不必要的二次 CORS 图片模式，避免与页面先载入的按钮／商店图缓存冲突。最终浏览器验收为入口 16/16、总调度 114/114、失败 0，10 张 GAL 立绘在非 GAL 队列后静默完成，控制台无 warning/error；完整门禁 `npm test` 191/191。对应本地提交为 `bbc0e07` 与 `5558356`。生产域名拟使用 `ssrfrrt.ccwu.cc`，但现有 Cloudflare 管理 Token 返回 401、Wrangler 未登录，域名尚未绑定；继续时应先恢复控制台登录或新最小权限管理 Token，再绑定、等待 SSL active、生成新 release 并复验。所有秘密均不得写入仓库。

> 2026-08-01（七十二，R2 上传前本地实现）：在不登录 Cloudflare、不建桶、不应用 CORS/域名、不上传对象的边界内，release manifest 已升为 `gensokyo-r2-release.v2`，114 项均带 `priority_class / bundle / trigger / entry_gate / category`；其中 103 项非 GAL、11 项 GAL。预加载器已改为幂等优先调度器：开场后台加载、入口门独立进度、15 秒一次性安全超时、单项三次尝试、状态上下文门、场景／单素材 `ensure` 抢占、非 GAL 完成或 fallback 后 GAL 才启动、慢网降并发、页面隐藏暂停低优先派发及销毁取消。新增固定纯 HTTPS origin/release/hash 的可信 manifest resolver、远程图片匿名 CORS、显式 Cache Storage 离线包服务模块（尚未接设置页），以及只读 staging 的单桶 `publish-r2-assets.mjs --dry-run`；真实上传路径故意不存在。主代理复核时修正了入口超时在刷新中重复阻塞的问题，统一收紧 staging／UI 构建的 origin 与 manifest 哈希校验，把发布器限制到 `dist/asset-release/<release>/manifest.json`，明确计划由素材对象加最后发布的 manifest 构成，并补齐所有 Canvas 图片加载器“先设置匿名 CORS、后赋值 src”的顺序。embedded 仍为默认，`remote-r2` 构建必须显式读取干净 v2 staging manifest。最终门禁为 `check:ui`、`npm test`（191/191）、`build:ui`、`check:assets:r2` 与 `git diff --check` 全绿；dry-run 为 114 个素材对象、54,968,893 bytes、manifest SHA-256 `07bf63c2…4cab5fb3`。当前工作树为脏树，未生成正式 staging；下一停止点是所有者提供唯一桶名、生产域名、正式 release ID 与上传授权后，先从干净提交生成 staging 并审查上传计划。

> 2026-08-01（七十一，R2 单桶规划收敛）：所有者确认整个项目永久只使用一个 R2 桶。部署规划已统一为单桶、单自定义域名、单 CORS 与单最小权限上传入口；核心 `releases/`、GAL `gal-pools/`、`channels/` 和未来 BGM 只通过桶内前缀及独立 manifest 隔离，发布工具不得要求或创建第二个业务桶。首版仍为公开匿名只读，因此只允许公开分发素材；内容 UI 开关不构成访问控制，未来若确需私有读取只能保留单桶并整体迁移到经安全评审的 Worker。压缩后的离线包预算已修正为单 release 至少约 65.53 MiB、双版本切换建议至少约 131.05 MiB；GAL 调度按所有者要求改为 103 个非 GAL 对象完成或 fallback 后才后台启动，提前触发时仅抢占命中图与直接 fallback。同步更新 R2 总计划、部署交接、GAL 卡池规划和项目总览；本轮只改计划文档，未建桶、未登录 Cloudflare、未上传、未接 `remote-r2`、未打包。

> 2026-08-01（七十，非 GAL 运行素材 WebP 压缩）：按所有者授权压缩活动 manifest 中除 GAL 立绘与 GAL 舞台背景外的 88 张运行图片；25 张角色小人图集使用原尺寸无损 WebP，27 张 S0/S1/S2 战斗立绘使用 Q50、等比缩至 800px 高，9 张 Boss 四状态图集使用原尺寸 Q50，其余地图、设施、UI、自机和特效使用原尺寸 Q70。PNG 维护源全部保留在同目录且不进入运行发布清单。88 张运行图由 149,872,049 bytes 降至 29,310,208 bytes（-80.44%）；当前 R2 dry-run 仍为 114 个对象，总计 54,968,893 bytes（52.42 MiB），其中非 GAL 103 项 29,619,208 bytes（28.25 MiB），GAL 11 项保持 25,349,685 bytes（24.18 MiB）。`build-ui` 已改为按 manifest 读取 Boss／世界素材并按扩展名生成正确的 PNG/SVG/WebP data URL，预览服务器补 WebP MIME；61 张未缩放 WebP 的 alpha 平面逐张与 PNG 完全一致，代表性地图、设施、UI 和战斗特效已目视抽检。`check:ui`、`npm test`（182/182）、`build:ui` 与 `check:assets:r2` 全绿；未上传 R2、未改 Cloudflare、未正式打包，Q50/Q70 素材仍待真实 SillyTavern 的实际缩放与连续动画验收。

> 2026-08-01（六十九，Cloudflare R2 本地部署准备）：本地 `main` 已按所有者要求强制对齐最新 `origin/main` `9795868` 后开工。新增 manifest 驱动的运行素材收敛器与 `build-runtime-assets.mjs`：当前精确收敛 114 个运行文件、175,530,734 bytes，生成不可覆盖的 `dist/asset-release/<release-id>/files + manifest.json`，逐文件记录 ASCII 对象 key、MIME、字节、SHA-256、immutable 缓存与 fallback；路径越界、符号链接、source/frames、chroma、Aseprite 和历史归档均被拒绝。四张此前硬编码的 UI 入口图已补入活动 asset manifest，`build-ui` 改由 manifest 读取。新增公开匿名 GET/HEAD + Range 的 R2 CORS 模板、部署交接文档、Wrangler 4.118.0 固定依赖及 2 项专项测试；`check:assets:r2`、同名 release 拒绝覆盖、`check:ui`、`npm test`（178/178）和 `build:ui` 全绿。当前没有 Cloudflare 登录态、桶名、域名或秘密配置，未建桶、未上传、未改 DNS/CORS、未接 `remote-r2`、未打包角色卡；下一步需所有者提供 account、桶、生产域名、公开读取确认、release ID 与协议修订授权。

> 2026-08-01（六十八，弹幕阵营识别优化）：针对“小符札与敌弹过于相像”的反馈，`battle-renderer.ts` 已将自机射击从“辉光胶囊→彩体→白芯”改为独立纸符文法：米白纸面、近黑描边、普通／专注对应红／青符印、金色顶签与双尾带；敌弹继续使用圆弹／米弹／鳞弹等发光弹体，即使颜色重叠也能依靠轮廓和尾迹辨认。红色 P 点额外增加不随翻面压缩的四角拾取框，降低与红弹混淆。模拟、碰撞半径、伤害、弹速、Power、结算及 `BattleResult` 均未改；新增“自机符札与敌弹使用不同轮廓语言”契约测试。`check:ui`、`npm test`（176/176）与 `build:ui` 全绿，本地预览已重载并进入妖精弹幕练习检查实际缩放；真实 SillyTavern、320px、色觉辅助与高密度交火仍待验收。本轮未打包、未上传 R2。

> 2026-08-01（六十七，战斗内置音频设置与暂停）：战斗弹窗顶部现提供“暂停／继续”和“音频设置”；设置以战斗专属小弹窗承载，不再把裸露滑杆常驻 HUD。面板可分别启用和调整音效、选择 BGM 模板曲目并调整 BGM 音量，设置打开期间游戏暂停，关闭后按此前状态恢复；偏好只写本机 `localStorage`，不写 MVU。新增 `battle-bgm.ts` 与 `battle-bgm-catalog.json`：当前登记三首模板曲目，`source_url` 均为 `null`，未来仅接受 HTTPS R2 URL，不包含桶名、凭据或远程脚本；接入说明见 `project/battle-bgm-r2-template.md`。离线预览已检查默认 45%、曲目／65% 音量持久化及恢复，`check:ui`、当时 175/175 测试与 `build:ui` 全绿；当前总门禁以后续六十八条的 176/176 为准。真实 SillyTavern 的音频权限、后台恢复、移动端短视口与实际 R2 曲源仍待验收，R2 尚未部署。

> 2026-08-01（六十六，魔理沙 GAL 十图本地接入）：清点并目视 `旧素材/素材处理/CG/魔理沙/透明背景/` 的 10 张所有者 PNG：`sfw` 五图映射 `normal`，`nsfw` 五图映射 `nude`，“正常／开心／害羞／哭泣／生气”映射 `neutral / smile / shy / sad / angry`。全部为 `1152×1920` RGBA；已按原字节复制到 `src/assets/characters/marisa/gal/{normal,nude}/`，源目录保留且所有复制哈希一致，未裁切、缩放、改 Alpha 或重编码。asset manifest 新增 `gal_portraits.marisa`；注册表 10 槽改为 `ready` 并增加受控源解析、角色级 fallback 和 URL 协议白名单。预览构建会复制 10 张原图，自包含构建会嵌入 PNG data URL，宿主通过 `data-gal-portrait-sources` 注入 iframe，GAL 舞台按当前 beat 真正切图并为高分辨率立绘禁用像素化缩放。`sexualPoseIds` 仍为空，明确 sexual 请求降级到同反应 nude／normal。`check:ui`、GAL 定向测试和 `build:ui` 通过；dist 十图与维护源哈希一致，本地浏览器确认 10 槽进入页面且无控制台 warning/error，但未完成真实消息驱动的逐表情舞台截图，也未做真实 SillyTavern、窄屏或显式内容设置验收。本轮未改 MVU、未上传 R2、未打包检查点。
>
> 2026-08-01（六十五，魔理沙 GAL 首批表情槽）：新增 `src/ui/gal-portrait-registry.ts`，为 `marisa` 固定登记 `neutral / smile / shy / sad / angry` 五种反应，并为 `normal` 与 `nude` 各预留五个逻辑素材槽，共 10 槽。图片尚未提供，因此槽位路径保持 `null`、状态为 `awaiting-owner-image`，没有伪造文件或修改 asset manifest。魔理沙的角色级归一化已接入 GAL 解析链：其它通用反应安全回退 `neutral`，`normal`／`nude` 姿势固定 `default`；当前 `sexualPoseIds` 为空，虚构姿势会归一化为 `default`，后续素材解析按同反应 `nude` → `nude.neutral` → 同反应 `normal` → `normal.neutral` 降级。模型可见协议已同步这五种反应及“暂无 sexual 图”的输出规则。本轮未接图片、未改 MVU、未改 R2、未打包；待所有者提供 10 张图片后再做人工对应、归档、manifest 和 UI 接线。
>
> 2026-08-01（六十四，GAL 三态 LLM 表现变量）：按 `project/gal-portrait-variable-and-r2-pool-plan.md` 完成 A 阶段。`GalBeat` 新增 `normal / nude / sexual` 三值 `visualMode`；`scene.v1` beat 与优先投影的庭园正文 `<dialogue visual_mode="...">` 均进入同一归一化链，缺失或未知值安全回退 `normal`。`normal`／`nude` 暂强制 `pose_id=default`，`sexual` 保留安全格式姿势 ID，待下一阶段按角色贴图注册表做存在性白名单；模型协议已明确裸露不等于亲密阶段、模式不授予行为或状态、不得输出素材路径／URL／对象键。GAL 舞台同步写入 `data-visual-mode`，但本轮没有接正常／全裸／姿势图片，没有修改 MVU、上传 R2、部署 Worker、打包或覆盖 r54。`check:ui`、GAL／庭园正文定向测试与 `build:ui` 通过；全量测试 170 项中 169 项通过，唯一失败为工作区既有对战卡按钮测试仍寻找已被当前道具匣替代的 `gg-use-duel-card`，与本轮无关。真实 SillyTavern 的模型遵循度、Swipe／重生成与旧聊天回退仍待验收。
>
> 2026-07-31（六十三，琪露诺四阶段修正）：固定副本 `fairy_pattern_practice_v1` 从两阶段扩为四阶段并升至配置 v5，保持非符／符卡交替；新增“冰晶交叉练习”与“冰符「完美冻结练习」”，只使用现有白名单弹型与合法参数。既有 `bossDamageLevel` 因此会在 P1–P2 选择 S0、P3 选择 S1、P4 选择 S2，解决实战到不了全损槽位的问题。按所有者要求，本轮禁止查看 S2 图片：未解码、未截图、未做像素分析，只以代码槽位和阶段索引验证。
>
> 2026-07-31（六十二，弹幕真音效本地接入）：已把 `音效/web-sfx/` 的 26 个 AI 重生成 WAV 原字节归档到 `src/assets/audio/source/battle-ai-regenerated-v1/`，并派生 14 个稳定英文事件文件到 `src/assets/audio/runtime/battle/`；活动 manifest 登记来源、事件映射与逐文件 SHA-256。`BattleSoundBus` 现使用惰性 WebAudio 解码、应用级缓存、高频事件节流、页面隐藏挂起和静默失败；设置页新增启用、音量与试听，战斗 HUD 可即时开关，偏好只写 `localStorage`。预览构建复制 WAV，自包含 mount 改用 WAV data URL，宿主透传 dataset；模拟与结算合同未改，`nullSoundBus` 仍保留为 fallback。本轮不含 BGM，也没有上传或部署 R2。`check:ui`、`npm test`（166/166）与 `build:ui` 全绿；本地 HTTP 与实际听感仍需最后确认，真实 SillyTavern 的首次手势解锁、音量和连续高频声音待验收。
>
> 2026-07-31（六十一，音效入库与全素材 R2 发布规划）：盘点 `音效/web-sfx/` 的 26 个 WAV
> 候选，确认总计 660,862 bytes，均为单声道 22050Hz（20 个 8-bit、6 个 16-bit），当前仍未
> 进入 `src/assets`、manifest、构建或真总线。新增
> `project/asset-delivery-and-r2-plan.md`，规划 source/runtime 双层音频目录、14 个既有
> `BattleSfxId` 的候选映射、WebAudio 解码缓存／节流／静音／生命周期，以及全部地图、UI、
> 角色、设施、战斗和音频素材的 manifest 驱动发布清单。未来上线目标为固定 release ID 的
> Cloudflare R2 自定义域名、匿名 GET／HEAD、不可变缓存、CORS、远端哈希校验和 embedded／
> remote-r2 双模式回退；明确禁止整树上传 `src/assets` 或可能含历史残留的 `dist/assets`。
> 本轮只更新规划与导航，没有复制／加工音频、修改运行时代码、上传 R2、改 DNS／CORS、打包或
> 部署；当前弹幕仍静音，现行“禁止远程运行依赖”协议仍有效，实施远程模式前需所有者另行授权
> 修订并提供明确部署坐标。

> 2026-07-31（六十，V2 地图素材增量归档）：完成 V3 接入后的二次资产引用审计。将 1 张已被 `garden-base-owner-v3.png` 取代的 V2 底图、32 张 V2 设施／损坏覆盖层及其 chroma 维护源、4 张早已退出运行时的主屋损坏／修复叠图，共 37 个文件、64,007,650 bytes，按原相对路径移动到 `旧素材/`。`project/manifest.json` 的当前底图指针同步纠正为 V3；`asset-manifest.json` 不再把退役主屋图片伪装成活动素材，改为仅登记归档根与历史状态。角色 `sequence-v1` 测试链、approved 序列、turnaround／walk fallback、世界近景 states、V3 地图设施及新战斗素材均未移动。归档前后 `check:ui`、`npm test`（161/161）、`build:ui`、r54 dry-run 与 `git diff --check` 全绿；最终 dry-run 为 `205,582,027` bytes、SHA-256 `9b0506f5…19427b7c`，与归档前完全一致，证明交付语义未改变。**未正式打包、未覆盖 r54。**

> 2026-07-31（五十九，温室花妖核心与妖精小怪图片补齐）：按所有者授权使用内置 ImageGen 生成温室花妖核心同设计三阶段 cut-in，完好／轻损／重损分别接入 `portrait-flower-core-s0-v1.png`、`s1`、`s2`；生成蓝色小 P／金色大 P 两类妖精及各两帧翅膀的 2×2 sheet，色键维护稿保留为 `fairy-sheet-v1-chroma.png`，透明运行时稿缩为 `fairy-sheet-v1.png`（128×128、10,042 bytes）。manifest、构建复制与 data URL、宿主 dataset、atlas source、花妖战损选择和妖精动画渲染均已接线；品红维护稿不进入 dist，几何妖精与抽象 cut-in 卡仅保留为加载失败 fallback，fallback 文案已由“素材待换”改为“立绘载入失败”。专项素材清单、美化规划、弹幕交接与项目总览已同步，弹幕必需图片素材全部补齐；剩余素材欠账为真音效，可选主题背景贴图不阻塞。`check:ui`、`npm test`（161/161）、`build:ui`、`git diff --check` 均通过，本地预览页面及透明妖精资源返回 HTTP 200。**未正式打包、未覆盖 r54；真实 SillyTavern 的花妖三阶段、妖精动画、宿主缩放与移动端显示仍待验收。**

> 2026-07-31（五十八，首发八人角色人设全部完成扩写）：此前暂停的米斯蒂娅·萝蕾拉与十六夜咲夜人设线已恢复并完成，`src/lorebook/characters/mystia.xml` 由 17 行简版扩展为 136 行／19,845 bytes，`src/lorebook/characters/sakuya.xml` 由 17 行简版扩展为 168 行／24,421 bytes；至此第一版八名固定角色均已有完整独立人设。两次扩写均采用“所有者参考 + THBWiki 一设资料／官方设定文档 + 萌娘百科汇编”的分层写法：米斯蒂娅补齐夜雀食堂老板娘、料理／演唱强项、识字与账目弱项、禽类料理立场、迷惑歌声／夜盲／飞行和撤离型战斗逻辑；咲夜补齐完美潇洒与天然脱线的稳定共存、对蕾米莉亚的主动忠诚与温和玩心、时间／空间操作、飞刀／体术／料理、压力递进及“没有弱点但并非无敌”的能力边界。`142 cm`／`168 cm`、统一衣装与瞳色演出、短指甲、红瞳时停表现等所有者指定内容均明确作为本卡采用设定，不冒充原作唯一结论。两人仍各自占用原有唯一绿灯世界书条目，路由、MVU、事件与结算链未改，世界书总数保持 16。两轮均通过 XML 开闭标签／乱码检查、`check:ui`、`npm test`（161/161）、`build:ui` 与 r54 dry-run；最后一次 dry-run 为 `194,082,625` bytes、SHA-256 `ce6df091…d14c51c0`。**未正式打包、未覆盖 r54、未导入真实 SillyTavern；八名扩写人设仍需在真实对话中统一复核语气密度、关系递进、能力尺度和提示预算。**

> 2026-07-30（五十七，连续新手指引与离线教程修复）：修复离线 Preview Bridge 错把 `reimu_boundary_inspection` 预置为完成、导致灵梦“检查结界”入口消失的问题；预览的真实教程行动现在也通过项目既有 `localSettlementAction → stageLocalSession → applyLocalSettlement` 链推进，不再只生成假对话而不改状态。运行壳新增按当前正式教程步骤和当前 UI 场景派生的浮动引导：庭园地图高亮目标，打开目标菜单后高亮剧情按钮，设施确认页高亮确认行动，进入 GAL 后指引点击对白框读完固定剧情；完整覆盖灵梦结界检查、主屋维修、温室魔力线索、第二点灵感以及后续既有 13 步教程。每个聊天可用“跳过指引”隐藏引导，高亮偏好按 chat ID 保存于本地 `localStorage`，不写 MVU、不伪造教程完成，剧情任务仍须正常推进。**未正式打包、未覆盖 r54；真实 SillyTavern 中的画布高亮、窄屏遮挡、聊天切换偏好隔离和整条模型剧情仍待验收。**

> 2026-07-30（五十六，继承序章前置）：开场不再在身份确认后立即把庭园写成继承成功。新流程为“确认身份并打开遗信 → 发送带事务标记的真实开场消息 → 在开场页展示祖父遗信／旧木匣／移动庭园的聊天序章 → 玩家点击接过庭守钥 → bridge 在该序章 assistant 楼层写入并复读玩家资料、庭园名、苏醒的庭守钥与开场标志 → 开放庭园”。序章契约明确继承尚未完成、玩家尚未穿过结界，正文必须停在钥匙等待玩家选择的时刻；其他角色不得提前闯入，模型不得自行初始化 MVU。旧版带标记开场与隐藏的末级修复链继续兼容；按所有者要求，序章面板不再提供“显示原生聊天”或“重新讲述”按钮。离线 Preview Bridge 已补齐同样的两阶段状态，用于前端完整验收。**未正式打包、未覆盖 r54；真实 SillyTavern 的正文生成质量、额外变量阶段与刷新恢复仍待验收。**

> 2026-07-30（五十五，灵梦／魔理沙 BOSS 四状态素材升级）：所有者提供两张更优的 `1254×1254` 黑底四宫格，分别替换灵梦与魔理沙的待机／施法／受击／击破画面。扩展 `scripts/prepare-character-boss-sheets.py`，新增可重复 `--source CHARACTER_ID=PATH` 与归档版本参数，支持只更新指定角色；本轮仍使用确定性黑底 alpha 推导、RGB 反预乘和隐藏 RGB 清理，没有调用生成模型重绘。新版原始字节归档到 `旧素材/素材处理/battle-boss-owner-source-v2/`，旧版 v1 原始档保持不动；输入／输出 SHA-256、逐格包围盒和像素统计记录于 `project/character-boss-sheet-replacement-report-2026-07-30.json`，透明总览为 `project/character-boss-sheet-replacement-preview.png`。稳定运行路径仍为 `reimu-battle-sheet-v1.png`／`marisa-battle-sheet-v1.png`，避免改动 atlas 与宿主合同；manifest 新增 v2 当前档、v1 被替代档和报告指针，测试会校验报告哈希等于运行时字节。离线浏览器通过小店购买对战卡，分别开启灵梦、魔理沙极难战并立即暂停，确认新版人物、透明背景、尺寸与锚点均正确；证据为 `project/runtime-qa/boss-reimu-v2-paused.png`、`boss-marisa-v2-paused.png`，页面控制台 warning/error 为 0。完整门禁 `check:ui`、`npm test`（154/154）、`build:ui` 与 r54 dry-run 全绿；dry-run 为 `101,878,951` bytes、SHA-256 `a0605c9c…7586cf`。**未正式打包、未覆盖 r54；真实 SillyTavern 中的两角色四状态切换、宿主缩放与移动端显示仍待验收。**

> 2026-07-30（五十四，八角色独立 BOSS 四状态图集补齐）：所有者提供灵梦、魔理沙、荷取、米斯蒂娅、萃香五张 `1254×1254` 黑底四宫格战斗图，顺序均为待机／施法／受击／击破。新增 `scripts/prepare-character-boss-sheets.py` 以确定性黑底 alpha 推导、RGB 反预乘和隐藏 RGB 清理生成透明 PNG，没有调用生成模型重绘；所有者原始字节按角色归档到 `旧素材/素材处理/battle-boss-owner-source-v1/`，完整输入／输出 SHA-256、四格可见包围盒和像素统计记录于 `project/character-boss-sheet-preparation-report.json`。atlas、渲染器、app、宿主 dataset、构建内嵌和 manifest 已补齐五类 sheet；同时修正对战档案中爱丽丝／琪露诺／咲夜的旧 `boss_*` 视觉 ID，使八名角色均以统一角色 ID 解析到独立图集，不再在对战卡中误回退温室妖花。透明总览与五张浏览器实显截图分别保存于 `project/character-boss-sheet-alpha-preview.png` 和 `project/runtime-qa/boss-*-paused.png`；离线预览通过真实商店购买→背包对战卡→逐角色开战→立即暂停，确认灵梦、魔理沙、荷取、米斯蒂娅、萃香均显示对应人物且无黑底方块，控制台无 warning/error。完整门禁 `check:ui`、`npm test`（154/154）、`build:ui` 与 r54 dry-run 全绿；dry-run 为 `101,234,151` bytes、SHA-256 `e1f1ec11…b05bd88`。**未正式打包、未覆盖 r54；真实 SillyTavern 中的八角色逐场、受击／阶段切换／击破和移动端显示仍待验收。**

> 2026-07-30（五十三，三座设施共享废墟图接入）：所有者提供 `1536×1024` RGBA 废墟图并指定作为设施损坏形态，原始字节已归档至 `旧素材/素材处理/facility-ruin-shared-v3/facility-ruin-owner-source-v1.png`，SHA-256 为 `9a00186d…f0b5b1e7`。本轮没有调用生成模型重绘；`scripts/prepare-shared-facility-ruins.py` 以预乘 alpha 缩放、清除隐藏 RGB、裁去 alpha `<8` 的几乎不可见暗场，并分别正规化到妖精花园 `592×464`、月见温泉 `624×464`、宴会广场 `656×464` 的同组画布，完整输入／输出哈希记录于 `project/shared-facility-ruin-report.json`。manifest、构建链与地图 resolver 已接通 `damage_replacement_alpha`：三座设施仅在 `damaged` 时以完整废墟替换正常形态，`normal`／`abnormal` 不受影响；魔法温室没有误接废墟。地图合成预览已目视检查，三处废墟没有遮挡主屋、红桥或中央道路；真实 SillyTavern 中的损坏／修复切换、缩放与命中仍待验收。完整门禁 `check:ui`、`build:ui`、`npm test`（153/153）与 r54 dry-run 全绿；dry-run 为 `86,743,293` bytes、SHA-256 `5083dcf8…633afd68`。**未正式打包、未覆盖 r54、未提交或推送。**

> 2026-07-30（五十二，浏览器原生弹窗全面替换）：针对购买商品时跳出浏览器级确认框的问题，审计维护源后确认共有 9 处原生调用：商店 `confirm` 1 处、温泉／宴会 `prompt` 3 处、自定义异变 `prompt` 5 处。现已全部替换为项目内共享的单例 `<dialog>`：支持纯确认、单行／多行输入、默认值、必填和长度限制，取消按钮、Esc 与背景点击统一返回取消，关闭后焦点归还原触发控件；购买确认会显示商品名与实际金币价格。弹窗使用 `textContent` 写入动态内容，桌面宽度受限于 520px，`≤420px` 双按钮等宽回流、控件不小于 44px。新增契约会扫描 `app.ts`，禁止重新引入 `window/globalThis.alert/confirm/prompt`，并覆盖弹窗节点、取消、焦点归还、购买路径与窄屏 CSS。完整门禁 `check:ui`、`build:ui`、`npm test`（153/153）与 r54 dry-run 全绿；dry-run 为 `85,481,900` bytes、SHA-256 `da956157…282ca5ef`。离线内置浏览器的开场预览未稳定进入已解锁小店，因此没有把购买实点目视记为通过；仍须在真实 SillyTavern 复核 iframe 内购买确认、取消、Esc、手机键盘和焦点归还。**未正式打包、未覆盖 r54、未提交或推送。**

> 2026-07-30（五十一，机遇卡／对战卡阶段 C 完成）：两张卡已进入背包受控入口；机遇卡会直接执行本地稳定邀请，对战卡提供八人选择、当前杂鱼标签与锁定难度预览，并复用既有弹幕弹窗运行三档白名单配置。取消对战不消费；失败只在本地消费卡片并显示“杂鱼标签 +1”，不创建消息、不进入 GAL；可信胜利减一枚标签后才打开不可跳过的要求面板。胜利要求限制 1–240 字，提交后按 settlement ID 锁定，使用稳定真实消息事务生成“对手必须接受”的独立战后剧情；刷新可从真实 user 消息恢复，生成失败只重试对话，完整回复与变量阶段结束后本地清空 pending，绝不重复战斗结算。对手不要求在庭院，胜利投影只亮对应角色绿灯且明确禁止把出场写成 `GensokyoPresence`。离线浏览器已检查桌面与 `390×844` 背包、对手选择和零标签极难战斗入口，无页面控制台错误；一次闲置实测很快落败并正确只加本地标签，符合极难档高压预期。完整门禁 `check:ui`、`build:ui`、`npm test`（152/152）与 r54 dry-run 全绿；dry-run 为 `85,468,974` bytes、SHA-256 `3b99d20b…c7af9716`。**阶段 D 尚未开始：正式卡面／小鱼干素材待所有者提供，真实 SillyTavern 胜败分流、消息恢复与难度手感仍待验收；未正式打包、未覆盖 r54、未提交或推送。**

> 2026-07-30（五十，机遇卡／对战卡阶段 B 与零标签极难档完成）：按所有者新要求把对战难度扩为三档并完成 Bridge／战斗配置接入。0 枚杂鱼标签锁定 `character_duel_hard_v1`：2 命、2 Bomb、105ms 决死窗口、五阶段，以固定种子环、扇弹、自机狙、旋转环、预警激光、安全道和使魔多源叠压形成原作 Hard 风格高压，不使用随机瞬杀；1–2 枚锁定四阶段标准档，3 枚以上锁定三阶段援助档。`duel-profiles.json` 为八人登记三档配置 ID，`duel-configs.ts` 会校验弹型与参数白名单并按角色覆盖对手视觉／姓名。Host 与 Preview Bridge 已新增机遇卡写入复读，以及对战卡开始、取消、可信结算接口；开始前原子锁定目标／标签快照／难度／配置，取消不扣卡，结算复读成功才消费，回复生成或其他卡片事务进行中会拒绝并发写入。专项测试增至 10 项，完整门禁 `check:ui`、`build:ui`、`npm test`（151/151）与 r54 dry-run 全绿；dry-run 为 `85,440,021` bytes、SHA-256 `15b7abd8…d74cd1d`。**阶段 C 尚未开始：背包使用入口、对手选择、难度预览、战斗弹窗接线、杂鱼标签视觉和仅胜利开放的要求剧情仍待实现；未正式打包、未覆盖 r54、未提交或推送。**

> 2026-07-30（四十九，机遇卡／对战卡阶段 A 完成）：已按 `project/opportunity-duel-card-plan.md` 完成状态与纯规则层。`src/items/catalog.json`、`src/shop/catalog.json` 登记两张本地消耗品；`src/battle/duel-profiles.json` 为首发八人建立通用对战档案；MVU schema、初始状态、类型、字段台账和幂等迁移新增 `inventory.card_runtime`。机遇卡会以 `chat_id + use_id` 稳定抽取完整登记、未知且不在场的角色，成功原子到场后才消费，满员／无候选／事务占用均不消费。对战卡开始时锁定角色、配置和难度，取消不消费；有效结算才消费，失败只在本地增加一枚杂鱼标签且不创建胜利剧情，胜利减一枚并只建立待提交的胜利要求；三枚及以上锁定援助难度。新增 `card-item-rules.ts`、`duel-card-rules.ts` 与 8 项规则测试，完整门禁 `check:ui`、`build:ui`、`npm test`（149/149）及 r54 dry-run 全绿；dry-run 为 `85,400,820` bytes、SHA-256 `e61a996d…3bfe39`。**阶段 B 尚未开始：当前没有背包／商店 UI 使用入口、Bridge 持久写入、真实通用对战配置、战斗弹窗接线或胜利模型剧情；未正式打包、未覆盖 r54、未提交或推送。**

> 2026-07-30（四十八，机遇卡／对战卡／杂鱼标签进入规划；历史状态，人设暂停已由五十八条解除）：所有者决定暂停米斯蒂娅与咲夜人设线，新增两种本地托管消耗品。机遇卡稳定随机邀请一名已完整登记但尚未认识的角色进入庭院，全程不调用 LLM；对战卡允许选择任意完整登记角色进行本地符卡战，不要求已认识或当前在场。对战卡不奖励金币、不推进时段；`clean_win`／`narrow_win` 胜利时杂鱼标签减一并进入一次“对手必须接受玩家一个要求”的真实胜利剧情，`loss` 时只在本地增加一枚以小鱼干表现的“杂鱼标签”，不得创建消息楼层或调用任何模型。标签达到三枚后锁定援助难度，胜利减到两枚后下次恢复标准难度。完整字段、幂等事务、失败回滚、UI、模型投影、测试矩阵和四阶段施工顺序见 `project/opportunity-duel-card-plan.md`。本条与计划只更新文档，**尚未修改功能源码、未构建、未打包**；两张卡与杂鱼标签正式素材由所有者后续提供。

> 2026-07-30（四十七，etama3 素材授权澄清）：所有者确认 `etama3.png` 是其此前使用 AI 生成并修改的项目素材，允许随项目打包分发。已撤销原错误的用途限制、构建阻断和相关文档提示；素材继续沿用既有稳定文件名与内部键，避免扰动运行时资源链。正式打包仍须遵守项目通用的“必须由所有者明确授权且不得覆盖历史检查点”规则。

> 2026-07-30（四十六，本轮角色人设收束与下次更新目标；历史状态，后续完成情况见五十八条）：本轮角色人设扩写先在此收束。八名固定角色仍各自对应一个独立绿灯世界书条目，连同八条基础世界书总数保持 16；人设文件内部的小节扩充不会额外增加世界书条目。已完成本轮扩写的六人及当时文件大小为：博丽灵梦 `9,588` bytes、雾雨魔理沙 `14,225` bytes、琪露诺 `17,103` bytes、爱丽丝·玛格特洛依德 `20,390` bytes、伊吹萃香 `18,635` bytes、河城荷取 `19,856` bytes。米斯蒂娅·萝蕾拉（当时 `1,597` bytes）与十六夜咲夜（当时 `1,752` bytes）在该检查点暂时保留简版，并列为后续人设更新目标；计划继续采用“所有者参考 + 官方设定资料 + THBWiki／萌娘百科交叉核对 + 防脸谱化行为规则 + 能力边界”的写法，并保持唯一绿灯精确触发，不拆出新增世界书条目。本轮最后一次已记录的完整离线门禁为 `check:ui`、`npm test`（141/141）、`build:ui` 与 r54 dry-run 全绿；dry-run 为 `85,383,013` bytes、SHA-256 `e9c266d9…e58fd80`。**该条仅保留历史检查点；两名简版角色已于五十八条完成扩写。**

> 2026-07-30（四十五，V3 人偶／河童温室标注扩边修复通过）：所有者重新提供准确圈选图后，确认第三十六条的封闭孔洞修复没有覆盖真正缺失的外缘区域，该条关于 `512×416` 成品与对应旧哈希的结论已被本条取代。`旧素材/素材处理/extract_facility_differences.py` 现将四张温室源图统一置于扩展 ROI，并只在所有者标注的恢复区内合并新增候选像素：人偶维护型补回顶部屋脊、右侧人偶区和底部构件；河童自动化型补回顶部管道、左侧水轮／设备、右侧河童／设备区和底部基座；原有 ROI 内的稳定遮罩继续复用，未把扩区变成无约束整图抠取。四张温室正规化画布同步扩为 `608×528`，manifest 的 `width_ratio` 调整为 `0.291`、`render_center` 调整为 `[0.19, 0.274]`，保持地图像素密度和落位。人偶／河童正式 PNG 的 SHA-256 分别为 `5cdd7c48…e396b2`、`5b12d408…b0956`；所有者已明确回复“通过”。`check:ui`、`npm test`（141/141）、`build:ui`、r54 dry-run、桌面与 `390×844` 离线地图预览均通过，浏览器控制台无警告／错误；临时候选目录和诊断图已删除。修复采用确定性源图提取与标注恢复区，没有用生成模型重绘像素。**未正式打包、未覆盖 r54；真实 SillyTavern 运行时验收及三张 V3 损坏覆盖层仍待后续执行。**

> 2026-07-30（四十四，河城荷取人设、水能力与工程商业逻辑扩写）：在所有者未提供私设参考的前提下，以《东方风神录》《东方地灵殿》设定文档、《求闻口授》荷取条目、人妖名鉴、THBWiki 原作资料及萌娘百科外观汇编为主要来源，将 `nitori.xml` 从简略条目扩充至 19,856 bytes。新增“身份与社会”“怕生与虚张声势”“技术思维”“能力与行动逻辑”“背包与工作台”“商业与交易”“压力递进”“与人类”“与雾雨魔理沙”“对玩家——关系递进”“二次解释与防脸谱化”等模块：以独处见生人先逃、无路可退时摆出傲慢姿态、同伴圈内活泼及进入技术／交易话题后迅速流畅构成稳定性格，不写成单一社恐或奸商。操纵水明确为河童天生能力，与光学迷彩、机械臂、推进、鱼雷／导弹和工程制作分层；加入水源、压力、规模、弹药、燃料、承重、库存、仪器校准及迷彩故障限制。物理装置是强项，魔法／咒术／灵魂／结界设备为相对短板，未知能源允许研究失败和承认不会，禁止万能科学家、四次元背包及科技自动压过幻想乡规则。河童市场、摊位、报价、研究经费与商机敏感保留为商业动机，但不简化为诈骗或见钱眼开。与魔理沙的地底核能调查关系写为风险代理、技术支援与利益交换，不自动恋爱或固定后勤。庭园自动化方案继续独立于爱丽丝路线，只登记候选与工程判断，不偷做永久选型；仪器禁止读取隐藏剧情、玩家内心、未连接地点和高权限宿主接口。XML 外壳开闭各一、无替换字符；角色绿灯、MVU 与结算链未改。`check:ui`、`npm test`（141/141）、`build:ui` 与 r54 dry-run 全绿，最新 dry-run 为 `85,383,013` bytes、SHA-256 `e9c266d9…e58fd80`、世界书仍为 16 条。**未正式打包、未导入；怕生强度、推销频率、报价透明度、水能力尺度及工程术语密度仍待所有者审阅和真实运行验收。**

> 2026-07-30（四十三，爱丽丝人设、人偶操纵与有限自主意识深化）：根据所有者提供的种族、职业、身高、外观、关系演绎及人偶能力资料，并交叉核对《求闻史纪》爱丽丝条目、《妖妖梦》《永夜抄》《萃梦想》等设定文档、人妖名鉴、THBWiki 原作资料及萌娘百科汇编，将 `alice.xml` 从简略条目扩充至 20,390 bytes。新增“身份与职业”“外冷内热的行为依据”“独处与社交”“能力与行动逻辑”“本卡深化——有限自主意识人偶”“制作与研究习惯”“压力递进”“与雾雨魔理沙”“对玩家——关系递进”“面对直球告白——本卡关系演绎”“二次解释与防脸谱化”等模块：以收留迷路者、祭典人偶表演、安静留宿及实际维护行为托住外冷内热，不以“哼／才不是／笨蛋”复读替代傲娇；初识、熟悉、信任与亲密可能只改变距离、交付和坦率程度，不把人格做成关系数值开关。162 cm 与所有者给定衣装固定为本卡外观。人偶能力拆为魔法线、多线操纵、亲手制作、生活劳动、层级命令、战斗布置与自爆人偶，并保留注意力负荷、本体近身弱点、材料／装填／安全距离与均衡属性限制。原作通常强调常规人偶仍由爱丽丝操纵且未普遍获生；所有者提供的“少数人偶已有意识”明确作为本卡深化，仅允许少数难复制试作体具有限自主、短时脱离即时命令，仍依赖魔力且不等于完全自律技术毕业。直球告白只在关系基础充分时触发脸红、停顿和操纵失误，不强制接受、辱骂或暴力反应。XML 外壳开闭各一、无替换字符；角色绿灯、MVU 与结算链未改。`check:ui`、`npm test`（141/141）、`build:ui` 与 r54 dry-run 全绿，最新 dry-run 为 `85,364,682` bytes、SHA-256 `4fcc69aa…e6221b3`、世界书仍为 16 条。**未正式打包、未导入；陌生期距离感、关系递进速度、自主人偶人格强度、傲娇复读频率与告白反应仍待所有者审阅和真实运行验收。**

> 2026-07-30（四十二，伊吹萃香人设与密疏能力联动优化）：根据所有者提供的性格、身高、外观、鬼族体能、锁链与伊吹瓢资料，并交叉核对《东方萃梦想》设定文档、《求闻史纪》萃香条目、THBWiki 原作资料／考据及萌娘百科汇编，将 `suika.xml` 从简略条目扩充至 18,635 bytes。新增“角、锁链与几何饰物”“漫长岁月与孩子气”“对孤独的表现”“能力与行动逻辑”“力量事迹与尺度”“能力边界”“伊吹瓢”“饮酒表现”“压力递进”“二次解释与防脸谱化”等模块：以恢复坦率聚集与人与鬼旧日信任解释宴会倾向，将怕孤独写成召集、观察和延续联系而非黏人苦情；操纵密疏拆为物质聚集、雾化、增密发热、分身、巨大化与大型人心聚集表现，并以“重排已有事物而非凭空创造力量”为总边界，禁止聚集真相／好感／忠诚、分散矛盾或借文字游戏自动解题。鬼族怪力、投掷、近战、锁链吸灵与灼热酒气均转为可演战斗逻辑；碎月表现不误写为摧毁真实月球，毁山豪言不作为日常地形删除按钮。141 cm 与完整服装按所有者指定固定为本卡外观。伊吹瓢保留持续产酒、单次容量制动、鬼用烈酒与武器用途；《醉蝶华》中幻觉／记忆异常明确与奥野田美宵及醉魔情节条件相连，不归纳为葫芦酒的永久固有效果。XML 外壳开闭各一、无替换字符；角色绿灯、MVU 与结算链未改。`check:ui`、`npm test`（141/141）、`build:ui` 与 r54 dry-run 全绿，最新 dry-run 为 `85,167,538` bytes、SHA-256 `898ffa07…ad1044e`、世界书仍为 16 条。**未正式打包、未导入；密疏能力强度、宴会主动频率、醉意清醒度及孤独感隐蔽程度仍待所有者审阅和真实运行验收。**

> 2026-07-30（四十一，琪露诺人设与能力资料联动优化）：根据所有者提供的性格、外观、能力、大妖精关系与算术约束，并交叉核对《求闻史纪》琪露诺条目、THBWiki 一设资料／官方出版物索引及萌娘百科外观汇编，将 `cirno.xml` 从简略条目扩充至 17,103 bytes。新增“存在方式”“本卡采用外观”“最强的内在逻辑”“主要行为倾向”“能力与行动逻辑”“能力边界”“认知方式”“压力递进”“与大妖精——本卡关系设定”“二次解释与防脸谱化”等模块：明确其在妖精中偏强而非幻想乡总体强者，以被看见和保有独特位置解释“最强”自称；冷气、瞬冻、冰塑形、弹幕冻结、高热弱点与妖精复归均加入体积、环境、专注、自然根基和真实消亡风险，禁止绝对不死、无限冻结与抽象概念冰封。不到一米、绿蝴蝶结、黑色玛丽珍鞋按所有者指定固定为本卡外观，以容纳历代立绘差异；“听大妖精的话／大妖精哭时不知所措”明确为本卡关系深化，不伪装成资料已完全证实的原作关系。所有者指定的“只稳定掌握九以内日常心算”仅以低权重认知边界出现，并明确不得主动出题、复读数字梗、作为高频语料或否定原作所表现的阅读、计数、观察及危险判断。XML 外壳开闭各一、无替换字符；角色绿灯、MVU 与结算链未改。`check:ui`、`npm test`（141/141）、`build:ui` 与 r54 dry-run 全绿，最新 dry-run 为 `85,150,406` bytes、SHA-256 `907144b7…78af5d0`、世界书仍为 16 条。**未正式打包、未导入；“最强”复读频率、算术约束隐蔽度、大妖精关系强度及能力代价仍待所有者审阅和真实运行验收。**

> 2026-07-30（四十，魔理沙人设与能力资料联动优化）：根据所有者提供的外观、性格、灵梦关系及能力参考，并交叉核对官方出版物相关资料、THBWiki 一设资料与萌娘百科汇编，将 `marisa.xml` 从简略条目扩充至 14,225 bytes。新增“本卡采用外观”“外貌与携行”“主要行为倾向”“能力与行动逻辑”“能力边界”“压力递进”“与博丽灵梦”“二次解释与防脸谱化”等模块：明确黑白衣装、金发侧辫、宽檐巫师帽、扫帚与迷你八卦炉；以光、热、星及高火力魔法为优势，并用魔法森林采集、材料处理、反复实验和工具依赖托住华丽表现；将多系术式、力量感知、速度／力量与体术短板转为可演行为，同时禁止全知复制、无限魔力、环境无视与一炮自动解题。与灵梦确认为亲密挚友兼独立竞争者，不自动恋爱或互为从属；“借书／收藏”保留为边界冲突，不写成无差别盗窃，也不替越界永久免责。准确年龄不作定论，所有者给定的细节作为本卡采用外观，以容纳作品间设计差异。XML 外壳开闭各一、无替换字符；角色绿灯、MVU 与结算链未改。`check:ui`、`npm test`（141/141）、`build:ui` 与 r54 dry-run 全绿，最新 dry-run 为 `85,134,912` bytes、SHA-256 `7b2b72e9…0633193e`、世界书仍为 16 条。**未正式打包、未导入；人物强度、说话节奏及与灵梦的默契表现仍待所有者审阅和真实运行验收。**

> 2026-07-30（三十九，灵梦能力资料与人设联动优化）：根据所有者提供的能力参考，并交叉核对 THBWiki 一设资料、分析考据及《求闻史纪》资料索引，将 `reimu.xml` 补充至 9,588 bytes。新增“能力与行动逻辑”七项：飞翔于空中的能力本质与高上限、非日常绝对免疫护栏、博丽巫女的灵力／御札／阴阳玉／结界技法、空间间隙与循环空间表现、与八云紫“操纵境界”的区别、操纵灵气、异常幸运与直觉，以及这些能力如何造成缺乏危机感和不受强者气势压迫的行为倾向。百科中的道教表现只保留为技法外观，不虚构门派师承；骰子命中、间隙追踪等表现不升级为全知、万能传送或自动胜利。二次解释同步增加飞翔、幸运与空间能力的纠偏。XML 外壳开闭各一，绿灯与 MVU 不变；`check:ui`、`npm test`（141/141）、`build:ui` 与 r54 dry-run 全绿，最新 dry-run 为 `85,122,375` bytes、SHA-256 `ed765c60…6601ffc8`、世界书仍为 16 条。**未正式打包、未导入；能力强度与实际世界书表现仍待所有者审阅及真实运行验收。**

> 2026-07-30（三十八，灵梦人设扩写样板）：按 `character-persona-writer` 的“事实—驱动—情境行为—代价—二次解释”方法，将 `src/lorebook/characters/reimu.xml` 从 1,765 bytes 扩写为 6,616 bytes；保留原作核心与现有庭园职责，新增庭园定位、性格底色、四项主要行为倾向、日常判断、四级压力递进、基于实际行为而非好感数值的玩家关系变化、魔理沙关系、语言节奏及五项常见误读纠偏。没有新增原作履历、MVU 字段、触发规则或固定示范台词，也没有把灵梦改成常驻监管者、任务发布器或万能直觉来源。XML 外壳开闭各一且绿灯路由不变；`check:ui`、`npm test`（141/141）、`build:ui` 与 r54 dry-run 全绿，最新 dry-run 为 `85,119,392` bytes、SHA-256 `c0ee638c…f955bdee`、世界书仍为 16 条。**本轮未正式打包、未导入；其余七名角色尚未扩写，建议先由所有者确认灵梦样板的深度与语气，再沿同一结构继续。**

> 2026-07-30（三十七，角色人设绿灯路由优化）：角色世界书已从自然姓名／别名关键词触发改为唯一保留标记 `GSK_CHAR_<ID>_ACTIVE` 触发；`src/lorebook/character-routing.json` 成为八名角色 ID、显示名与绿灯的共同事实源。所有庭园剧情请求在 `withGardenNarrativeContract` 汇合点按正式在场、当前会话、当前活动事件、活动中的设施会话／宴会、装修代码选角、场景道具目标及固定事件登记参与者生成绿灯；玩家输入中大小写不同的保留标记会先被清除再按可信状态重建。角色世界书条目同时启用 `exclude_recursion` 与 `prevent_recursion`，姓名、别名、历史记录或其他档案内部提及不再互相诱发。未新增 MVU 字段，绿灯不代表角色到场或关系事实。新增 3 项定向契约测试；`check:ui`、`npm test`（141/141）、`build:ui` 与 r54 dry-run 全绿，dry-run 为 `85,114,505` bytes、SHA-256 `a6043163…70d958a7`、世界书仍为 16 条。**本轮未正式打包、未导入；SillyTavern 1.18.0 + Tavern Helper 4.8.19 中的实际世界书激活、递归扫描与多人同时亮灯仍须真实运行验收。**

> 2026-07-30（三十六，V3 人偶／河童温室缺块修补）：所有者指出刚提取的 `人偶维护型温室` 与 `河童自动化型温室` 在上方、下方和左方存在小块缺失。复核原始全景差分、透明抠图和 `extract_facility_differences.py` 后，确认是若干与空底图色差过低的封闭小区域被 GrabCut 误判为背景，并非原图缺件。提取脚本现仅对 `人偶温室.png`／`河童温室.png` 启用有面积上限的封闭孔洞修复：不与 ROI 外缘相通且面积不超过各自阈值的小背景分量才会回填，其他 11 张素材路径完全不受该规则影响；试跑逐文件 SHA-256 证明另外 11 张抠图字节不变。正式 `抠图结果`、透明总览、抠图报告、13 张正规化 V3 产物和准备报告已重建；人偶／河童正式资产仍保持 `512×416` 同组画布、24px 安全边及原有锚点，SHA-256 分别为 `7034f105…c4c786`、`e0452c7f…a1aa14`。`check:ui`、`npm test`（138/138）、`build:ui` 与 r54 dry-run 全绿；新 dry-run 为 `85,110,125` bytes、SHA-256 `c80be087…f85ca6d`。**未正式打包；三张 V3 损坏层及真实 SillyTavern 验收仍按第三十五条计划继续**。

> 2026-07-30（三十五，下一窗口执行规划）：当前 V3 庭园接入停在“13 张正常形态已完成、3 张损坏覆盖层待补、真实 SillyTavern 待验收”的干净阶段边界；本条只更新文档，**没有生成新素材、没有改运行时代码、没有构建或打包**。下一窗口按以下顺序执行：① 先读 `project/README.md`、本条、`project/v3-map-facility-asset-checklist.md` 与当前 `git status`，保留工作树中既有地图／UI／归档改动；② 使用 `imagegen` 工作流，以三组 V3 正常形态和旧 V2 损坏层仅作视觉参考，制作妖精花园／月见温泉／宴会广场三张共享损坏覆盖层，禁止直接缩放或复用 V2 成品；③ 将覆盖层正规化到各组现行画布 `592×464`／`624×464`／`656×464`，保持 RGBA、透明像素隐藏 RGB 为零、至少 16px 透明安全边，只留下裂纹、碎片、焦痕、断栏等变化像素，并分别与同组全部三种正常形态合成目视；④ 所有者视觉确认后再把 `damage_overlay_alpha` 写入 `src/assets/asset-manifest.json`，同步更新清单状态、构建契约和 `tests/ui-contract.test.mjs` 的精确路径／同画布／损坏态选择断言；⑤ 依次运行 `npm run check:ui`、`npm test`、`npm run build:ui`、`npm run package:checkpoint:dry`，仅做 dry-run，禁止正式打包或覆盖 r54；⑥ 离线实看桌面和 `390×844` 的正常／损坏切换、命中、菜单跟随和控制台后，再使用 `sillytavern-runtime-debug` 在真实 SillyTavern 复核 iframe 资源注入、设施损坏／修复切换、拖拽缩放、320px／短视口、重挂载与聊天切换。两张主屋状态层不属于本轮必做，必须由所有者另行确认后再开工；真实宿主验收通过前不得把 manifest 状态写成 runtime validated。完整素材细则与逐步验收标准见 `project/v3-map-facility-asset-checklist.md` 的“下一窗口执行计划”。

> 2026-07-30（三十四，V3 十三形态设施接入候选）：将所有者提供的 13 张 V3 全景差分经 `旧素材/素材处理/extract_facility_differences.py` 抠图后，再由新增 `scripts/prepare-v3-facility-assets.py` 按设施组做 alpha 并集裁切、至少 24px 透明安全边、16px 画布对齐与隐藏 RGB 清零，正式产物进入四个 `src/assets/world/map-facilities/*/` 目录；同组画布分别为温室 `512×416`、花园 `592×464`、温泉 `624×464`、宴会场 `656×464`。manifest 已启用 4 组 V3 形态并登记新 `width_ratio/render_center/ground_anchor/label_anchor/hit_polygon`；不匹配的三张 V2 损坏层明确退出，V3 损坏层仍待补。`build-ui.mjs` 新增 RGBA、512–768px、同组画布、16px 透明边与透明像素隐藏 RGB 门禁，契约测试同步改为 V3 正向断言。`check:ui`、`npm test`（138/138）、`build:ui` 与 r54 dry-run 全绿；dry-run 为 `85,111,045` bytes，SHA-256 `51add895…9c71d77`。离线浏览器已实看桌面全建设施与 `390×844` 横向寻址、温室命中及手机底部菜单，运行时注入 4 组／13 形态，控制台无警告／错误。**未正式打包；三张 V3 损坏层、所有者最终视觉复核及真实 SillyTavern 生命周期／触控验收仍待执行**。

> 2026-07-30（三十三，桌面／手机人物比例回调，所有者验收通过）：针对 V3 横向庭园中人物在桌面与手机均偏大的反馈，将全局人物视觉基准由 `0.73` 调为 `0.64`，并把手机额外倍率从 ≤360px 的 `1.35`／361–520px 的 `1.25` 回调为 `1.18`／`1.12`；现行有效比例为桌面 `0.64`、普通手机约 `0.72`、窄手机约 `0.76`。标签、选中光环与多人间距同步收紧；命中半径改为从实际 sprite 尺寸派生，并保留约 `22 CSS px` 半径的手机下限，避免视觉缩小后难以点选。角色源图、四方向待机变换、移动序列及琪露诺移动帧亮度均未改动。`check:ui`、`npm test`（138/138）与 `build:ui` 全绿，人物缩放／待机对齐／琪露诺亮度定向测试通过；所有者已确认桌面与手机效果合适并验收通过。本轮未打包；R21 旧倍率只作历史记录，不得恢复为当前基线。

> 2026-07-29（三十二，手机上下软边界修正）：所有者指出手机版上下边界异常后，确认原因是 V3 横图在手机 `1×` cover 下纵向合法平移范围为 `0`，但旧实现仍分配了约 `47px` 的通用软越界额度，导致一拉就明显露边。现新增按轴计算的 `resolveAxisOverscrollLimit`：有实际行程的横轴保持原果冻反馈，无行程轴限制为约 `16 CSS px`；Canvas 清空后先绘制森林绿到草地绿的边缘渐变，短暂越界不再闪透明／黑边。`check:ui`、`npm test`（138/138）与 `build:ui` 全绿；本地 `390×844` 同幅度纵向拖拽峰值从约 `47.18px` 降至 `15.73px`，`0.5s` 回到约 `0`，横向边界保持 `457.49px`，控制台无警告／错误。未打包；真实 SillyTavern 的顶部安全区、浏览器地址栏收缩和 pointercancel 仍待手机实机复核。
> 2026-07-29（三十一，地图果冻软边界）：`GardenMap` 相机现在根据画布、底图 cover 尺寸和当前缩放动态计算合法平移范围，地图缩放下限由 `0.8` 收紧为 `1`，不会再因缩得比视口小而直接露底。拖拽越界不作硬裁切，而是进入有限的指数阻力区；松手、缩放或尺寸变化后由 `stiffness=180`、`damping=18` 的阻尼弹簧回到合法范围。该状态只存在于 UI 相机内，不写 MVU；`prefers-reduced-motion` 下立即归位。新增纯函数契约覆盖 cover 尺寸、边界和橡皮筋阻力，离线门禁为 `check:ui`、`npm test`（138/138）、`build:ui` 全绿。`390×844` 浏览器实测：横向边界 `457.49px`，越界峰值约 `502.72px`，约 `0.9s` 回到图内；纵向边界 `0`，软拉伸约 `47.18px` 后回到 `0`，控制台无警告／错误。未打包；真实 SillyTavern 内高刷新率触控、宿主缩放与手势取消仍待实机复核。
> 2026-07-29（三十，V3 横向庭园底图）：所有者提供的 `1672×941` 春日主屋庭院已以 `garden-base-owner-v3.png` 接入 manifest、离线预览与内嵌构建，SHA-256 为 `b5d89e347fa448ecf82690a43a5639649d44956d544e4295a72ec08c62926c13`。主屋交互锚点迁移到中央建筑前阶，中央庭院及四处设施地块按新构图重新布点。由于旧 V2 设施素材的透视和归一化几何不匹配，魔法温室／妖精花园／月见温泉／宴会广场的旧贴图暂设为 `map_usage=false`，源文件和状态契约仍保留，运行时先回退为空地标记，未改设施业务状态或 MVU。素材需求已写入 `project/v3-map-facility-asset-checklist.md`：必需 13 张形态图 + 3 张损坏层，另建议 2 张主屋维修状态覆盖层。`check:ui`、`npm test`（137/137）与 `build:ui` 全绿；本地 `907×894`、`390×844` 实看构图稳定且控制台无警告／错误。未打包，真实 SillyTavern 的拖拽边界、移动端横向寻址和后续设施命中仍待新素材接入后验收。
> 2026-07-29（二十九，手机人物视觉尺寸）：针对手机端地图即使放到最大仍觉得人物偏小的问题，在 `GardenMap` 绘制层增加仅由画布 CSS 宽度决定的视觉倍率：`≤360px` 为 `1.35`、`361–520px` 为 `1.25`、其余为 `1.00`。人物精灵、命中半径、选中光环和活动标签偏移使用同一倍率；多人站位间距只温和扩大并封顶 `1.18`，避免八人拥挤测试散得过远。没有改变地图世界缩放、设施尺寸、角色状态或 MVU 数据。`check:ui`、`npm test`（137/137）与 `build:ui` 全绿；本地浏览器已实看 `320×720`（1.35）、`390×844`（1.25）及 `1200×900`（1.00），八人同场仍可辨认，控制台无警告／错误。未打包；真实 SillyTavern 内连续移动人物的触控命中仍需实机复核。
> 2026-07-29（二十八，手机目标操作抽屉）：针对手机端点击人物／设施后透明气泡互相遮挡、按钮被视口底边裁切的问题，仅调整目标菜单表现与焦点生命周期。`#gg-target-menu` 在 `≤700px` 改为 iframe 视口固定的底部操作抽屉：人物操作使用双列快捷卡（`≤360px` 单列），设施操作使用单列列表；标题、当前状态、禁用原因和 44px 关闭按钮分层呈现，操作区支持内部滚动、`overscroll-behavior` 与底部安全区。桌面端锚点半环布局、`TargetAction` 注册、地图命中、bridge 与结算均未改变；关闭按钮和 Escape 会恢复地图焦点。`check:ui`、`npm test`（136/136）与 `build:ui` 全绿；本地 390×844 实测设施五项操作全部落在视口内，菜单底边精确贴合 iframe 视口且无横向溢出。未打包；真实 SillyTavern 的 320px 类名改写、触控命中与焦点返回仍待复核。

> 2026-07-29（二十七，etama3 敌弹素材、HUD 与手机操作收口；授权描述已于四十七条修正）：将所有者提供的 `etama3.png` 以 `battle-bullets-etama3-local-v1.png` 接入敌弹绘制链，覆盖圆弹、米弹、鳞弹、针弹、玉弹等已登记 shape×hue，未知组合仍回退几何绘制。该素材现已确认由所有者使用 AI 生成并修改，允许随项目打包分发。庭园顶部状态文字由过小调整为桌面约 `15px`／手机 `14px` 粗体，随后按反馈压缩标签、标题与顶栏留白；实测桌面顶栏约 `67px`、手机约 `164px`，均无横向溢出。弹幕手机输入改为主手指实际拖动超过轻触阈值后自动连射，松手／取消即停；轻触不射击，第二指继续负责专注，双击继续触发 Bomb，鼠标拖动仍不射击。`check:ui`、`npm test`（136/136）与 `build:ui` 全绿；本地 `390×844` 弹幕演练画布约 `350×467`，dialog 无横向／纵向溢出，控制台无错误。**未打包；真实 SillyTavern 的多点触控、宿主缩放与生命周期仍待验收**。

> 2026-07-29（二十六，符卡副本选关绘卷）：按所有者反馈，将原有纵向朴素按钮列表重构为东方幻想乡风格的三卷弹幕绘卷。妖精／森林／结界副本分别使用冰雾、魔法森林、境界主题色，并明确展示卷次、难度、地点、Boss、阶段数、时长和弹幕标签；「正式挑战」与「弹幕演练」改为主次分明的语义按钮，保留原解锁条件与结算逻辑。桌面端三栏并排，窄屏回落单列滚动；所有动态内容通过 DOM API 和 `textContent` 安全构造。`check:ui`、`npm test`（136/136）与 `build:ui` 全绿；本地浏览器已实看默认窄屏与 `1200×900` 桌面布局，控制台无警告／错误。未打包，真实 SillyTavern 的宿主焦点、类名改写与触控滚动仍待验收。

> 2026-07-29（二十五，地图设施悬停反馈）：内置区域新增固定中文显示名，状态缺少 `area.name` 时不再把 `fairy_garden_plot`、`moon_spring_plot` 等内部 ID 暴露给玩家。四座已建设施的悬停／选中反馈改为按透明 PNG 的 alpha 边缘绘制暖金色呼吸光；登记 `hit_polygon` 继续负责精确命中，但不再作为已建设施的可见脚底光圈，未建空地仍保留多边形／地面提示。`check:ui`、`npm test`（135/135）与 `build:ui` 全绿；本地浏览器以“设施全建成”快进确认“妖精花园 · 启用”中文标签与花圃边缘光正常，控制台无警告／错误。未打包，真实 SillyTavern 仍待验收。

> 2026-07-29（二十四，主屋地图去重）：按所有者反馈，停用旧主屋损坏／修复透明贴图的运行时地图叠加，避免它与 `garden-base-owner-candidate-v2.png` 左上角已经绘制完成的庭院重复并产生画风突兀。素材仍保留为历史维护源，`areas.main_house` 状态、检查／维修行动和事件结算不变；共享地图锚点迁至左上庭院前场 `(0.16, 0.15)`，同时用于区域点击、角色落脚和菜单跟随。`check:ui`、`npm test`（134/134）与 `build:ui` 全绿；本地浏览器已确认旧叠图消失，点击新锚点仍打开“旧主屋 · 损坏”的检查／维修菜单。未打包，真实 SillyTavern 宿主缩放与窄屏仍待验收。

> 2026-07-29（二十三，大型功能入口面板）：按所有者反馈，将顶栏中被压缩的符卡副本／灵梦小店／背包三张图片入口收拢为单一「幻想乡案内」按钮；点击后打开原生单例 dialog，大面板内展示三张大尺寸玩法卡与开放庭园／全屏／设置次级操作。三张源图仍走既有预览和自包含 data URL 链，但面板内明确改为 `image-rendering: auto`，不再参与地图像素素材与浏览器缩放补偿链。所有入口关闭面板后调用原有商店、背包、副本和机会页函数，不新增状态源或业务分支。桌面实测三卡约 `268×250`，`390×844` 下为 `347×138` 横向大卡，`320×720` 无横向溢出；大按钮进入灵梦小店的现有路径验证通过，控制台无警告／错误。`check:ui`、`npm test`（134/134）和 `build:ui` 全绿。规划见 `project/large-entry-panel-plan.md`。**未打包，未做真实 SillyTavern 的类名改写、重挂载、缩放与焦点返回验收**。

> 2026-07-29（二十二，旧素材归档）：完成全项目图片与制作产物引用审计。将 569 个素材／元数据文件（约 279.71MB）及 7 个仅服务旧试作的脚本，按原相对路径移动到 `旧素材/`；主要包括地图生成候选与重复副本、26 张已被 v2 取代的设施 v1 图、取消的四张角色菜单图、Alice/Cirno/Reimu/Sakuya 旧动画试作、Marisa 与旧 sequence 的 QA 预览、未登记樱花地标候选。当前 asset manifest 的 117 个素材／描述路径全部存在；approved 序列、`最终版` 原始输入、测试使用的 `sequence-v1`、运行时 fallback、chroma 维护源与历史 dist 均未移动。归档说明见 `旧素材/README.md`。归档前后 `check:ui`、`npm test`（134/134）、`build:ui` 全绿；checkpoint dry-run 字节数与 SHA-256 保持 `115260989` / `1a2bbf07…45c82`，证明当前交付语义未改变。未正式打包、未实机验收。

> 2026-07-29（二十一，三副本 Boss 战斗图接入）：所有者提供琪露诺、爱丽丝、咲夜三张 `1254×1254` 四状态战斗图。原图为 RGB 黑底；按 imagegen 技能流程生成平坦洋红色键控底，再以保守硬阈值派生透明 PNG，避免通用软蒙版误伤肤色、金发和银发。三套透明图及 chroma 维护源已归档到 `src/assets/battle/boss/`，asset manifest 登记为 `owner-provided-integrated-pending-runtime-validation`。atlas 新增三类 boss sheet 与四宫格裁切，渲染器按 `presentation.boss_id` 选择琪露诺／爱丽丝／咲夜，四格依次用于待机、施法、受击、击破，加载失败仍回退温室妖花。构建器、宿主 dataset 与 app atlas source 链已补齐。`check:ui`、`npm test`（134/134）和 `build:ui` 全绿；本地浏览器已实跑妖精练习至琪露诺到场，尺寸、锚点、透明背景正常且无控制台警告／错误。**爱丽丝／咲夜尚未逐场目视，三张源图放大时可见轻微洋红边缘，实际战斗缩放下不明显；未打包，未做真实 SillyTavern 验收**。

> 2026-07-29（二十，地图全形态接入收口）：所有者对九种补充形态、三张损坏覆盖层及四组叠图回复“全过”。新底图 `garden-base-owner-candidate-v2.png`、四座设施全部正式 `-v2` 形态和三张覆盖层已登记到 asset manifest，并通过 `project/manifest.json`、构建器和 `GardenMap` 进入离线预览／自包含运行时。四座设施使用独立归一化渲染中心、落脚点、标签点和命中多边形；旧主屋保留旧回退位置，底图背景房屋不转成交互对象。设施命中优先级改为角色 → 精确设施多边形 → 旧区域圆形，避免中央庭院抢点击。宽屏 `1440×1000` 与窄屏 `390×844` 已做离线视觉检查，`check:ui`、`npm test`（134/134）和 `build:ui` 全绿。**未打包、未覆盖 `0.2.0-r54`，真实 SillyTavern 的点击、拖放／缩放菜单跟随、损坏层和宿主窄屏仍待验收**。

> 三个异常关闭任务的可执行续接清单统一见 `project/interrupted-work-recovery-2026-07-28.md`；后续 Agent 应先按工作线阅读对应章节，再查看当前未提交差异。

> 2026-07-29（十九，GAL 背景与姓名牌收尾）：所有者提供的 `ChatGPT Image 2026年7月29日 01_10_32.png` 已按原字节归档为 `src/assets/ui/gensokyo-gal-shrine-background-v1.png`，SHA-256 `b1b998f50219824af4340e70285097534c3355e3ebd98bd813ed1b2d0745df44`。素材登记在 `asset-manifest.json/ui_assets.gal_shrine_background`；`build-ui.mjs` 为离线预览复制文件并为内嵌构建生成 data URL，宿主再通过 `data-gal-background-src` 传入 iframe。GAL 舞台改用该图，旧 CSS 月亮和雾气／底部条纹层已禁用。姓名不显示的根因是 `.gg-scene-speaker` 原先位于带 `clip-path` 的对白框外侧而被裁掉；现已增加对白框顶部内边距并把姓名牌移到框内（桌面 `top:.72rem`，窄屏 `.65rem`）。`check:ui` 与 `build:ui` 通过，源与 dist 背景哈希一致；`npm test` 为 132/133，唯一失败是地图契约仍硬编码旧底图文件名，而 manifest 已切至 `garden-base-owner-candidate-v2.png`，与本轮 GAL 变更无关。**未打包检查点，未做真实 SillyTavern 验收**。

> 2026-07-29（十八，交互与验收工具连续优化）：角色／设施目标菜单缩小，并在渲染前过滤与关闭按钮重复的 `close/leave` 动作；设施“查看”模式隐藏图片且清空 `src`，其他设施行动仍保留视觉图。背包改为独立道具袋视图；开放庭园页面从正式状态派生教程完成数、当前步骤和下一步；测试快进扩成分组控制面板，覆盖七个教程断点、M1/M2、道具恢复和八名角色单独／全员入园及清空在场。浏览器缩放新增角色与菜单视觉补偿，地图滚轮缩放仍维持独立锚点；小店／背包／符卡三入口放大下移，旧外层 `gg-header` 黑边移除但内部 HUD 保留。琪露诺待机帧按方向做亮度校正，GAL 使用道具控件改为御札式选择槽。以上均只有维护源、契约测试和离线构建证据；实机应重点检查 100%／125%／200% 缩放、320px、移动中菜单跟随、键盘焦点、重挂载与真实资源注入。

> 2026-07-28（十七，角色菜单视觉定稿）：按所有者新决定，角色点击菜单不再采用四张徽章图片。已从 `scripts/build-ui.mjs`、宿主 `data-*` 和 `app.ts` 移除 `target-action-*.png` 的复制、base64 内嵌与运行时读取；原 PNG 仅保留为本地历史参考，不删除。`createBubbleButton()` 现在只创建受控文本符号节点，CSS 以御守圆章、顶部结绳、虚线内圈、纸札和四套语义配色绘制对话／摸头／任务／离开图案；按钮实际行动名、可见禁用原因、桌面半环与窄屏网格保持不变。契约测试已改为断言构建与宿主不得再出现徽章图片资源链。`check:ui`、`npm test`（125/125）和 `build:ui` 全绿；本地浏览器已实看角色菜单与区域菜单，无控制台警告／错误。**未正式打包，仍需真实 SillyTavern 复查动态类名样式、移动锚点、键盘焦点与 reduced-motion**。

> 2026-07-28（十六，角色菜单徽章续接）：四张 `target-action-{talk,leave,pat-head,quest}-v1.png` 已接入 `scripts/build-ui.mjs` 的离线复制与自包含 data URL 链，并由宿主注入 iframe `data-*`。`src/ui/app.ts` 仅从既有 `TargetAction` 派生 `talk / leave / pat-head / quest` 纯视觉类别：`close/leave` 为离开，`pat_head` 为摸头，登记事件／设施／战斗入口为任务，其余为普通对话；未修改动作注册表、剧情条件、角色移动、bridge 或结算。原不透明图通过 CSS 柔边徽章容器显示；桌面半环半径随 3–6 个按钮扩展，窄屏 390px 双列、320px 单列，触控目标不小于 44px；任务徽章下仍保留“检查结界／观察旧地基”等真实标签，禁用原因增加可见文本。新增资源链、语义与响应式契约测试；`check:ui`、`npm test`（125/125）和 `build:ui` 全绿。本地浏览器已检查桌面、390px、320px，无控制台警告／错误；**尚未正式打包，仍需真实 SillyTavern 复查类名改写、移动中锚点跟随、键盘焦点与 reduced-motion**。

> 2026-07-28（十五，异常任务恢复）：已读取 UI 优化任务 `019fa84a-b6ab-7eb2-a85b-00afcb9ffdb5`。灵梦小店入口／商店 4:3 底图与十槽界面、魔理沙背包入口均已完成并通过此前本地预览；符卡副本入口 `reimu-dungeon-button-v1.png` 已接入预览与内嵌资源链，本地曾验证为约 `87×72px` 且点击仍打开原副本框。该任务中断在临时色键文件清理后的最终收尾之前；恢复后重新执行 `check:ui`、`npm test`（124/124）和 `build:ui`，当前全绿，未发现符卡按钮专用色键临时文件残留。另有四张角色点击菜单素材 `target-action-{talk,leave,pat-head,quest}-v1.png` 已归档，但对应改造只完成方案：桌面半环大徽章、窄屏徽章网格、任务徽章保留具体剧情名；**尚未修改 DOM/CSS，也未改 `TargetAction`、剧情条件或结算逻辑**。

> 2026-07-28（十四，异常任务恢复）：已读取地图优化任务 `019fa8d9-05bc-7173-b17a-fd5212f30226`。在所有者提供的 `1448×1086` 新底图候选上，已生成基础魔法温室、四季花境、露天月见汤、灯火夜市四张 `1254×1254` RGBA 风格样板并完成透明边缘、色键残留、入口朝向与确定性叠图检查；所有者回复“看上去不错，开始下一步”，故风格校准门视为通过。中断发生在“下一步”刚开始、尚未继续生成其余形态时。样板仍位于 `src/assets/world/map-facilities/new-map-style-v2/`，没有登记 `asset-manifest.json`、没有替换现运行时素材；后续应以这四张为风格／占地锚，补齐其余形态与三张损坏覆盖层，再统一校准落脚点、缩放、命中多边形与标签锚点。

> 2026-07-28（十三，异常任务恢复）：已读取像素移动任务 `019fa8e5-2662-7af0-804f-f151564f5a80`。二维随机巡游、强制休息、turnaround 四视图静态待机、待机／移动视觉尺寸对齐，以及单段距离增至 `0.034–0.080` 均已完成并已写入下方（九）至（十二），没有遗失的动画代码步骤。该任务最后被中断的实际请求是角色点击菜单视觉改造：四张 `1024×1024` 不透明图分别映射对话、离开、摸头和任务，计划以柔边大徽章接入，任务类继续显示“检查结界／观察旧地基”等具体标签；当时尚未产生文件改动。不要误把这项 UI 方案记作已完成的动画工作。

> 2026-07-28（十二）：二维随机巡游的单次移动已再次加长，避免角色频繁“动一下、停一下”。共享距离从初版 `0.012–0.035` 提升到 `0.034–0.080`；八名角色水平活动半径扩大到 `0.090–0.110`，步行／浮游垂直半径分别扩大到 `0.065/0.075`。速度保持不变，因此一次移动通常持续约 `2–5s`，抵达后的转向、收步和强制休息规则也保持不变；目标继续受区域锚点周围椭圆边界约束。新增配置合同测试。`check:ui`、`npm test`（124/124）、`build:ui`、r54 dry-run 全绿；dry-run 107,582,225 bytes，SHA-256 `beecd738…a266`。**未正式打包，仍需真实 SillyTavern 观察多角色同时长程巡游时的区域边界、遮挡和点击体验**。

> 2026-07-28（十一）：修复 turnaround 待机图与移动动画切换时人物忽大忽小。根因是两套素材虽绘制到同一目标方框，但 turnaround 单格为 `627×627`、正式动作帧单格为 `209×209`，透明留白比例不同。现已按八名角色四个朝向的透明主体包围盒实测 `scale/x/y`，在 `SpriteActor` 的本体与轮廓发光绘制中共用变换，使待机视图的可见高度、水平中心和脚底线与对应动作首帧对齐；原 PNG 未修改，仍保持最近邻绘制。新增变换合同测试。`check:ui`、`npm test`（123/123）、`build:ui`、r54 dry-run 全绿；dry-run 104,472,990 bytes，SHA-256 `4a949821…8a75`。本地预览已观察灵梦、魔理沙多次移动／休息切换，尺寸与落脚点稳定且无控制台错误；**未正式打包，仍需真实 SillyTavern 验收全部角色和窄屏缩放**。

> 2026-07-28（十）：像素角色的停止表现已改为复用现有四视图 turnaround 静态待机图。`SpriteActor` 在休息、转向预备、收步和 reduced motion 状态优先按 `front/back/left/right` 读取 `2×2` turnaround 对应格，只有静态图加载失败时才回退动作序列或 V2 的当前方向首帧；移动期间仍播放已验收动作序列。因此强制休息不再停在走路姿势上，也没有重新引入待机切帧、呼吸或上下浮动。新增契约测试覆盖四方向格位、非动画标记及加载失败回退。`check:ui`、`npm test`（122/122）、`build:ui`、r54 dry-run 全绿；dry-run 104,470,573 bytes，SHA-256 `92347b1d…966a`。**未正式打包，仍需真实 SillyTavern 观察移动收步到 turnaround 站姿的视觉衔接**。

> 2026-07-28（九）：庭园 NPC 巡游由固定横向往返升级为受限二维随机行动。`SpriteActor` 现在在区域锚点周围的椭圆范围内按单轴随机选择 `front/back/left/right`，通过 `rest → turn → travel → settle` 状态推进；每次抵达后保持当前朝向，收步并强制休息 `760–2200ms`，不再立刻反向。普通 MVU 刷新只更新权威朝向，不覆盖正在进行的本地巡游；离场、换区和 reduced motion 会回到区域锚点并清除旧目标。`GardenMap` 已接入 `offsetX/offsetY`，点击热区、轮廓光晕和气泡锚点继续读取实际绘制位置。新增确定性测试覆盖四方向、单轴目标、椭圆边界、强制休息和刷新朝向保护。`check:ui`、`npm test`（121/121）、`build:ui`、r54 dry-run 全绿；dry-run 104,470,212 bytes，SHA-256 `02ad1417…87c2`。本地预览已观察到前后与水平移动及抵达停顿；**未正式打包，仍需真实 SillyTavern 验收长时间随机分布、移动中点击跟随、后台恢复、窄屏与 reduced motion**。

> 2026-07-28（八）：地图设施第一阶段优化完成。修复三座可换型设施英文素材键与中文 `current_form` 不匹配的问题；主屋和魔法温室启用地图图层；五座设施全部登记显式 `area_id`，主屋按区域状态选损坏／修复图，温室四种正式形态当前共用已验收 operational 图，三座 M2 设施保留损坏覆盖层。构建器会对重复素材源去重，避免 Windows 并发复制文件锁；离线预览现由 asset manifest 注入新版空底图和设施图层，不再误用旧建筑底图。另补齐稀疏离线状态的设施验收快进初始化。类型检查、`npm test`（117/117）、`build:ui`、r54 dry-run 全绿；dry-run 100,043,565 bytes，SHA-256 `7da6554c…6727e`。本地浏览器已检查初始损坏主屋及五设施全建成状态，宽／窄视口均无控制台错误；**尚未正式打包，仍需真实 SillyTavern 校准锚点、比例和命中边界**。

> 2026-07-28（七）：按所有者要求取消全部运行时待机动画。七名 `sequence-approved-v1` 角色停止时固定显示当前方向第 001 帧；魔理沙 V2 停止时固定对应方向站姿；不再切换待机帧，也不做程序化呼吸、缩放或上下浮动。asset manifest 已移除 `dedicated-idle`／`procedural-idle` 待办，并统一已验收角色状态。离线门禁 `check:ui`、`npm test`（116/116）、`build:ui`、r54 dry-run 全绿；dry-run 为 84,548,905 bytes，SHA-256 `ab6bb4cf…74e55d`。审视同时确认：扩大庭园底图已替换；三座可换型设施的素材键为英文而运行态 `current_form` 为中文，当前贴图无法选中；主屋与魔法温室素材尚未接入地图且锚点命名不兼容；r54 输出位已占用，正式打包前须修复地图映射、选择新检查点并完成 SillyTavern 实机验收。本轮未正式打包、未删除素材。

> 2026-07-28（六）：所有者提供并确认 `F:/agent airp/卡/参考素材/New project 11` 为七名角色的完整验收项目，已完成正式接入。604 张验收独立帧按原字节复制到 `sequence-approved-v1/source/`，以角色级统一最近邻缩放和固定锚点生成 `209×209` 帧及 `N×4` 图集；Cirno 使用四方向独立源锚点，Suika 保留 `y≈313` 锚点及已修正的背面正序。运行时速度采用验收 GIF 实值：Alice 90、Cirno 100、Mystia 80、Nitori 90、Reimu 110、Sakuya 100、Suika 100ms。`SpriteActor`、registry、asset manifest、构建器和宿主已支持可变长验收序列优先加载；失败时回退旧 V2/旧图集，魔理沙继续 V2 r2。旧 `sequence-v1` 候选未覆盖。新增可 dry-run 的导入脚本和逐源哈希/图集逐格一致性门禁；`check:ui`、`npm test`（115/115）、`build:ui`、r54 dry-run 全绿，dry-run 预测自包含 JSON 为 84,549,297 bytes。尚未正式打包，也未在真实 SillyTavern 做地图尺度、方向切换与光晕锚点验收。

> 2026-07-28（五）：所有者已选择“最终版”序列统一按 `90ms` 预览，并要求修正前后方向脚部误删、灵梦绿幕残留、萃香道具内部白块与腿间白底。蒙版管线已升级为 `connected-edge-plus-interior-islands-v2`：放宽主体附近小组件保留以保护脚尖/鞋跟；绿幕强清理；灰白背景只清理至少 48 个原始像素的内部同色岛；黑底禁用全局内抠以保护黑色轮廓。期间补入荷取 `001`–`022`，当前七名角色共 151 张原始合帧，已重建 604 个透明方向帧、7 张 `N×4` 图集、7 份 manifest 与 7 个 `90ms` 总览 GIF；旧 `110/130ms` 预览已清理。透明棋盘底与高反差洋红底接触表复核未再发现上述问题，咲夜白色发丝高光也得到保护。离线门禁 `check:ui`、`npm test`（112/112）、`build:ui`、r54 dry-run 全绿。**状态仍为 `generated-pending-owner-review`，未登记 asset-manifest / registry、未接入运行时、未正式打包**；下一步只做所有者 90ms GIF 美术复核，通过后再决定 P1 接入。

> 2026-07-28（四）：六名角色“最终版”可变长序列 P0 已完成，统一状态 `generated-pending-owner-review`。只读处理 129 张 `640×640` 四方向合帧，生成 516 个透明方向帧、6 张 `N×4` 候选图集、6 份可追溯 manifest 与 `90/110/130ms` 共 18 个真实来源总览 GIF；原件未覆盖。构建脚本为 `scripts/build-variable-character-sequences.mjs`，预览脚本为 `scripts/export-variable-character-sequence-gifs.py`，资产门禁为 `tests/character-sequence-assets.test.mjs`。自动 QA：可见高度 `138–150px`、统一底线 `y=179`、四角透明、原件 SHA-256 一致；离线门禁 `check:ui`、`npm test`（112/112）、`build:ui`、r54 dry-run 全绿。**尚未登记 asset-manifest / registry、未接入运行时、未正式打包**；下一步必须先由所有者查看三档 GIF 并逐角色确认透明边缘、循环和速度，通过后才开始 P1 爱丽丝垂直切片。

> 2026-07-28（三）：咲夜四向动作参考序列第二方案 `sequence-imagegen-v2`（逐图调用内置 imagegen，以当前帧锁动作、`sakuya-turnaround-v1.png` 锁身份）同样被所有者判定**验收不通过**，工作已立即停止。项目仅保留首张 `raw/001.png` 与失败说明；随后 `002–005` 的并行调用在完成前被中止，任何默认生成目录中的临时结果均未收进项目。统一状态为 `owner-rejected-imagegen-sequence-v2`；不得继续补齐 23 帧、不得登记或接入，也不得拿本轮 `001` 作为后续身份锚点。此前 v1 换色方案同样维持拒绝状态。

> 2026-07-28（二）：咲夜四向动作参考序列 v1 已生成 23 张 `720×720` 四向帧、Aseprite 母档与 `130ms` GIF，但**所有者验收不通过**。该批产物仅保留在 `src/assets/characters/sakuya/sequence-reference-v1/` 作为失败试作与方法对照，状态为 `owner-rejected-reference-sequence-v1`；不得登记 `asset-manifest`、不得接入 sprite registry 或运行时，也不得作为咲夜 V2 后续动作基线。后续返修必须重新确认失败点后另起版本，不能覆盖本批留档。

> 2026-07-28：独立设施地图素材第一轮已收束。现有五座地图对象均有独立透明素材：主屋（受损／修缮后）、魔法温室（已建成）、妖精花园／月见温泉／宴会广场（各三形态 + `damaged` 覆盖层）。后面三座的绘制链已接入维护源：构建器从 `asset-manifest.json.map_facility_assets` 嵌入透明图，宿主传入 iframe，`GardenMap` 仅在设施已建成且 `current_form` 有映射时绘制，并在 `facility_runtime.status=damaged` 时叠覆盖层；未知／未建成状态仍回退空地标记。离线门禁 `check:ui`、`npm test`（110/110）与 `build:ui` 均通过，所有者已验收视觉和离线接入；**尚未完成真实 SillyTavern 的锚点、缩放、命中边界与各状态截图验收**。R56 花见回廊、R57 缘侧书斋、R58 祈愿分社仍是暂名/暂定锚点的规划项，未生成素材。

> 2026-07-27（八）：琪露诺 `9×4` 指导版图集已获所有者验收；所有演示 GIF 的帧时长已按反馈从约 `95–100ms` 下调为 `130ms`。`pixel-character-animation-v2-plan.md` 的水平/垂直默认播放速度同步调整为 `130ms`（允许 `120–140ms`）。该素材仍未接入运行时。下一角色按登记顺序开始爱丽丝，先核对既有四向基准与步行动作，再走同一关键姿势→Aseprite→GIF 分组验收流程。

> 2026-07-27（七）：所有者裁定琪露诺 V2 **保持旧 `9×4 / 32 格` 合同**；前后 8 帧指导仍是已验收的动作参考，但向上/向下交付各砍回 `00 + 4`，固定取样 `F1/F3/F5/F7`。因而已开始的 `10×4 / 40 格` V3 图集与总览试作废弃，不能接入。`pixel-character-animation-v2-plan.md` §3.3 已改为“前后 8 帧指导的 V2 四帧适配”；下一步是按旧槽位完成 9×4 候选图集并导出真实总览 GIF 再验收。

> 2026-07-27（六）：所有者已验收琪露诺指导版的向下/向上 8 帧步行。母档为 `src/assets/characters/cirno/cirno-walk-guided-{down,up}-v1-work.aseprite`，演示为 `cirno-walk-guided-{down,up}-v1-demo.gif`；逐帧源位于 `guided-{down,up}-v1-frames/`。前后指导的文字版与配图已归档 `project/animation-guides/front-back-8-frame-walk-cycle-guide.{txt,png}`，并被 `pixel-character-animation-v2-plan.md` §3.3 设为后续角色前后移动的强制基线。琪露诺左右、向下、向上四个 8 帧动作组都已通过；尚未组装 `9×4` 总图集、未登记 registry/asset-manifest、未接入运行时。下一步是组装图集与总览 GIF 后再验收。

> 2026-07-27（五）：所有者已验收琪露诺的指导版水平 8 帧步行。左行母档 `src/assets/characters/cirno/cirno-walk-guided-left-v2-work.aseprite` 与 GIF `cirno-walk-guided-left-v2-demo.gif` 已归档；右行依所有者授权由同一 8 帧水平翻向，母档/演示为 `cirno-walk-guided-right-v2-work.aseprite`、`cirno-walk-guided-right-v2-demo.gif`。指导原件已入库 `project/animation-guides/8-frame-walk-cycle-guide.{docx,png}`，并已写入 `pixel-character-animation-v2-plan.md` §3.2 作为后续角色横向动画的强制步态基线。该批准只覆盖琪露诺左右行走；上下动作、完整 V2 图集、registry/asset-manifest/运行时接入仍未处理，下一步从上下动作开始。

> 2026-07-27（四）：所有者提供扩大视角的无设施庭园底图，已以 `src/assets/maps/garden-base-expanded-empty-v1.png` 接入维护源（1536×1024，SHA-256 `19a88b71…fd52fd`），由 `asset-manifest.json` 的 `maps.garden_base` 驱动构建，不再硬编码旧 `garden-base-spring-v1.png`。地图人物显示缩至旧比例 73%，设施占位光环缩至 76%；旧底图手描建筑轮廓已停用，后续设施必须使用独立透明贴图及登记 hit polygon/透明边界，不能复用旧描点。现有区域锚点与交互保留；设施贴图尚未接入。离线门禁：check:ui 通过、npm test 109/109、build:ui 通过、r54 dry-run 通过（37,420,011 字节，SHA-256 `b9124180…0e3cae`）；**未正式打包、未实机验收**。

> 2026-07-27（三）：魔理沙像素动画 V2 r2 已接入维护源。运行时图集 `src/assets/characters/marisa/marisa-animation-v2-r2.png`（`9×4` / `209×209`，由 `v2-hover-keyframes` 四方向 low/high 最近邻对齐合成悬浮循环）；母档 `marisa-animation-v2-r2-work.aseprite`；构建脚本 `scripts/build-marisa-v2-r2.mjs`。registry / asset-manifest / UI 契约测试已登记；旧 `riding-turnaround-v3 + hover-cycle-v1` 保留回退。离线门禁：check:ui 通过、npm test 108/108、build:ui 通过。**未打包、未实机验收**。欠账：Aseprite 手绘 in-between 精修、二维路径、其余六名角色 V2。记录见 `project/pixel-character-animation-v2-plan.md` §10。

> 2026-07-27（二）：所有者对设施扩展计划三项拍板：①R55 底座泛化确认执行；②契约修订已授权并写入 `contract.md`（「八名固定角色」改为首发名单+登记接入制、新增「新设施不得携带后置主线/全局前置/跨设施门票」红线，大妖精禁令保留）；③新角色素材**占位先行**——不再依赖灵梦 V2 试点验收，占位图集规格见计划 §4.4（主题色剪影、V2 版式 9×4、`status: placeholder` 登记、真素材原位替换零链路改动），真素材与动画后补按 r49 流程逐项勾销。R55（泛化）→R56 花见回廊→R57 缘侧书斋→R58 祈愿分社→R59 妖梦→R60+ 帕秋莉/早苗可依序开工；排期与验收欠账的取舍仍开放（计划 §9.4，默认 R55 先行）。本轮仅改文档（计划/契约/本文件），未改代码、未打包。

> 2026-07-27：新增规划文档 `project/r55-r60-facility-character-expansion-plan.md`——沙盒设施扩展与新角色引入线（纯规划，未施工、未改代码、未打包）。所有者方向确认：首批 3 座温泉式无主线沙盒设施（花见回廊/缘侧书斋/祈愿分社，暂名），用于承接后续新角色（推荐妖梦/帕秋莉/早苗，走「静水观测池引出咲夜」的装修 roll 初遇模式）并吸引人设相符的老角色；设施先行、角色二期。施工前置：R55 设施底座泛化（约 7 处三设施硬编码 + `moon_spring_session` 通用化，见计划 §5）。

> 2026-07-26（深夜）：所有者授权打包 —— `0.2.0-r54` 前端美化测试包已正式生成。产物 `dist/checkpoint-0.2.0-r54/幻想乡物语-测试检查点-0.2.0-r54.json`，SHA-256 `ee8587e7e67c832ac7d175c0eb3b58e625e4afc0640de67388fc246d3257ac73`，36,181,652 字节，UI 脚本 `gensokyo-garden-ui-020-r54`，16 条世界书。打包前门禁：check:ui 通过、npm test 108/108、build:ui 通过、dry-run 通过。r53 及更早 dist 均未覆盖。**r54 仍是离线候选，实机验收未执行**。项目总览导航见 `project/README.md`。

> 2026-07-26（晚）：前端美化专项 R1–R7 已在维护源（当前检查点线 `0.2.0-r53`）完成：设计 token 体系、符卡框语言、圆点→半环绕气泡菜单（含视角跟随）、开场页三轮重构（结界祭夜 + 所有者主视觉 base64 嵌入 + 全屏 + 移除魔法阵）、全阶段全屏（含启动首帧修复）、角色轮廓染色发光、区域底图手描轮廓发光、顶栏角色主题按钮、副本页弹幕夜空。离线门禁 106/106 全绿；**未打包、未实机验收**。方向与阶段见 `project/ui-beautification-plan.md`，逐轮施工与待办见 `project/ui-beautification-log.md`。r53 dist 目录已被占用，打包测试需先把 `package.json` 与 `project/manifest.json` 升到未占用的 r54 并重跑 dry-run。战斗/副本命名空间未越界（仅命名空间内底纹与金边）。

> 2026-07-26：R48 运行事务与调度热修复包已生成。产物为 `dist/checkpoint-0.2.0-r48/幻想乡物语-测试检查点-0.2.0-r48.json`，SHA-256 `04a2e920e9342104d170b50d5f4156b8575a72ce132f8830da23f8c704c95ea9`；R47 保留未覆盖。R48 修复最近设施换型封锁、副本时间推进后的宴会触发、异变卡代码原子启用、异变结算恢复、同步遮罩与会话记录。离线门为 81/81，等待所有者在 Luker 2.7.0 实机验收。

> 2026-07-25：R45 已按用户授权正式打包并进入所有者验收。产物为 `dist/checkpoint-0.2.0-r45/幻想乡物语-测试检查点-0.2.0-r45.json`，SHA-256 `70ce77350f66b89fb3b52eb460d5614e481f292b7b35e4a2590332aee56335c1`。设置页现有 9 个独立验收快进按钮；操作清单见 `project/r45-owner-acceptance-checklist.md`。离线门禁通过，真实 SillyTavern 验收仍待所有者完成。

## 当前交接点

- 2026-07-25 收尾修复已把此前仅存在于纯规则测试中的 M2 功能接入 app/bridge：异变启用与源头回执、每日调查/最终收束、三设施施工/换型/恢复、来访邀请与通知、温泉/宴会、场景道具成功后消费及收尾清理。
- 完整 `stat_data` 世界书条目现只进入 `[mvu_update]`；剧情请求由 UI 注入脱敏事实，普通剧情不再接收 `hidden_origin`。M2 本地根字段在每次 MVU 回复后恢复所有权并统一执行时间调度。
- 当前是已打包的离线验收候选，不是实机验收通过。最终门禁结果见 `project/r38-r45-implementation-log.md` 最新条目。

- 当前已验收运行基线仍为 `0.2.0-r32-extra-model-binding`；R32 的角色主世界书绑定、额外模型 `UpdateVariable` 路线与 MVU 写入已由所有者确认。
- 2026-07-25 所有者授权在维护源连续推进 M2（R38–R45）。维护源已实现开放庭园、背包、来访调度、自定义七日异变、三后续设施、场景道具与 R45 离线候选准备。
- **R37 真实 Luker 集中验收仍未执行**；M1 不得标记 complete。M2 也不得标记 complete。
- 最新离线门禁：`npm run check:ui` 通过、`npm test` 56/56、`npm run build:ui` 通过。
- R45 dry-run（未正式写入 dist 成品授权包）：检查点 `0.2.0-r45`，SHA-256 `c7c5d497136fe122d6c71c3746cbe02a9c7938940eba53d999fb7526cc42cfc4`，约 `30,141,017` 字节，UI 脚本 `gensokyo-garden-ui-020-r45`，16 条世界书。正式打包仍需所有者明确授权。
- 历史 R37 正式包仍保留于 `../dist/checkpoint-0.2.0-r37/`；不得覆盖。R34 成品继续冻结。
- 施工日志：`project/r38-r45-implementation-log.md`。

## R31 内容与 R32 基础设施修复

- 已在源代码实现 `0.2.0-r31-marisa-free-growth`：温室菜单的“整理自由生长方案”是单回合 `progression_fixed` 事件；仅在妖花核心已结算、基础温室仍存在且没有其他主要事件时出现。
- 本地白名单结算只登记 `自由生长型温室` 到 `unlocked_forms`，并记录魔理沙合作事实；不施工、不改 `current_form`、不扣资源、不推进时间、不激活异变。
- 夜晚、魔理沙在场时显示“夜间观察”自由支线；它不带受控事件标记，可多轮交流，不能解锁方案或主线。
- 已生成独立 R31 候选：`../dist/checkpoint-0.2.0-r31/幻想乡物语-测试检查点-0.2.0-r31.json`，SHA-256 为 `960249eef27a91252c694828699eea60329b6dcabae3e3553fcb9a4f267de419`，UI 脚本 ID 为 `gensokyo-garden-ui-020-r31`。
- 旧的同名 R31 预重组包未被覆盖，已按哈希归档为 `../dist/checkpoint-0.2.0-r31/superseded/幻想乡物语-测试检查点-0.2.0-r31.pre-extra-model.4952371b.json`。
- R31 自由生长型维护源已包含在 R32 运行包中；自由生长方案、额外模型变量更新路线、固定事件的魔理沙本地入场和草稿泄露下的 `GensokyoPresence` 回执解析均已通过所有者验收。夜间观察保持为无强制离场规则的自由支线。

## 本轮已完成

### 0.4 R33/R34 内容与事件架构收口

- R33 `alice_greenhouse_maintenance_proposal` 与自由支线 `alice_doll_workshop_chat` 已进入维护源；方案登记、爱丽丝入场、关系事实和会话 UID 均由本地链路拥有。
- R34 `nitori_greenhouse_automation_proposal` 与自由支线 `nitori_instrument_calibration_chat` 已进入维护源；荷取方案不依赖爱丽丝方案，可按玩家调查顺序独立登记。
- `src/ui/event-registry.ts` 现在严格校验事件类型、入口、轮次、投影键、允许结果和在场迁移；`allowed_results` 不再在结算器维护第二份副本。
- 新增 `src/ui/event-projection.ts`：只向模型注入当前事件的大纲、节拍、禁止偏离、允许结果及 `projection_keys` 指定状态切片。
- 打包器不再把整份 `greenhouse-vertical-slice.json` 作为通用温室关键词世界书条目；普通对话不能因此看到未来事件。
- 固定事件结算锁定请求前时间并由本地按规则推进；额外模型写入的倒退日期/时段会被拒绝。未知区域也不能让新角色登场或覆盖已有合法位置。
- 稳定约束已同步到 `contract.md`、运行时优化计划、R29～R37 与 R38～R45 路线图及 API 来源记录。

### 0.6 R35 三方案选型与换型（维护源完成）

- `select_greenhouse_form` 与 `remodel_greenhouse_form` 已进入 `greenhouse-upgrade-routes.json`；三个首次选择和三个换型入口分别通过 `action_results` 固定映射结果、提出者、形态、路线 effect 与固定结尾。
- 首次选型只在三项方案完成标记和三个 `unlocked_forms` 同时齐备时开放，扣除 4 物资并推进一个时段；后续只能换到已解锁且不同于当前的形态，扣除 3 物资并推进一个时段。
- `current_form` 仍是唯一当前路线事实源；换型只替换三种路线 effect，保留通用设施效果、全部方案解锁与关系事实，未新增 `current_route`。
- 新增 bridge 独占的 `events.settled_ids` 字段链；庭园行动写入稳定结算 ID，刷新、Swipe、重放和旧楼层扫描不会重复扣费或倒灌旧形态。
- 模型只看到本次 action 对应的提出者、形态、结果和固定结尾；`local_settlement` 的成本与 effect handler 不投影给模型。
- 离线门禁 42/42 通过，R35 dry-run SHA-256 为 `5c4dcbe257154c253e4e5688821ec8c532c5b853bbd1d6769bde13009eda7529`；没有正式打包、导入或真实 Luker 验收。

### 0.7 R36/R37 特殊道具与集中验收候选

- 灵梦小店新增本地目录商品：30 金币的可重复异变触发卡与 80 金币的唯一咲夜怀表；购买、持有、使用和复读均不调用 LLM。
- 异变卡以 `use_id` 从登记事件中确定性抽取，成功写入最多 3 条等待队列后才消费；刷新不重抽、队列满或无候选不消费。
- 咲夜怀表每天最多使用一次，只登记五分钟停顿与时间痕迹，不推进或回滚正式时段；第二次成功使用可登记咲夜调查候选，但不强制常驻，不创建紫或辉夜档案。
- 新增 `special-item-events.json` 并接入统一 loader、最小投影 allowlist、庭院行动入口与本地结算；独立异变支线不写关键完成态。
- R37 保留窄屏、200% 缩放所需的弹性布局、44px 触控尺寸、焦点可见、reduced motion、事务重试、卸载清理、数据库离线降级和道具复读校验。
- 当前 package/manifest 已指向 `0.2.0-r37-m1-release-candidate` 的 dry-run；正式运行基线仍是 R32，现有运行产物仍保持 R34，不得在真实验收前把 M1 标记为 complete。

### 0.5 额外模型变量解析重组（R32 已验收）

- 世界书已按 MagVarUpdate 路由拆成 `[mvu_plot]` 剧情条目、`[mvu_update]` 变量规则/输出格式，以及同时进入两阶段的 D0 最新状态快照。
- 最新状态使用 `{{format_message_variable::stat_data}}`，打包时写入 `extensions.position=4`、`depth=0`，不再依赖静态“投影契约”假装提供实时值。
- 变量阶段即使无变化也必须输出空 JSONPatch，额外模型只拥有开放语义字段；资源、商店、战斗、在场回执、受控主线和楼层幂等字段继续由本地 bridge 独占。
- bridge 不再在 `MESSAGE_RECEIVED` 时抢先结算；它等待 `VARIABLE_UPDATE_ENDED`，失败/禁用额外解析时使用 2.5 秒安全回退，并在同一 assistant 楼层的最新变量状态上合并本地结果。
- R32 已修正角色主世界书绑定：`extensions.world`、`extensions.mvu_worldbook_name` 与 `character_book.name` 使用同一名称；所有者已确认额外模型能够实际输出并更新变量。

### 0. R29 金币副本与 R30 灵梦小店

- R29 已实现并验收：妖花核心完成后解锁三种纯本地可重复副本；clean/narrow/loss 分别奖励 12/8/3 金币并推进一个时段，取消零结算，奖励 ID 幂等且不创建聊天楼层。
- R30 已实现并验收：`shop.unlocked` 与副本同步解锁；小店仅由 `src/shop/catalog.json` 和预写对话驱动，首发单份物资（6 金币/+1）与组合箱（22 金币/+4）。
- 购买为零 LLM 本地原子事务：余额、物资上限 20、未知商品和重复购买 ID 都会整笔拒绝；购买不推进时间、不创建聊天楼层。
- 设置页保留明确标记的验收快进：温室可用、妖花战后／副本解锁、小店测试状态（50 金币）。它们只写受控 MVU 快照。

### 1. 温室固定剧情收口

- `greenhouse_research_with_marisa` 最多两次有效 LLM 回合：首次行动为第 1 回合，玩家最多补充一次（120 字），第 2 次 assistant 回复自动结算并回庭院。
- 固定剧情的正文目标不超过约 300 个汉字；结束后关闭输入、选项和续聊，点击回庭院。
- 主线/固定行动继续使用本地白名单结算，取消依赖第二次预设解析，避免“第二次结算解析结果不符合 schema”。

### 2. 正文与 GAL 体验

- 载入新对话先清空 GAL 旧正文；生成完成后展开建议回复、手动输入、结束聊天与场景道具区。
- GAL 顶栏只保留“重新生成／停止”并保持单行；返回庭院由回复区“结束聊天／返回庭院”承担，历史与 Swipe 不再在顶栏暴露。
- 正文提取优先使用酒馆原生楼层中可读正文，过滤代码块、协议和边界外标签，适配不同预设的返回格式。

### 3. 在场角色快照同步（R28）

- 每次庭院 UI 发起的 LLM 请求都注入当前 `presence_snapshot`：在场角色 ID、姓名、区域、动作、朝向，及完整不在场名单。
- 角色抵达、离场或换区时，模型必须在正文后追加一次受控回执：

```xml
<GensokyoPresence>{"version":"presence.v1","present_character_ids":["reimu"],"character_views":{"reimu":{"area_id":"garden","action":"idle","facing":"front"}}}</GensokyoPresence>
```

- `bridge.ts` 只接受已登记角色与白名单字段；回执原子覆盖 `presence_snapshot`，离场角色的小人视图会一并移除。
- 没有位置变更时不输出该标签。叙事写了“离场”却没有回执，属于模型协议违例，不应由本地文本猜测器擅自改状态。
- 契约见 `presence-sync-contract.md`；回归测试在 `tests/ui-contract.test.mjs`。

## 关键文件

| 主题 | 文件 |
|---|---|
| 庭院界面与固定剧情收口 | `src/ui/app.ts` |
| 本地事件结算、两回合上限 | `src/ui/event-settlement.ts` |
| 模型请求、当前事件精确投影与在场契约 | `src/ui/target-actions.ts`、`src/ui/event-projection.ts` |
| 受控在场回执应用 | `src/ui/bridge.ts` |
| 事件 JSON 严格登记表 | `src/ui/event-registry.ts` |
| 时间单调规则 | `src/ui/time-rules.ts` |
| 正文投影与 GAL | `src/ui/gal-scene.ts` |
| 在场同步契约 | `project/presence-sync-contract.md` |
| R28 运行/导入报告 | `project/runtime-report-0.2.0-r28.md` |
| 角色卡打包器 | `scripts/package-checkpoint.mjs` |
| 前端美化方向与阶段 | `project/ui-beautification-plan.md` |
| 前端美化施工日志（R1–R25） | `project/ui-beautification-log.md` |
| 区域手描轮廓（换底图必须重描） | `src/ui/garden-spatial.ts` |
| 架构优化状态 | `project/runtime-architecture-optimization-plan.md` |
| M1 / M2 后续路线 | `project/r29-r37-m1-expansion-plan.md`、`project/r38-r45-m2-expansion-plan.md` |

## Luker 当前数据状态

- 已确认可访问本机 SillyTavern 服务，但本轮只读探测时没有可用于 O4 的选中角色和聊天上下文。
- 本轮没有导入、替换或清理任何角色卡、世界书、聊天及用户配置。
- 过往 R30～R32 的运行数据状态以对应运行报告和所有者环境为准；下一 Agent 不得根据 `dist/` 目录存在就推断已经完成真实导入验收。

## 下一阶段

- M1 计划：`project/r29-r37-m1-expansion-plan.md`；M2 计划与施工：`project/r38-r45-m2-expansion-plan.md`、`project/r38-r45-detailed-execution-plan.md`。
- 维护源已含 R38–R45 规则与离线测试；当前可选路径：
  1. 授权正式打包 `0.2.0-r45` 并做真实新聊天验收；
  2. 先补做 R37 集中真实验收（清单 `project/r37-acceptance-checklist.md`）；
  3. 继续打磨设施装修/异变启用的宿主 GAL 事务 UI。
- 独立检查点策略不变：不覆盖 R28/R32/R34/R37 等历史产物；无授权不正式打包；离线通过 ≠ 实机通过。
- 自定义异变：玩家填表 → 预留卡 → 隐藏源头一次生成并锁定 → 28 标准时段 → 每日短线索 → 最终收束；不可叠加、不可主动结束；怀表不缩短计时。
- 三后续设施：妖精花园 4 / 月见温泉 6 / 宴会广场 5 物资建成，换型统一 2；同时仅一个大型施工；形态 2/4 聊天或 12/24 时段兜底。
- 背包只显示本地目录；场景道具同场最多 3 种；修缮包只走损坏修复入口。

## 下一位 Agent 开工顺序

1. 不得覆盖任何既有 dist 检查点；需要新候选时用未占用版本。
2. 先读 `project/r38-r45-implementation-log.md` 最新收工条与本交接。
3. 若做实机验收：导入精确候选到新聊天，按 R37 或 R45 矩阵留证据；不得把 dry-run/预览写成 accepted。
4. 若续做 UI 深接：优先异变结构化表单、设施方案比较页、装修/修复完整消息事务，保持 rules 纯函数与 bridge 事务边界。
5. 任何模型输出仍不能直接完成资源、路线、UID、在场、异变生命周期或关键事件结算。

## 操作约束

- 不直接编辑 `dist/`；修改维护源后依次执行：`npm run check:ui`、`npm test`、`npm run build:ui`、`npm run package:checkpoint:dry`。
- Git 默认只提交维护源、测试和文档；`dist/` 下历史检查点、构建产物和 superseded 归档不进入源码提交，除非所有者对某个精确产物另行授权。
- 所有者已批准 R29～R37 采用逐轮独立检查点、打包、导入和真实 Luker 验收的交付方式；每轮执行写入前仍须核对精确版本、未占用输出路径和目标 Luker 会话，不得把该授权扩大为覆盖历史产物或清理无关数据。
- 打包器拒绝覆盖已有检查点；需要新候选时先更新 `package.json`、`project/manifest.json` 的检查点。
- 真实 Luker 验收使用右侧内置浏览器，不操作桌面浏览器。
- 清理 Luker 数据时先核对精确目标；优先移动至回收站，并保留与本项目无关的数据。
