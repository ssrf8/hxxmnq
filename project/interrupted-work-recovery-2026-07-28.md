# 2026-07-28 异常关闭任务续接说明

> 用途：供后续 Agent 直接续接三个异常关闭任务。本文只记录可由聊天记录、当前文件与离线门禁共同证明的状态；“已生成”“已接入”“已验收”严格区分。
> 当前工作区含大量未提交改动与未跟踪素材。续接时禁止回退、覆盖或批量整理不属于自己工作线的文件。

## 0. 续接前共同基线

- 项目目录：`F:/agent airp/卡/幻想乡物语`
- 恢复来源：
  - 地图：`019fa8d9-05bc-7173-b17a-fd5212f30226`
  - 像素移动／角色菜单：`019fa8e5-2662-7af0-804f-f151564f5a80`
  - 前端按钮：`019fa84a-b6ab-7eb2-a85b-00afcb9ffdb5`
- 2026-07-28 恢复复核：`npm run check:ui`、`npm test`（124/124）、`npm run build:ui` 全绿。
- 没有正式打包；`0.2.0-r54` 仍是已占用旧检查点，不得覆盖。
- 新素材目前多为 Git 未跟踪文件。它们确实在本地，但换机、清理工作区或只拉 Git 仓库时不会自动恢复；续接 Agent 不得把“未跟踪”误判成“可删除”。

## 1. 地图优化：新底图设施全形态扩展

### 已完成

- 新底图候选：`src/assets/maps/garden-base-owner-candidate-v2.png`。
- 第一批四张风格样板及色键原图、叠图预览均已落盘：
  - `magic-greenhouse-base-v2.png`
  - `fairy-garden-four-season-v2.png`
  - `moon-spring-open-air-v2.png`
  - `banquet-plaza-lantern-market-v2.png`
  - 历史生成目录已归档：`旧素材/src/assets/world/map-facilities/new-map-style-v2/`
- 四张均为 `1254×1254` RGBA，四角透明，无可见洋红色键残留。
- 所有者查看叠图后回复“看上去不错，开始下一步”；风格方向已通过，目录状态为 `owner-approved-style-calibration`。

### 原中断位置（已于 2026-07-29 收口）

- “下一步”刚开始便异常关闭；以下缺口是当时状态，已由本节末尾的 2026-07-29 记录取代。
- 当时尚未生成其余形态、三张损坏覆盖层，也尚未登记新样板到 `asset-manifest.json`。
- 当时运行时仍使用旧的 `garden-base-expanded-empty-v1.png` 和旧设施透明图层；当前运行时已经切换到新底图与正式 `-v2` 资源。

### 原续接清单（1–5 已完成，第 6 项保留实机门）

1. [x] 以已通过的四张样板锁定画风、镜头、左上光向、占地和入口方向。
2. [x] 补齐 9 张未完成形态：
   - 魔法温室：自由生长型、人偶维护型、河童自动化型。
   - 妖精花园：妖精游乐庭、冰露迷宫。
   - 月见温泉：静水观测池、雾隐汤屋。
   - 宴会广场：鬼之大宴台、符卡演武场。
3. [x] 补齐妖精花园、月见温泉、宴会广场三张低遮挡损坏覆盖层。
4. [x] 同组形态保持相同视角、足印、落脚点与视觉尺度；候选保留在 `new-map-style-v2/` 作为生成证据。
5. [x] 全形态通过叠图后更新 `asset-manifest.json`，校准 `ground_anchor`、缩放、命中多边形和标签锚点。
6. [x] 完成宽屏／窄屏离线检查；[ ] 真实 SillyTavern 锚点、遮挡、点击与菜单跟随验收。

### 禁止事项

- 不重做已经认可的四张样板，除非所有者明确要求。
- 不提前修改存档 schema、设施结算或 `current_form` 中文正式键。
- 不把新底图中背景房屋自动变成可交互设施。
- 不在全形态复核前替换运行时素材（该门已通过；后续仍不得绕过所有者视觉复核替换正式资源）。

### 2026-07-29 接手收口

- 所有者已对九种补充形态、三张损坏覆盖层和四组确定性叠图回复“全过”；地图美术候选复核门已关闭。
- 新底图、13 张形态图与 3 张损坏覆盖层已复制到正式资源族并登记到 `src/assets/asset-manifest.json`；候选、样板和色键源图均保留。
- `project/manifest.json`、`scripts/build-ui.mjs` 与 `src/ui/garden-map.ts` 已接入新底图、正式资源和四组归一化几何数据。
- 地图设施命中使用精确多边形，并优先于中央庭院等旧圆形回退区域；新增契约测试覆盖该优先级。
- 离线宽屏 `1440×1000`、窄屏 `390×844` 已完成视觉检查；类型检查、134 项测试和 UI 构建均通过。
- 未正式打包，未覆盖已占用的 `0.2.0-r54`。真实 SillyTavern 的点击、缩放／拖动后菜单跟随、损坏层与宿主窄屏仍是最终验收门。

## 2. 角色点击菜单 UI：异常任务已恢复并完成离线实现

### 最终结论（取代异常关闭时的图片接入计划）

- 所有者最终决定不采用图片按钮。四张 `target-action-*.png` 已移动到 `旧素材/src/assets/ui/` 保留为历史参考；运行时、预览构建和自包含宿主均不读取、复制或内嵌它们。
- `src/ui/app.ts` 从既有 `TargetAction` 只读派生四种纯视觉类别：
  - `talk`：普通交谈。
  - `leave`：关闭菜单。
  - `pat-head`：专属摸头互动。
  - `quest`：登记事件、设施和战斗等任务入口。
- `createBubbleButton()` 只创建受控文本符号节点；`src/ui/styles.css` 以圆形御守、顶部结绳、虚线内圈、纸札和语义配色绘制东方可爱风徽章，不依赖外部图片或模型输出。
- 桌面端保持围绕目标的动态半环，半径随 3–6 个按钮扩展；390px 为双列网格，320px 为单列布局，触控目标不小于 `44px`。
- “任务”只决定视觉类别，按钮仍显示“检查结界”“观察旧地基”等真实行动名；禁用原因增加可见文本，不再只依赖鼠标 `title`。
- 未修改 `src/ui/target-actions.ts` 的白名单、剧情条件、事务、角色移动、bridge 或结算逻辑。

### 已完成验证

- `npm run check:ui`、`npm test`（125/125）、`npm run build:ui`、`npm run package:checkpoint:dry` 全绿。
- 契约测试明确断言构建器、宿主和控制器不得重新出现四张徽章图片的运行时资源链。
- 本地浏览器已检查角色菜单、区域菜单、桌面、390px 与 320px 布局；无控制台警告／错误。
- 当前 dry-run 为 111,079,951 bytes，SHA-256 `d9b3a659bbbe228a0e3f09aca5ef06d6e0f7dbcd246a2b5762f6a9a2e2cfb21e`；未正式打包。

### 剩余真实运行时门

1. 在真实 SillyTavern 检查动态 DOM 类名的实际计算样式。
2. 检查角色移动、地图缩放和拖动期间的菜单锚点跟随。
3. 检查键盘焦点、关闭与焦点返回、禁用原因朗读和 repeated click。
4. 检查 reduced-motion、320px、短视口与宿主缩放。

## 3. 前端入口按钮：符卡副本收尾

### 已完成

- 灵梦小店入口、商店 4:3 十槽界面与魔理沙背包入口已经完成。
- 符卡副本素材已经落盘：
  - 原图：`src/assets/ui/reimu-dungeon-button-source-v1.png`
  - 透明运行版：`src/assets/ui/reimu-dungeon-button-v1.png`
- `scripts/build-ui.mjs`、`src/runtime/ui-host-shell.js`、`src/ui/app.ts`、`src/ui/index.html`、`src/ui/styles.css` 与契约测试中已存在符卡按钮接线。
- 异常关闭前本地预览曾确认约 `87×72px`、文字可辨、顶栏未被撑坏，点击仍打开原符卡副本对话框。
- 恢复后已重新通过类型检查、124 项测试和 UI 构建；未发现符卡按钮专用色键中间文件残留。

### 中断位置与剩余工作

- 中断发生在清理中间文件和最终总结附近，核心实现没有缺失。
- 后续只需在真实 SillyTavern 复查顶栏换行、窄屏尺寸、键盘焦点和点击入口；若实机正常即可关闭该工作线。
- 不要重复抠图、重复接线或另起一套符卡入口实现。

## 4. 建议的 Agent 分工

- 地图 Agent：只处理 §1 的候选美术、落点和地图资源链。
- UI Agent：只处理 §2 的角色菜单徽章；避免同时改地图绘制。
- 验收 Agent：只处理 §3 的真实 SillyTavern 收尾，也可顺带执行现有移动与地图实机检查，但不得改业务逻辑。

三条工作线共享 `scripts/build-ui.mjs`、`src/ui/app.ts`、`src/ui/styles.css` 和测试文件时，必须先查看当前差异并做窄补丁。这里是最容易让三个勤快人一起拧坏同一颗螺丝的地方。
