# 0.2.0-r16 运行验收记录

## 交付物

- 角色卡 JSON：`../dist/checkpoint-0.2.0-r16/幻想乡物语-测试检查点-0.2.0-r16.json`
- SHA-256：`bf59e4e7dc386d790e6ddbf2d671e3cee816bd3901179141042cda4050fd72d3`
- 文件大小：`19,968,027` bytes
- 生成时间：2026-07-24
- 目标环境：`F:\agent airp\Luker`
- 本轮修复：GAL 回复页面不显示、事务卡在 generating、refresh 挂死、建议回复字段别名、初始灵梦在场

## 自动与离线门禁

- `npm test`：17/17 通过
- `npm run build:ui`：通过
- `npm run check:ui`：通过
- `npm run package:checkpoint:dry`：通过
- `npm run package:checkpoint`：通过
- UI 脚本 ID：`gensokyo-garden-ui-020-r16`（唯一）
- 世界书条目：16
- 初始 `presence_snapshot.present_character_ids`：`["reimu"]`

## Luker 导入与清理

已完成文件级导入与旧产物清理：

- 已写入角色卡：`data/default-user/characters/幻想乡物语·移动庭园（测试检查点 0.2.0-r16）.png`
- 已写入世界书：`data/default-user/worlds/幻想乡物语·移动庭园 0.2.0-r16.json`
- 已删除角色卡：`0.2.0-r14`、`0.2.0-r15` PNG 与缩略图
- 已删除世界书：`0.2.0-r14`、`0.2.0-r15`
- 历史聊天目录保留（含 r15 验收过程记录），未删除

导入后请 **刷新 / 重启 Luker**，再在角色列表中确认只剩 r16。若世界书未自动绑定，手动绑定 `幻想乡物语·移动庭园 0.2.0-r16`，并启用三个角色脚本。

## 所有者验收名单（请按序打勾）

### A. 导入与挂载

1. 角色列表只见 r16，不见 r14/r15 角色卡
2. 世界书列表只见 r16 世界书（旧 r14/r15 世界书已不在）
3. 新建 r16 聊天，常驻游戏壳挂载成功，无控制台 error
4. 三个角色脚本启用：MVU loader / schema / 移动庭园界面 r16

### B. 开场（零生成）

5. 填写姓名、称谓、外貌、庭园名 → “载入资料并进入庭院”
6. **不创建 user 楼层、不调用 LLM** 即可进入庭院
7. 顶栏显示庭园名与时间；状态为“庭园状态已同步”
8. 重复点击载入：幂等成功，不覆盖已有不同开场

### C. 地图与小人（r16 关键）

9. 中央庭院可见 **灵梦** 小人（非空白地图）
10. 点击灵梦弹出目标菜单：对话 / 摸摸头 / 离开
11. 点击旧主屋弹出：检查 / 维修 / 离开

### D. 核心修复：壳内显示 LLM 回复

12. 对灵梦选“对话”，发送后进入 GAL 页
13. 生成结束后 **壳内出现旁白/台词**（不是永远“对方正在回应……”）
14. 原生聊天中能看到对应真实 user/assistant 楼层（`is_hidden=false`）
15. 建议回复按钮可点；手动输入可发；发送后再次在壳内看到新回复
16. 点击段落进度可推进多段（若有 2+ beats）

### E. 结束与结算

17. 点“结束聊天”发送收尾；收尾回复后可“返回庭院”
18. 同一会话重复结束 **不二次扣资源 / 不二次乱推时间**（检查 `settled_ids`）
19. 短暂闲聊可不推进时段；若模型写了时间变化，以 MVU 复读为准记录

### F. 旧主屋

20. “检查”可提交并在壳内看到结果
21. “维修”：若未完成灵梦结界检查，应显示前置阻断（不是误伤物资）
22. 物资仍 ≥1 且前置满足时，维修确认 → 施工动画 → 允许结果仅 `main_house_enabled` / `temporary_shelter_only`

### G. 生成控制与原生恢复

23. 生成中可停止；停止后可继续生成（不重复 user 楼层）
24. 左右 Swipe / 重新生成后，壳内文本与建议跟随切换
25. “显示原生聊天”可看到完整输入区与楼层；返回游戏后再次受控隐藏
26. 刷新页面后壳仍为单例；切到其他角色卡后壳与隐藏类清理干净

### H. 记录方式

每项只标：`通过` / `失败` / `跳过`，失败请附：

- 操作步骤
- 壳内现象
- 原生最后 1–2 楼摘要（可打码隐私）
- 控制台 error（如有）

## 已知非阻断说明

- 正式近景立绘仍是占位；r16 优先保证可玩与可显示
- 模型可能输出 `participants` 而非 `participant_character_ids`；r16 已兼容读取
- 时间推进依赖模型与事件配置，结束聊天不保证一定推进时段
- 旧 r15 聊天记录保留，但请用 **r16 新聊天** 验收（旧状态无灵梦在场种子）
