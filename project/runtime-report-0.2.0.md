# 0.2.0 运行检查记录

## 当前检查点

- 角色卡：`dist/checkpoint-0.2.0-r11/幻想乡物语-测试检查点-0.2.0-r11.json`
- 格式：SillyTavern `chara_card_v2` JSON
- SHA-256：`949abe31381cddf9123cb769b05cfc3ef68d21559198e304121f59c7787c2587`
- 大小：10,502,505 bytes
- 打包策略：dry-run 与写入分离，目标存在时拒绝覆盖。

## 静态与离线检查

- TypeScript 严格检查通过。
- 契约测试 10/10 通过。
- UI 挂载产物自包含底图 data URL，不依赖开发服务器或数据库 CDN。
- 地图不读取 `state.player`；玩家占位小人只用于符卡游戏。
- 互动按多轮真实聊天设计，并保留结束当前聊天与显示原生聊天入口。

## 地图滚轮缩放修复（r6）

1. 地图世界尺寸不再除以 `camera.zoom`，避免与 `ctx.scale()` 相互抵消。
2. 滚轮使用指数缩放，并保持指针下方的世界坐标不漂移；范围限制为 `0.8–2.0`。
3. 地图容器使用稳定响应式高度，Canvas 不再通过 `height: 100% + min-height` 形成尺寸反馈。
4. `ResizeObserver` 仅在像素尺寸实际变化时重设 Canvas，避免重复清空与回调循环。
5. 运行时预览中的真实滚轮结果：缩放值 `1.000 → 1.616 → 1.162`；Canvas 始终为 `748×444`，页面 `scrollY` 始终为 `0`。
6. 完整页面截图确认底图、区域标记和访客标记均正常绘制；浏览器控制台无 error/warning。

## r11 开场恢复修复

1. 世界书新增 `[initvar] 移动庭园初始状态`，从 `src/schema/initial-state.json` 为消息楼层提供完整初始 `stat_data`。
2. 开场 user 消息之后存在 assistant 回复、但 `meta.opening_committed` 未成立时，界面显示“确认资料并进入庭院”。
3. 按钮只解析带当前聊天事务标记的原始 user 消息，不依赖 LLM 正文或变量块格式。
4. 缺失状态先以项目初始状态补齐，再只覆盖开场字段、庭守钥状态与两个 meta 标志。
5. 写入精确落在开场后的最新 assistant 楼层，完成后复读同一楼层；验证失败时不会进入主庭园。
6. 内嵌世界书名称包含 r11 检查点版本，避免导入时复用旧世界书。

## 真实 SillyTavern 验收

- SillyTavern：`1.18.0 release`（revision `8172dcd0e`）；酒馆助手实际运行版本 `4.8.19`。
- r11 JSON 可导入，内嵌世界书与随卡脚本可启用，移动庭园 iframe 可单例挂载。
- 实际发送开场资料并取得首轮回复；本次 LLM 没有输出可用变量格式，成功覆盖目标故障场景。
- 回复完成前确定性进入按钮保持禁用，回复完成后启用。
- 点击按钮后进入“风铃庭”主界面；对应 assistant 楼层持久化了完整 `stat_data`。
- 已核对玩家字段、庭园名、庭守钥、开场 meta、初始资源、六个区域和四个设施。
- 浏览器控制台无 error。

## 旧 r6 地图验收边界

- Luker：2.7.0 release，revision `01d52e6eb`；酒馆助手 4.8.18；ST-Prompt-Template 3.17.4。
- 角色卡 JSON 可导入，内嵌世界书与随卡脚本可启用，移动庭园界面可单例挂载和收起/恢复。
- 当时 Luker 没有可用模型 API，因此 r6 的滚轮修复只在同一份构建 UI 的本地运行预览中完成实交互验收。此记录不替代 r11 的 SillyTavern 开场验收。

## 数据库边界

- 配置 URL：`https://gcore.jsdelivr.net/gh/AlbusKen/shujuku@spv8.0/index.js`。
- 本轮未启用、下载或执行全局数据库脚本，只验证数据库缺席时的 fail-soft 路径。

## 结论

r11 已在真实 SillyTavern 中关闭“LLM 丢变量格式后无法进入庭院”的开场死锁。它仍是本地测试检查点，不是正式发布版；刷新重开、重复点击、Swipe 分支和生成失败重试仍需继续做真实运行覆盖。
