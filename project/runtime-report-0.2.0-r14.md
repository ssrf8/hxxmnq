# 幻想乡物语 0.2.0-r14 运行时验收报告

## 本轮目标

- 庭院界面启用时保留 SillyTavern 原生聊天输入框与魔法棒。
- 从本卡直接切换到其他角色卡时，立即清理庭院壳层、样式和状态类。
- 切回本卡时只挂载一份庭院界面，不产生重复实例。
- 保持“填写数据后直接进入庭院，不调用 LLM”的确定性开场流程。

## 构建产物

- 文件：`dist/checkpoint-0.2.0-r14/幻想乡物语-测试检查点-0.2.0-r14.json`
- 大小：13,181,185 bytes
- SHA-256：`fc9ae4be91f6c42fa65436cb5e6a45535a9619ff52555b6de3354d1eb1d83a30`
- 世界书：`幻想乡物语·移动庭园 0.2.0-r14`
- 脚本：
  - `gensokyo-mvu-loader-020`
  - `gensokyo-mvu-schema-020`
  - `gensokyo-garden-ui-020-r14`

## 自动检查

- `npm run build:ui`：通过。
- `npm run check:ui`：通过。
- `npm test`：14/14 通过。
- `node --check dist/runtime/ui-mount.js`：通过。
- `npm run package:checkpoint:dry`：通过。
- 正式包与 dry-run 包哈希一致。

## Luker 实机检查

运行环境：Luker 2.7.0 release。

### 导入与启用

- r14 JSON 已通过酒馆界面导入。
- 内嵌世界书和三个脚本均已启用。
- 初始页可见“载入资料并进入庭院”及“不调用 LLM”提示。
- 庭院壳层报告版本：`0.2.3-host-lifecycle`。

### 原生输入区

- `#send_form`：可见，布局为 `flex`。
- `#send_textarea`：可见。
- `#extensionsMenuButton`（魔法棒）：可见。
- 庭院界面不再隐藏整个原生发送表单。

### 直接切卡生命周期

从 r14 直接切换到 Assistant 后：

- 庭院壳层数量：0。
- 庭院样式节点数量：0。
- 返回按钮数量：0。
- `body` 和 `#chat` 上的庭院激活类均已清除。
- 原生输入区与魔法棒仍可见。
- 一秒后复查无延迟回挂。

从 Assistant 切回 r14 后：

- 庭院壳层数量：1。
- 庭院样式节点数量：1。
- 原生输入区与魔法棒仍可见。
- 页面重载后再次进入 r14，挂载结果一致。

### 控制台

- 过滤 `gensokyo`、`MVU`、`host-lifecycle`：未发现本卡相关错误。
- 观察到的其他网络或扩展日志与本卡无关。

## 旧测试版本清理

- 已删除 r13 角色卡。
- 已删除 r13 世界书。
- 当前项目名前缀下仅保留 r14 角色卡和 r14 世界书。
- 历史聊天目录共 8 个，按“删除角色但保留聊天”的策略保留，未破坏用户聊天记录。

## 用户验收

1. 初始页确认原生聊天框、发送区和魔法棒都存在。
2. 不退出初始页，直接切换到其他角色卡，确认庭院窗口立刻消失。
3. 切回 r14，确认只出现一份初始页。
4. 填写开场资料并点击“载入资料并进入庭院”，确认过程不触发 LLM。
5. 进入庭院后刷新页面，确认资料与庭院状态可以恢复。
6. 发起第一条真实行动，确认此时才进入正常 LLM 对话流程。

2026-07-23，所有者确认 r14 验收通过。r14 作为后续 r15 GAL 单壳交互重构的已验收运行基线；它仍是 `0.2.0` 迭代检查点，不等同于 M1 完整第一版发布。
