# 0.3.0-r5 正式候选发布记录

> 已被 0.3.0-r6 取代：所有者在最终交付前要求关闭设置测试面板，因此 production manifest 已推进到 UI r6。r5 的不可变对象和本地产物仅保留作历史回滚，不作为最终交付。

日期：2026-08-11

## 发布结论

- 卡类型：SillyTavern `chara_card_v2`，MVU + Tavern Helper 嵌入式脚本，远程 production UI。
- 运行 profile：`standalone-mvu`。
- UI 通道：`production`，版本 `r5`。
- 公共素材：继续使用 R2 live generation 8；本次没有修改或重传素材 manifest。
- 创作者注释：`creator_notes` 为空字符串，JSON 与 PNG 解码结果均确认长度为 0。
- 状态：离线发布门通过；真实 SillyTavern 导入与运行验收仍待执行，因此标记为正式候选而非实机已接受版本。

## R2 UI

- URL：`https://ssrfrrt.ccwu.cc/gensokyo-moving-garden/live/ui/profiles/standalone-mvu/ui-mount-r5.js`
- 字节数：2,243,823
- SHA-256：`f7ecf118ceb3f95b1640594a1e436740993f9bbf28d463de2fea8bbcc65e90d4`
- 缓存：版本化 UI 为 `public, max-age=31536000, immutable`；profile manifest 为 `no-store`。
- 上传顺序：先创建不可变 UI 对象，再条件更新 production manifest；两项均通过公网逐字节、MIME、Cache-Control 与 SHA-256 读回。

## 正式产物

### JSON

- 路径：`dist/checkpoint-0.3.0-r5/幻想乡物语-正式版-0.3.0-r5.json`
- 字节数：329,189
- SHA-256：`a5e6304fda184f37a82b38205c533413935ebfe2e5f4c6a39a1b102101f42d2c`
- 世界书：`幻想乡物语·移动庭园 0.3.0-r5`，30 条内嵌条目。
- UI 脚本：`gensokyo-garden-ui-020-r5`，内嵌 4,881-byte production loader；不含 test manifest 引用。

### PNG

- 路径：`dist/checkpoint-0.3.0-r5/幻想乡物语-正式版-0.3.0-r5.png`
- 字节数：1,273,524
- SHA-256：`0f4e088d1afde4459f1afa1893eff106b9ee82a956d5d28c4ecbfacf3ca8cfdf`
- 画布：512×853，可由 `pngjs` 正常解码。
- payload：恰好一份 `chara`；base64 解码字节与正式 JSON 完全一致。

## 验证

- `npm run check:ui`：通过。
- `node --test --test-concurrency=1 tests/*.test.mjs`：754/754 通过。
- production remote UI build：通过。
- UI publish dry-run：通过。
- production R2 UI 上传与公网读回：通过。
- production package dry-run：通过。
- JSON parse、正式卡名/版本、世界书绑定、loader 通道与 `creator_notes`：通过。
- PNG 签名、结构解码、payload 数量和 JSON parity：通过。
- `git diff --check`：通过。

## 运行依赖与边界

- 卡内已嵌入 MVU 固定版本加载器、MVU Schema、production UI loader、初始 `stat_data` 与绑定世界书。
- 宿主仍需项目既定的 SillyTavern、Tavern Helper、MVU/MagVarUpdate 能力；Node、TypeScript、esbuild、sharp 与 pngjs 仅是开发期工具，不是玩家安装项。
- UI 和媒体依赖 HTTPS R2；loader 会校验 production manifest、同源路径、字节数与 SHA-256，失败时不降级执行未校验代码。
