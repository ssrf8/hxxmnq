# 咲夜逐图生图序列 v2（已拒绝）

本轮按所有者指定的第一种方案，逐张调用内置 imagegen：

- 当前 `正常/NNN.png` 作为唯一动作、四向布局和步态权威；
- `../sakuya-turnaround-v1.png` 作为咲夜身份与服装权威；
- 从第二帧起原计划以本轮 `001` 作为身份一致性锚点，但不得覆盖当前动作。

## 最终裁定

所有者于 2026-07-28 判定本方案**验收不合格**，工作立即停止。

- 仅 `raw/001.png` 已正式复制进项目，作为失败试作留档。
- `002–005` 的并行调用在完成前被中止；可能遗留于 Codex 默认生成目录的临时结果
  不属于项目产物，也不得补录。
- `006–023` 未调用。
- 不得继续生成剩余帧，不得登记 `asset-manifest`、sprite registry 或运行时。
- 不得把 `raw/001.png` 当作后续方案的身份锚点。

统一状态：`owner-rejected-imagegen-sequence-v2`。
