{
  "name": "gensokyo-moving-garden-card",
  "private": true,
  "version": "0.2.0",
  "type": "module",
  "scripts": {
    "build:ui": "node scripts/build-ui.mjs",
    "check:ui": "tsc --noEmit",
    "package:checkpoint:dry": "node scripts/package-checkpoint.mjs --checkpoint=0.2.0-r23 --dry-run",
    "package:checkpoint": "node scripts/package-checkpoint.mjs --checkpoint=0.2.0-r23",
    "preview": "node scripts/preview-server.mjs",
    "test": "node --test tests/*.test.mjs"
  },
  "devDependencies": {
    "esbuild": "0.25.6",
    "typescript": "5.8.3"
  }
}
